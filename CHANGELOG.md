# 变更记录

## 2026-09-11 — Agent 执行、证据与上下文边界

本次合并此前累积的实现、回归、离线诊断脚本与测试策略，作为一个相互依赖的版本快照提交。

### Agent 与模型

- 对话框支持选择模型，并贯穿新任务、后续消息与恢复；默认使用 `deepseek-flash`，兼容历史模型标识及自定义端点。
- 保留供应商 reasoning/tool-call 协议、结构化内部响应契约、截断续写和逐次传输计量。
- 累计单轮 token 停止默认关闭；96 次物理请求、超时、无进展、权限及交付保护仍保留。
- 模型工具 schema、参数约束、压力估算和执行准入使用同一当前能力定义；未执行拒绝返回结构化恢复指引。

### 持久证据与恢复

- 工具原文、事件持久化与模型可见投影分离，增加哈希绑定的 session-local 归档及 `read_context` 分页/查询。
- 阶段控制提供有界证据定位符；上下文压力包含真实出站 system、工具与补充控制，不用持久化对象大小冒充请求大小。
- 收敛、任务计划/需求绑定、来源覆盖、内容审阅、成品身份与最终交付使用独立证据边界，保留失败与未知状态。
- 研究正文、同快照分页、引文/数字/声明与来源角色校验，以及研究依赖和参考资源修复能力可分别恢复。

### 视觉与交互

- CSS 完整证据解析与精简 profile 分离；新增绘制属性取证，同时保留 DOM 连接、变量消费及无效证据负例。
- 加强参考模板/字体/资源身份、原位内容编辑、可见性、文字几何、紧凑尺寸和外部控件遮挡验证。
- 优化思考/进度流式展示及预览生命周期，持久 workspace 预览不再仅因预览进程休眠而关闭。

### 测试与运行

- 将测试拆分为模块、集成、浏览器与显式付费验收；日常/CI 测试注入离线凭据并拦截真实模型 Provider。
- 增加按 import 依赖选择受影响测试、只读失败快照回放、统一付费预检/计量入口；封存未接入预算的旧 live smoke。
- CI 取消同分支过时运行，主分支保留完整离线测试，PR 运行受影响边界。命令与验证边界见 [TESTING.md](TESTING.md)。

### 发布边界

- `.env`、`.anera*`、`reports/`、构建目录和依赖目录不进入提交。
- `evidence/` 中既有公开验收记录保留其原始来源与历史实现指纹；不能把它们当作本次代码的重新验收证明，也不为消除版本差异而改写指纹。
- 离线回归和构建通过不证明真实模型生成质量、实际账单节省或生产部署完成。本次提交不执行付费生成或额外部署。

### 本次提交检查结果

- `npm run typecheck`、`npm run build`、`npm run build:showcase` 通过。
- `npm run test:full`：146 个文件、2,686 个用例；2,677 个通过，9 个失败。本次不是全绿版本，未跳过或弱化这些断言。
- 失败分布：`browser-manager.test.ts` 的跨内页布局验收 1 项（紧凑视口中导航遮挡正文）；`reference-language.integration.test.ts` 的恢复/组合调度 8 项（工具能力、任务绑定和独立审阅阶段相关假设仍需更新并复验）。
- 针对失败尝试更新夹具后仍存在未解决差异，实验性测试改动未纳入提交；保留原测试作为后续修复基线，不把局部复验当作全量通过。
- `npm audit --audit-level=high` 通过：0 高危、0 严重，仍有 3 项中危（Vitest、其 mocker 及 qs）；本次未引入大版本依赖升级。
- 构建后公开证据完整性检查仍有 146 项历史实现指纹差异，没有其他来源/产物完整性失败；旧指纹不改写。
- 暂存区敏感信息与路径扫描通过，命中的 token 样式字串为脱敏测试夹具；未包含本地密钥、会话数据或个人绝对路径。原始模板夹具保持其 SHA-256，单文件行尾空格例外见 `.gitattributes`。
