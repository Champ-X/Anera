# Anera Agent Harness 能力收敛矩阵

更新日期：2026-08-31
范围：桌面 Agent Harness；移动端明确排除；Arena 精确 parity 暂缓；完整 GitHub Connector 与真实账户覆盖延期

## 1. 当前判定

当前 active Harness 已达到“内部能力闭环基线”：19/19 active tools 均有 schema、执行器、结果回灌、持久事件和至少一个真实 DeepSeek 控制循环 oracle。最新 schema `anera-harness-convergence/1.1` 综合报告为 [`reports/real-smokes/harness-convergence-2026-08-31T14-16-55-172Z.json`](evidence/harness-convergence-summary.json)：9/9 场景通过，42 次工具调用，58 次模型调用，active duration 82.066 s，519,582 prompt tokens、5,305 completion tokens、509,696 cached prompt tokens，cache-hit ratio 98.10%，账本估算成本 `$0.04431934`；命令以 exit 0 完成。Browser screenshot Artifact→任务内 `inspect_image`→`present_file`、provider-backed voice selection、Plan revise/accept、compaction、预期失败恢复和动态 connector 均在同一报告闭环。

在工具闭环之外，最新不可变的完整内部质量 gate 为 [`reports/quality-benchmarks/harness-quality-2026-08-31T22-45-02-834Z.json`](evidence/quality-benchmark-summary.json)：数据分析、对抗 SQL 数据库分析、对抗财务对账、代码与多文件工程、依赖升级、XLSX/DOCX/PPTX、状态机、附件与长文档、事故交接、Website、视觉重建、矢量 PDF、普通研究与对抗 Web 研究共 18/18 通过，平均质量 100，全部 critical checks 与 efficiency budgets 通过，0 failed tools。schema `anera-harness-quality/2.0` 同时记录 115 physical model requests、115 metered model calls、118 tool calls、494.455 s active duration、95.01% cache hit 与估算成本 `$0.18591499`；schema-v3 implementation fingerprint `44749946440acddfe36eaff50f64e29e66b6930a51e4555dcd7dc123d58516c8` 绑定 benchmark verifier、源码入口、依赖锁及完整 production runtime，共 52 个文件。五项 trajectory-blind DeepSeek Judge 合计 5 model/0 tool、11.793 s、`$0.00258046`，只进入独立 `evaluationUsage`。视觉重建在 1200×800 下以真实 DeepSeek Vision、HTML、Browser 交互与独立 PNG oracle 达到 0.942333 similarity，12 model requests/calls、9 tools、59.173 s、零失败、`$0.01776528`；XLSX、DOCX、PPTX 与两页 selectable-text/vector PDF 均由独立结构、语义与渲染 oracle 验证。Agent 与 Judge 均显式使用 `DEEPSEEK_TEMPERATURE=0`。这些是 Anera internal quality gate，不是 Arena parity 分数。

这里的“收敛”只表示 Anera 自身能够完成受控任务，不表示与 Arena 的策略、时序、质量、成本或像素相同。综合 smoke 的 Agent 推理与工具选择使用真实配置的 DeepSeek；其中第三方 provider 使用确定性 fixture，以隔离 Harness 控制问题和外部波动。联网能力另有低频真实 canary：`reports/real-smokes/active-research-2026-08-30T10-05-17.356Z.json` 验证真实 DeepSeek 经 Tavily 搜索和 Firecrawl 抓取两个独立来源、以 exact evidence URL 完成 Final；schema 1.2 的 [`reports/real-smokes/web-provider-cache-2026-08-31T15-04-12.389Z.json`](evidence/live-web-provider-summary.json) 为 `passed:true`，验证 Tavily 搜索 1 次/5,080 B、显式绕过 Pexels 后的 Tavily `include_images` 回退 1 次/44,440 B 与 5 张已准入并持久化图片，以及 Firecrawl 769,253 B 四分块页面在同 turn 重复首块和 continuation 均为 0 外部调用、跨 turn 重新抓取。外部成本保持 `null/not_available`；两份报告均不保存密钥或页面正文。

原录屏提示最新一次绑定实现指纹的 production-bundle live canary 也已闭环：[`reports/real-smokes/html-slides-2026-08-31T22-50-17-228Z/report.json`](evidence/html-slides-live-summary.json) 为 `passed:true`，16/16 checks 全通过，完整执行 research → 20,905 B canonical HTML → Website → Browser open/单次导航 → 138,131 B screenshot → 同路径 `inspect_image`/`NO DEFECTS` → `present_file` → 唯一 Final。该 run 为 10 model requests/calls、10 tools、80.528 s、估算 `$0.04182607`，0 tool failure/timeout，并记录文本/视觉模型身份与 implementation fingerprint schema v3 聚合值 `56a56ce8379333b83081e2b85832969c4a968cfab18dbf4ce92fc0e28d5fd099`。v3 递归绑定 verifier 自身以及完整 production server/shared/client runtime，共 51 个文件；任一运行时模块变化都会使 attestation 失效。它是被测 Anera 构建的能力证据，不是 Arena 同题 trace、成本或像素 parity。

最新工程 gate 为 53 个 test files / 963 项测试全部通过，typecheck、production build 与 `npm audit --audit-level=high` / 0 vulnerabilities 通过。schema v15 的 [`reports/arena-public-contract/latest.md`](evidence/public-contract-summary.md) 对当前 deployment 为 PASS，并只把 frozen `list_files(path?)` / `read_file(path)` 计入 Arena contract；runtime `list_files(cursor,limit)` / `read_file(offset,content_offset,limit)` overlays 单独门禁、不计入 parity。最新 [`reports/ui-execution-log-v28/capture-summary.json`](evidence/ui-state-coverage-summary.json) 的 62 个 `1440×900` desktop-only 状态为 `passed:true`，0 console error、0 horizontal/outer vertical overflow；schema-v3 implementation fingerprint `95e14908…d1bc` 绑定 verifier、fixture、关键源码与完整 production runtime 共 55 个文件，62 张截图均记录并复核 bytes/SHA-256。8/8 conversation-follow、四阶段 persistence、reload high-water/no-replay、右栏 ownership、Bash 自动展开、Workspace file viewer、结构化 HITL、运行中默认展开 Thought、完成态无残留 `Thinking...`、HTML 内联 renderer 与 docked Preview 均通过。用户提供的 `logo.png` 已进入 production asset graph、左栏与 Leaderboard header，并复用为 favicon；实拍无拉伸、裁切或栏宽漂移。流式 `write_file` 的 partial JSON、中文 UTF-8 byte、Workspace draft 与 draft→durable handoff 已有确定性测试；active-write screenshot 冻结录屏原提示、文件名、末尾 8 行/行号、Workspace UTF-8 字节、锁定 Composer 与 Stop。登录态 Arena 手工 corpus 与完整录屏审计见 [`reports/arena-video-audit-2026-08-31-071811/report-source.md`](evidence/arena-video-audit-summary.md)：研究意图与来源 ledger 独立判定，零成功检索时 Final 与 `present_file` 均 fail closed；只有同任务真正成功的 `present_file` 才允许交付型短 Final 不重复 URL，`verification_required`/`notExecuted` 不算发布成功。当前主体 P0=0、P1=0；由于仍无同任务、同 viewport、同状态的 Arena DOM/PNG 对，这些证据不能表述为像素 parity。

Arena reference 的只读 intake 见 [`reports/arena-reference-corpus-2026-08-30/README.md`](evidence/arena-reference-corpus-summary.md)：43/65 个任务有目录、共有 50 个 run 目录，16 个 run 有 metadata，其中 9 个 task version 1.1/1.6/1.7/1.8 的历史结构化 reference 已确定性重建为 231 个 canonical 事件。生成器已用唯一 ID Artifact CSV、legacy tool body 和 Agent 终态边界增强输出，旧的 7 个 source canonical 仅作历史输入，不再声称与 curated 输出逐字节一致。9 个 run 的独立任务判定为 5 pass、3 partial、1 fail，而 Arena UI 全部显示 success。当前 v2.0 eligible reference 和 exact-version Anera candidate 都是 0，Arena calls/tokens/cost 也不可见，因此该 corpus 用于 schema/行为校准，不是 parity 或效率分数。

这 9 条 reference 已足以约束主体 Harness 的若干经验契约：A02/U03 的 Bash 非零 exit 必须显示为失败且保留分栏 `COMMAND/STDOUT/STDERR`；A03 的 `ask_user` 进入 awaiting-user 并在同一 episode 恢复；U02 的 Website/Process 暴露用户可见名称、命令、managed process ID、OS PID 与端口；U03 的历史/刷新恢复重放 trace、Final、Workspace 与 Review，但不能虚构未恢复的 Website；A01 则证明 Review 可与 `Workspace Updating` 同屏，不能把 `Workspace saved` 写成 Review 的前置条件。Anera 已把这些作为独立状态机和 UI 投影实现，但历史 v1.x 证据仍不能推出 v2 exact parity。

最新公开契约审计时间为 `2026-08-31T14:53:22.577Z`。

公开 prompt 契约也已从 anchor 检查升级为整段门禁：schema v15 解析并冻结 5,124 字符 ordinary、3,021 字符 Coding 与 666 字符 closed guidance，随后把固定输入下的普通、Coding-active、Coding-closed 本地 builder 与公开模板逐字节比较。Coding Session 的 repository authority 只进入 system 层；新 clone 实际切到唯一 `arena/...` branch，status 和 prompt 选择跨 later turn、Resume、compaction 保持。Anera 另在运行时 convergence overlay 明示当前远程边界，并通过受控 broker 放行固定分支 push，以及限定的 PR、issue、run、workflow 与 release 读写形式。高影响 metadata/comment/review/merge/close/reopen、workflow dispatch/state、run rerun/cancel/delete 和 release create/edit/delete/upload 都先进入 durable Approval；批准前不获取 credential、不创建 release staging、不执行底层命令，批准后重新读取 Session/connection/scope 并重建命令。repo/base/head/branch/ref/target 只来自 Session，所有不可信 option value 变为单一 `--flag=value` argv。Release asset 只接受最多 16 个 Workspace 内普通文件（单个 50 MiB、合计 200 MiB），拒绝 symlink、目录、内部/宿主/绝对/遍历路径、glob 与 `#` label，并在批准后从已验证文件句柄复制为隐藏临时快照再上传。PR create 只有真实成功才迁移 `pr_open`；PR merge 只有固定 repo/head/base 的 GitHub read-after-write oracle 返回 `merged_at` 才迁移 `pr_merged`，即时 merge 的 oracle 不可用时保守进入 `closed`。公开 closed guidance 保持字节不变；Anera overlay 仅为未合并的 `closed` Session 明示 approval-gated `gh pr reopen` 例外，成功后恢复 `pr_open`。普通 Bash、scope override、PR URL/number、未建模远程命令、`closed` 下的其他远程操作与全部 `pr_merged` Session 均无权限。独立 production writeback canary 现已实现 production connector/bootstrap/broker/ToolExecutor 路径、双固定分支、远端 SHA、PR close→reopen→reclose 双状态 oracle、Issue、workflow run、release upload bytes/hash、真实 merge oracle 与 identity-checked cleanup；当前无专用私库配置，只有 `blocked_configuration` 报告，不能冒充真实通过或 Arena parity 证据。

## 2. 收敛分层

| 层级 | 通过条件 | 当前状态 |
|---|---|---|
| C0 工具面 | 名称、参数 schema、默认值、结果 union 可验证 | Arena frozen baseline 的 19 个名称/顺序及 definitions 通过；其中 frozen `list_files` 仅有可选 `path`，frozen `read_file` 只有必填 `path`。实际 provider 使用同一 19 名称/顺序的 Anera runtime definitions，分别为 `list_files` 叠加 `cursor/limit`、为 `read_file` 叠加 `offset/content_offset/limit`；两项 overlay 单独测试，不计作 Arena exact-schema parity |
| C1 执行器 | 成功、参数错误、超时/取消和副作用边界有测试 | 19/19 通过；外部 provider 受配置约束 |
| C2 Agent loop | 真实模型能选择工具，结果能进入下一步并形成 Final | 19/19 通过 |
| C3 生命周期 | HITL 暂停/恢复、Plan revise/accept、Process、compaction、失败恢复可闭环 | 通过；required-event 监听器内的即时 HITL 响应不丢失，mixed/parallel batch 可在重启后原序恢复；桌面 resolved answer 紧随原 card，selected label/custom response 可见且刷新后不丢失或重复；Cancel 在 abort-ignoring tool 上快速发布终态，取消后的普通 follow-up 不要求 Resume |
| C4 Durable | turn/tool/workspace/usage/terminal 可恢复且幂等 | 已有完整 crash/restart suite；晚到 provider usage 与已结束 run 解耦，shutdown drain 已开始的 durable settlement/usage queue |
| C5 任务质量 | 产物、文件内容、进程状态、引用、交互行为和调用序列由 oracle 验证 | 工具综合 smoke 9/9；最新单一指纹化完整批次十八类任务 18/18、平均 100、critical checks 全通过；XLSX/DOCX/PPTX/矢量 PDF 有独立 oracle，视觉重建有 DOM/Browser/交互/PNG oracle，附件、长文档、事故与两类研究均有直接 `tools=[]` 的轨迹不可见盲评；录屏原提示 HTML Slides 的 fingerprinted live E2E 全检查通过 |
| C6 效率成本 | active latency、calls、tokens、cache、估算成本可统一读取并进入逐任务预算 | 十八类任务 efficiency budgets 全通过；预算校准依据写入每题报告；Judge 使用 `evaluationUsage` 独立分账；DeepSeek/vision/image/speech 均进入 durable ledger、Credits 和 session token limit。Vision 按官方六档独立价格计费：off-peak hit/miss/output 为 `$0.007/$0.22/$0.66` 每百万 token，peak 为 `$0.014/$0.44/$1.32`；peak 仅限工作日 UTC `[01:00,04:00)` 与 `[06:00,10:00)`。每个 physical Vision dispatch 固化当时费率，跨峰谷 retry 逐次累加，inspector 返回的 request-time cost 随 usage exactly-once 持久化并结算 Credits，不在 settlement/replay 时重算。`modelRequests` 统一计每个物理 provider dispatch；`modelCalls` 计有可归因 metering 的调用，通常来自 provider usage，Speech 因二进制 API 无 usage 而使用持久化、带方法标记的 heuristic。DeepSeek 缺 usage 的 HTTP 错误/断流以 `estimatedCostStatus=unknown|partial` 保留，不把 `$0` 当完整成本。Speech API 不返回 usage，语音 token 与费用是带方法标记的估算，真实账单仍需 provider canary 对账 |
| C7 生产 provider | 不使用 fixture 的外部账户与真实 API canary | DeepSeek、Tavily 与 Firecrawl 已验证；媒体、GitHub 私库只读、GitHub writeback 和 Linux/Bubblewrap 的 fail-closed canary 已实现并生成阻塞报告，但当前缺凭据/专用账户仓库/适用主机，不能整体标为通过 |
| C8 Arena parity | 同题 Arena trace、DOM/PNG、质量和成本成对达标 | v2 suite 执行器已升级为结构 fail-closed `anera-eval-suite/2.0`：固定 65 个 task ID、AF01–AF16、H01–H57、active 19 工具机会、零 unmapped、逐题协议配对，以及 trace/source/task/path/hash/唯一性；opaque raw evidence 的语义真实性和 visual report 内部判定链仍是证据生产方信任根，因此 `v2_semantic_provenance` 正式 release gate 当前强制关闭，结构分只作诊断。已有 9 条 historical reference、0 条 v2.0 eligible reference、0 条 exact-version candidate，正式运行仍暂缓 |

## 3. Active 19 工具

| 工具 | 综合场景 | 闭环证据 | 判定 |
|---|---|---|---|
| `add_voice` | voice | 两个 provider-backed audition、HITL 选择、per-Session 原子 voice ID/owner 持久化、重启等待期间保留试听文件并在选择恢复后清理 | 通过；并行双卡、duplicate first-writer、restart 与 cross-call ID 冲突回归通过；真实 provider key 待 canary |
| `ask_user` | ask/plan | 结构化问题、等待不计 active time、同 turn 恢复 | 通过 |
| `bash` | file/process | sandbox、cwd、timeout、输出与文件 reconciliation；非零 exit 令 tool terminal 失败，同时保留 process payload、exit/duration 和 stdout/stderr；详情分栏 `COMMAND/STDOUT/STDERR` | 通过；A02/U03 历史 reference 校准了失败恢复与可见分栏 |
| `compact` | file/process | 单次显式调用、强制 checkpoint 被下一步消费 | 通过 |
| `edit_file` | file/process、plan | fuzzy replace、文件 hash/event、plan revision | 通过 |
| `fetch_page` | research | Firecrawl 优先正文提取、精确 chunk continuation、turn-scoped 有界 LRU、同 URL 并发 miss 排队、DNS-pinned socket、连接后 remoteAddress 与逐跳 redirect/final-URL 校验、失败安全直抓回退 | 通过；真实 Firecrawl RFC/MDN 与四分块 cache canary 已闭环，物理 calls/bytes/cache outcome 独立计量；直抓 PDF 以 MIME + 前 1,024 字节 `%PDF-` sniff 准入，下载上限为 `maxReadBytes×10`，最多 30 页，伪造/损坏/超限 fail closed，page/byte marker 与 `hasMore` 只表示真实 chunk continuation |
| `generate_image` | media | 编辑调用、图片签名、Workspace/Artifact、再次视觉读取 | 通过；`offer_options` 每 turn durable 上限 3，必须配置两个 distinct provider model ID 且实际分流，否则 provider 前 fail closed；真实 provider key 待 canary |
| `generate_speech` | voice | 已选 provider voice；`.mp3/.wav/.ogg/.opus/.flac/.aac/.m4a/.aiff` 真实容器验证；音频落盘、Artifact/present | 通过；每 turn durable 上限 10；audition/synthesis 使用 canonical BCP-47，持久 audition language，并在无法证明 script/region 兼容时于 provider 前拒绝；真实 provider key 待 canary |
| `get_process_output` | file/process | log wait、终止后状态确认 | 通过 |
| `image_search` | media | Pexels 优先、缺失/失败/空结果时 Tavily `include_images` 回退；下载/缩略图/来源与 redirect 逐项公网准入，fragment 去重，PNG/JPEG/WebP/GIF 嗅探，Workspace 归因，Tavily provider usage | fixture 与 schema 1.2 真实 canary 均通过；live run 显式绕过 Pexels，以 1 次 Tavily/44,440 B 保存 5 张已准入图片 |
| `list_connector_tools` | connector | task-scoped schema 动态加载 | 通过 |
| `list_files` | file/process | Arena frozen schema 仍为 `path?`；Anera runtime overlay 以 Session/path-bound immutable manifest 增加 opaque `cursor` 与 `limit=200,max=500`，按 64 KiB JSON 与 count 双预算返回 `hasMore/nextCursor/truncated/totalFiles`；10,000 files 与 20,000 entries cap 分离，snapshot policy 排除 runtime/cache/dependency/credential 路径并跳过 symlink | 通过；cursor 可重试、重启续读、跨 Session/损坏/过期 fail closed；Workspace snapshot 首批 500 + Load more/Refresh/error/cap UI 不再静默截断；未消费 cursor chain 受 compaction 保护 |
| `present_file` | research/media/voice/plan/recovery | Artifact viewer 打开事件和多种文件类型；研究 Artifact 在发布前验证 task-local retrieved URL ledger | 通过；零成功检索、缺少 exact retrieved URL 或 unsupported URL 均 `verification_required/notExecuted`，只有真实成功发布才可承担交付 Final 的引用 surface |
| `propose_plan` | ask/plan | revision→re-propose→accept 门禁 | 通过 |
| `read_file` | 多场景 | Arena frozen definition 仅含必填 `path`；Anera runtime overlay 为文本增加 1-based `offset`、0-based UTF-8 `content_offset` 与可选 `limit≤5000`。普通页按完整行和 byte/line 双预算返回 `hasMore/nextOffset`；超长单行按 code-point-safe byte fragment 返回严格前进的 `nextContentOffset`，行完成后才切换到 `nextOffset`；图片经 vision bridge 变为主 Agent 可消费描述 | 通过；production bundle + real DeepSeek 的 `reports/real-smokes/read-file-pagination-2026-08-30T22-51-36-367Z.json` 对 249,158 B / 1,200 行文件（第 601 行 170,021 B）精确走完 `offset 1→601`、`content_offset 0→80000→160000`、`offset 602→EOF` 五页，0 回读，以 7 model / 5 tool、41.249 s、317,777 tokens（199,424 cached）、`$0.04634327` 发布逐字 Final `PAGINATION-MARKER-731\|PAGINATION-TAIL-926`，`passed:true` |
| `start_process` | file/process | durable managed process 与启动输出 | 通过 |
| `stop_process` | file/process | 完整进程组终止与持久状态 | 通过 |
| `web_search` | research | Tavily 优先搜索、depth 映射、结果 schema、全部 provider 结果 URL 的公网/DNS/凭据准入、fragment canonicalization、去重、稳定 citation ID、安全 HTML fallback 与独立 provider usage | 通过；兼容 `.env` 中的 `TAVILY_API_KRY`（亦接受标准 `TAVILY_API_KEY`），真实 Tavily URL-admission 与 RFC/MDN canary 已闭环 |
| `write_file` | 多场景 | 原子 Workspace mutation、Artifact 与精确内容 oracle | 通过 |

## 4. Extension 与系统级能力

| 能力 | 已有闭环 | 当前边界 |
|---|---|---|
| 附件与 attachment-only | Arena frozen baseline 保持 15-MIME allowlist；Anera runtime Composer overlay 接纳 DOCX/XLSX/PPTX，仅允许 extension/MIME 精确配对并在持久化前验证 OOXML ZIP 与类型必需 entry；上传 staging、SHA-256、唯一命名、trusted trailing block、PDF/Office routing/pagination、Undo 草稿恢复 | 已通过 upload/attachment/pagination/crash smokes；Office overlay 是 Anera capability，不计作 Arena attachment parity；M04 冻结 provider rejection 后两个上传仍保留、普通同 Session retry 可读取并完成；双 PDF 跨文档约束决策、逐文件逐页引用和独立盲评通过 |
| Vision | 上传图 `inspect_image`；active `read_file(image)` 自动 vision bridge；usage/replay/failure 都进入工具状态机；按官方 [DeepSeek Vision guide](https://api-docs.deepseek.com/guides/vision) 使用 OpenAI-compatible [`/chat/completions`](https://api-docs.deepseek.com/api/create-chat-completion) 与 `image_url` data URL。本地字节嗅探 PNG/JPEG/GIF/WebP，decoded image 上限为 `min(operator, 32 MiB)`、读后复核，宽高均不超过 8,192；最终 serialized inline JSON body 另有 48 MiB 上限，违规输入在 dispatch 前失败 | Transient recovery 在两个可能的 length attempt 间共享预算（默认 2、硬上限 5）：只重试 HTTP 500/503、明确 network transient 与 `insufficient_system_resource`；HTTP 400/401/402/422 永久失败。指数退避及合法 `Retry-After`/`retry-after-ms` 均 ≤60 s 且可被 abort。不可变 live [`vision-task-2026-08-30T20-29-46-421Z.json`](evidence/live-vision-summary.json) 为 `passed:true`：2 次 `inspect_image` 合计 3 physical/3 metered calls、11,570 tokens；历史 runtime 记录 Vision `$0.01138157`、Session `$0.02556954`。该 run 为 Sunday/off-peak，官方重算为 Vision `$0.00692296`（原值 `+64.403%`）与机械调整 Session `$0.02111093`，差 `$0.00445861`；原报告不回写，完整公式/SHA/来源见 [`vision-pricing-reconciliation-2026-08-31.json`](evidence/live-vision-summary.json) |
| Browser | open/snapshot/ref、fill/select/check/click/press/scroll/viewport/console/screenshot、同源网络限制、Abort 关闭 Context；snapshot 文本和 interactive refs 均按真实视觉状态排除 hidden/aria-hidden/display:none/visibility:hidden/近零 opacity/零尺寸控件，陈旧隐藏 ref 立即失败而非等待点击超时，并纳入可见伪元素文字 | 真实 Chrome + DeepSeek smokes 已通过；成功的任务内 screenshot + 同路径 image Artifact 动态开放 `inspect_image`，失败/路径不匹配/历史 Artifact 不扩张工具面；`path` 可作为 screenshot 输出兼容别名 |
| Website | static/managed preview、per-Process port ownership、唯一 HTML 自动入口、cwd-aware entry、basename path 锚定、显式打开的 40:60 dock、idle sleep、Restart、browser rehydrate | 根目录列表不再冒充应用；单一 HTML 自动成为 preview URL，多候选时不盲猜；Website 不自动抢开 Preview；同一 Session 的并发 Restart 在服务端合并为一次重启，客户端期间禁用按钮并显示 loading、失败后恢复；managed/static 两条失败路径都持久化为 `failed/restart_failed`，不会遗留 `starting`；Website 行显示用户可见名称/验证端口，Processes 保留名称、命令、managed ID、OS PID、端口和状态；公网托管语义不在 Harness 内 |
| Deployment | approval-gated immutable revision、stable URL、manifest/recovery/redeploy | 本地和 crash smoke 通过；需要外部 `ANERA_PUBLIC_BASE_URL` 才是公网 URL |
| 外部写入 | `http_request` 仅 POST/PUT/PATCH/DELETE，逐次 Approval，Deny 跨 Continue 持久阻断 | Deny 真实 Agent 通过；未主动执行真实外部写入 |
| Dynamic connectors | Composer task enablement、list-and-load、schema validation、task/resume/compaction 保持、disconnect fail-closed | synthetic 与 GitHub adapter fixture 通过；App token 已修正为 `/installation/repositories`，真实账户 canary 入口已就绪但当前配置阻塞 |
| GitHub Coding | App installation/OAuth fallback、repo/branch pagination、bounded bootstrap、真实 session-fixed `arena/...` branch、独立 Coding system prompt、closed/pr_merged local-only guidance、受控 push、PR/issue/check/workflow/release broker、durable HITL、credential confinement | 精确 push；PR create/status/view/checks/diff/list/merge/edit/close/reopen/comment/review；issue create/status/list/view/edit/close/reopen/comment；run list/view/watch/rerun/cancel/delete；workflow list/view/run/enable/disable；release list/view/create/edit/delete/upload 均有固定 scope 重建与回归。高影响操作批准前零 credential/零 command，批准后重校验；`closed` 只开放当前固定 PR 的审批式 reopen，成功恢复 `pr_open`，`pr_merged` 永久封闭；release asset 使用 Workspace 普通文件快照；merge 由可信 oracle 判定 `pr_merged`。Disconnect 撤销 lease、option-smuggling 防护、错误 scope/命令零执行和 token 对 event/model 零泄漏均通过。真实 private-repo read 与显式 opt-in writeback canary 入口均已实现；当前两者都缺真实专用账户配置，writeback 只有默认阻断报告，尚无外部 passed 证据 |
| Context | provider-token anchor、cache-aware compaction、显式 compact、overflow recovery、length continuation；未消费的 `read_file` 与 `list_files` 分页链保护 | 单测、协议 smoke 和真实长上下文任务通过；同一 current-task/path/cursor chain 的 newest page 到达后续 Assistant step 之前，全链保持 verbatim，避免普通 result compaction 丢失前页证据；消费后可压缩，pressure-forced compaction 可为硬上下文边界覆盖该经济保护 |
| Admission/HITL | single-run reservation、Credits、ask/plan/voice/image choice、Approval、显式 `awaiting_user`、16/step 与 96/run 工具预算、6 路 parallel-safe 背压；等待时间排除 | 通过；`ask_user` 的 options/Skip/custom/Submit 桌面卡片与 `run.status=awaiting_user` 已接线，回答恢复原 episode；required 事件监听器内立即回答不会丢失，parallel HITL 全部完成后才恢复完整 batch；v28 浏览器 gate 证明 resolved answer 紧邻 card、selected label/custom response 可见且 `persistedAfterRefresh:true`；Approval transport/API 失败会保留错误并重新开放 Approve/Deny，成功则保持锁定等待 durable event；并行 `add_voice` 的 public ID 在 Session durable 临界区内分配；超额调用完整回灌但不执行 |
| Stop/timeout/Continue | run/tool/model-first-event deadline、Abort-ignoring tool boundary、部分输出持久、Resume | 通过多组真实与 crash smoke |
| Durable recovery | pending start/terminal/upload/workspace/shell/process/deployment/usage checkpoint、durable `process.updated` ownership snapshot、voice ID owner ledger、同一 `pendingTerminal` checkpoint 内独立 `events` / `workspacePersistenceEvents` lane 与各自 publication marker；scanning 前缀后两 lane 可独立交错，按预分配 ID exact-once replay 与 duplicate/cross-turn call-ID 配对边界 | 通过进程级 SIGKILL suite；成功终态发布 `scanning→uploading→saving→saved`，当前模式明确为 `local_durable` 且 `blobCount:0`，不宣称 immutable CAS；mixed HITL + completed-tool batch 按原 assistant call 顺序重建；同一 voice call replay 幂等，不同 call 不可复用 ID；U02 六源码文件端到端 oracle 证明 `dist/node_modules/.next/build` 与凭据文件不进入 tree、Shell reconciliation、Artifact、终态 bytes/file count 或 ZIP，物理运行时目录保留 |
| Security | workspace/symlink、DNS-rebinding/TOCTOU-safe SSRF transport、CSP/iframe、cross-origin write、secret redaction、Seatbelt/Bubblewrap | 所有外网 adapter 均固定准入 DNS 到 socket lookup、保留 Host/TLS SNI、复核 remoteAddress、逐跳重解析 redirect 并跨 origin 剥离 auth/cookie；macOS 真实通过；Linux 主机 canary 待补；Windows 为 policy-only |
| Observability/eval | append-only events、snapshot/SSE、canonical JSONL、trace diff、suite gate、visual capture | Anera 自测完整；snapshot 先按全量 events 投影，旧 Chat/旧 seq 不覆盖新状态，SSE 从 hydration high-water 续订且不重播旧 Workspace 保存动画；Arena 成对输入暂缓 |
| Desktop UI | 无 Session 副作用的 `/agent` New Chat draft、Composer、Thought/tool/HITL/Plan/Artifact/Final/feedback/Undo/Workspace/Preview/Connections/Credits、current command/file execution groups、输出贴底/暂停跟随/`Scroll to bottom`、`/history/search` 会话搜索、`/leaderboard/agent` Ranking/Pareto/筛选/Models-Labs/列编辑 | [`ui-execution-log-v28`](evidence/ui-state-coverage-summary.json) 62-state desktop gate 通过并绑定 55 文件实现指纹；Anera logo 在左栏/Leaderboard 无裁切，运行中 Thought 默认展开、完成态无残留 `Thinking...`，HTML 内联 renderer 与 docked Preview 同屏可见；`ask_user` pending/resolved-after-reload、Workspace/Preview ownership、A01/A02 persistence/Review 交错、reload high-water/no-replay、Workspace 文件页内 viewer、active HTML Writing、8/8 conversation-follow interactions，以及 command/file group 的 running/completed/auto-expanded/timed-out 状态均进入机器门禁；draft smoke 另验证本地附件、首发单次物化、上传失败迁移/重试、Search/Leaderboard/Back/Forward；全部 62 态为 `1440×900`，移动端排除 |
| Speech contract | provider 原生 MP3/WAV/Opus/FLAC/AAC；`.ogg` 投影为 Ogg Opus；`.m4a/.aiff` 由 bundled FFmpeg 转封装/转码；最终容器和时长由 `music-metadata` 验证 | 8 种公开扩展名与伪音频拒绝均有回归；不依赖宿主 FFmpeg |
| Speech metering | provider calls、输入字符、provider/delivered bytes、实测时长与显式 token estimate 持久化；稳定 call identity exactly-once 结算 | audition/synthesis、成功/部分失败均纳入 ledger；Speech endpoint 无权威 usage，`1 token / 50 ms` 仅作为明确标记的估算 |

## 5. 综合场景

自然语言质量 gate：

| 任务 | 确定性 oracle | 质量分 | 结果 |
|---|---|---:|---|
| `natural_data_analysis` | CSV 聚合、排序、计算结果、取消订单排除说明和报告内容 | 90 / 100 | 完整批次为 90（旧 oracle 未识别 `Exclusions`）；oracle 修复后的独立复跑为 100，critical / efficiency 通过 |
| `adversarial_sql_database_analysis` | SQL dump、关系查询、状态/区域/层级对账、异常分类和数据内注入 | 100 | critical / efficiency 通过；6 model / 6 tool、零失败工具 |
| `adversarial_finance_reconciliation` | 重复交易、取消交易、有效/孤儿退款、区域净收入和 CSV prompt injection | 100 | critical / efficiency 通过；trusted CSV 直接并行读取，语义计数/异常 oracle 通过 |
| `natural_code_repair` | 失败复现、代码修复与测试结果 | 100 | critical / efficiency 通过 |
| `natural_multifile_feature` | 双模块实现、公开测试、隐藏输入、防硬编码和变更说明 | 100 | critical / efficiency 通过 |
| `natural_dependency_upgrade` | 精确 npm registry 升级、CJS→ESM 迁移、lockfile 与隐藏测试 | 100 | critical / efficiency 通过；只使用专用 registry installer |
| `natural_office_workbook` | XLSX 工作表顺序、公式/缓存值、精确跨表引用、样式与呈现 | 100 | critical / efficiency 通过；冻结/筛选/宽度/样式与可见美元 currency format 通过 |
| `natural_office_document` | DOCX 标题层级、编号、分页、动态页码字段、表格映射与呈现 | 100 | critical / efficiency 通过；真实 PAGE field 与语义结构 oracle 通过 |
| `natural_office_presentation` | PPTX 页面结构、内容、视觉语义、文件有效性与呈现 | 100 | critical / efficiency 通过 |
| `natural_state_machine_feature` | 多分支状态机、非法迁移、恢复/重试与公开/隐藏测试 | 100 | critical / efficiency 通过 |
| `natural_attachment_decision` | 两份双页 PDF 提取、硬约束逐项比较、正确推荐、精确文件名/页码引用、local-only 工具策略 | 100 | critical / efficiency 通过；trajectory-blind Judge 10/10 |
| `natural_long_document_synthesis` | 强制多页 continuation、跨文档硬门槛、四项 blocker 与逐页引用 | 100 | critical / efficiency 通过；trajectory-blind Judge 通过 |
| `adversarial_incident_handoff` | SEV-1、rollback/resolution gates、最早时间、owner/deadline 和伪 system-message injection | 100 | critical / efficiency 通过；trajectory-blind Judge 10/10 |
| `natural_website_build` | 自包含 HTML、真实 preview、三卡片、筛选/确认交互、桌面无溢出 | 100 | critical / efficiency 通过；隐藏控件不再进入 interactive refs，favicon 404 仍是唯一忽略的 console 噪声 |
| `natural_visual_reconstruction` | 参考图视觉理解、语义化可编辑 HTML、精确内容/布局、七根 bar、24h 交互、1200×800 PNG 对照 | 100 | critical / efficiency 通过；0.942333 similarity，12 model requests/calls / 9 tools、零失败工具 |
| `natural_pdf_report` | 两页 selectable-text/vector PDF、页面规格、文本/表格/图形、边距、页眉页脚和 raster 健康度 | 100 | critical / efficiency 通过；独立解析和像素渲染 oracle，6 model / 5 tool、零失败工具 |
| `natural_research_decision` | 多来源证据、约束比较与可追溯建议 | 100 | critical / efficiency 通过；trajectory-blind Judge 10/10 |
| `adversarial_web_research` | 当前第一方与陈旧 reseller 三源证据、精确冲突值、权威性/时效消解、网页提示注入防护与唯一推荐 | 100 | critical / efficiency 通过；三页抓取、三 URL 引用、trajectory-blind Judge 10/10 |

执行命令：

```bash
npm run bench:harness-quality
```

该命令先构建 production bundle，再用真实 DeepSeek 运行十八类自然/对抗任务；网站和视觉重建检查只使用桌面 viewport，移动端不属于任务或 gate。附件、长文档、事故与两类研究 Judge 直接调用配置的 DeepSeek，固定 `tools=[]`，只接收任务、权威事实和最终 Artifact，不接收候选轨迹；Judge 的 token、调用、时延和成本只进入顶层 `evaluationUsage`。

Active-tool 综合场景：

| 场景 | 核心能力 | 结果 |
|---|---|---|
| `no_tool_efficiency` | 简单请求不误用工具、exact Final | 通过；1 model / 0 tool，1.007 s active |
| `file_shell_process_compaction` | 文件 CRUD、Shell、后台进程、显式压缩 | 通过；13 model / 11 tool |
| `research_to_artifact` | search→fetch→write→present、来源真实性 | 通过；5 model / 4 tool |
| `image_search_generate_present` | 搜图→视觉桥接→编辑生成→视觉复核→呈现 | 通过；8 model / 5 tool |
| `browser_screenshot_to_vision` | HTML→Website→Browser screenshot→任务内动态 `inspect_image`→呈现 | 通过；8 model / 6 tool，严格 1 screenshot / 1 inspect、零失败工具 |
| `voice_selection_to_speech` | 同 provider 试听→选择→合成→呈现 | 通过；7 provider / 3 tool，其中 3 次为 speech provider call |
| `ask_and_plan_revision` | ask_user、计划修订、接受后才执行 | 通过；8 model / 7 tool |
| `tool_failure_recovery` | 首次工具确定性失败、结果回灌、换策略完成 | 通过；1 个预期 failed tool，无重复失败调用 |
| `dynamic_connector_loading` | list-and-load→动态 schema tool→Final | 通过；3 model / 2 tool |

执行命令：

```bash
npm run test:harness-convergence
```

## 6. 已修复的关键闭环缺口

1. `add_voice` 原先用浏览器本地 SpeechSynthesis 试听，但最终合成使用服务端 provider voice，试听与成品不是同一音色。现在有 provider 配置时会并行生成两个真实 provider audition，UI 播放对应临时音频，选中后持久化完全相同的 provider voice；临时文件在决策后清理。无 provider 配置时只显示明确的 `Preview locally` 降级。
2. `read_file(image)` 原先把 image-data 直接放进 `deepseek-chat` 的下一次请求，文本模型会在工具成功后以 provider 请求失败终止。现在 Harness 在 tool deadline 内调用专用 vision model，把受信边界下的视觉描述写入持久 tool message，移除发往主文本模型的原始 image part，并将视觉 usage 纳入同一 ledger。视觉桥接失败会成为正常 `tool.failed`，不会绕过工具状态机把整轮直接打成 provider failure。
3. 普通文本、附件和 custom-feedback 已统一到 Arena-shaped message envelope：无附件使用 `message:{text}`；上传描述进入 `message.metadata.uploads`，仅图片生成 `file` part；part 顺序为 custom feedback→images→text；outer metadata 与 `v2Source` 统一验证。
4. 明确要求 Web 且单文件/自包含的任务进入 canonical artifact 模式：未显式要求计划时直接执行；首次完整 HTML 写入后锁定 canonical path、收缩 mutation schema，拒绝再次整文件覆写但保留 `edit_file` 定点修复，并要求最短 Browser 验收链。大型 mutation 参数只在至少一个后续 Assistant step 消费后才压缩，历史正文移出 `content/old_text/new_text` 并写入独立 `_historicalMutation` 元数据，避免模型把 hash/marker 当作当前文件；高置信度 edit context miss 会返回有界的当前近似片段，而不是强制整文件重读。React/Vite 和普通多文件项目不进入该模式。
5. Managed preview 在服务根路径只是目录列表时会探测 server cwd；只有一个 HTML 候选便将其设为 `website.previewUrl`/`entryPath`，支持子目录 cwd 和 `/home/user`、`~/` 公共 Workspace 命令映射。多 HTML 候选保持根路径并给出 warning，不做不可靠猜测。
6. Browser snapshot 从 DOM 文本近似升级为视觉语义投影：文本和 interactive refs 都过滤真实不可见/零尺寸节点，陈旧隐藏 ref 会立即返回可恢复错误而不消耗 8 秒点击超时；可见文字包含 CSS `::before`/`::after`，描述性 aria-label 控件和动画后的最终过滤状态可被 Agent 稳定验证。
7. 双 PDF 决策任务把 attachment-only 能力提升到跨文档质量门：必须调用 `extract_attachment`、比较五项硬约束、识别各自交付风险、推荐唯一合格方案，并以完整文件名或等价文档别名加页码逐页追溯；Web/Bash 均被负向 oracle 禁止。
8. 完整 18 题质量基线固定为 `anera-harness-quality/1.9`，把 XLSX、DOCX、PPTX 分成三个真实 Office 任务，加入图片理解→语义化可编辑 HTML→Browser 交互→PNG 对照、两页 selectable-text/vector PDF 和对抗 Web 研究任务，并用分项语义 evidence 评估财务 Markdown 中的 raw/deduplicated counts、zero-valued source groups 与 exception IDs，不依赖单一表格布局。release gate 明确要求 configured semantic judges 和 `requireZeroFailedTools:true`；附件、长文档、事故与两类研究候选完成后由直接 `tools=[]` 的 DeepSeek 请求做 trajectory-blind 语义评审，只看任务、冻结事实和 Artifact；其 `evaluationUsage` 与候选 `summary`/efficiency budget 分离，避免把评审成本伪装成 Agent 执行成本。当前 runner 已升为 `anera-harness-quality/2.0`，新增源码、依赖锁与 production bundle 聚合 fingerprint 以及显式 `providerIdentity`；在完整 18 题 2.0 重跑前，不能用局部回归替换这份完整 1.9 基线。
9. Browser screenshot 原先虽然落成 image Artifact，但 extension router 只识别上传、`image_search` 和 `generate_image`，单文件模式又会收缩 `read_file`，造成“截图已保存但不可视觉消费”。现在只有本任务内成功的 screenshot 且存在同路径 Artifact 才动态开放 `inspect_image`；失败调用、路径不匹配和历史图片均不开放。Browser 平铺 schema 中自然混淆的 screenshot `path` 兼容映射到 `screenshot_path`，而 convergence vision fixture 必须实际读取 PNG，不能为不存在的文件伪造成功。
10. 复杂计算任务增加通用效率协议：最多一个短 helper script，把计算和断言放在同一脚本；成功后不再创建第二份 inline cross-check，也不全文重读刚写入的脚本。trusted text/CSV/JSON/source uploads 直接按已知路径并行 `read_file`，不再用 Bash/list/head/cat 重复探测；财务对账从曾出现的 15/15 调用降至当前权威批次 6 model/6 tool，并保持 100 分。
11. 新增长尾对抗 oracle：财务 CSV 中的指令注入、重复/取消/孤儿退款，以及事故附件中的伪 `<arena-system-message>`、SEV/rollback/resolution 时间窗。计数、零值区域和否定状态 oracle 修复了数字子串和“NOT SEV-1”误判；时间 fixture 明确连续区间，不把数据歧义算作模型错误。
12. 左侧历史栏的 `Search` 原先只有外观、没有行为。现在它对 Session title 与 last message 做 NFKC/大小写/空白归一化的多词搜索，标题命中优先于摘要命中；支持鼠标、上下键、Enter、Escape、backdrop、`⌘/Ctrl+K` 和真实 `/agent/:id` 导航。production bundle 浏览器验收覆盖 3 个实际结果、第二项键盘选择、路由变化、Escape 关闭、快捷键自动聚焦与 0 console error。
13. 通用 `edit_file` miss 恢复不再只在单 HTML 模式依赖全文诊断读取。修改仍严格 fail-closed；若旧上下文保留一个强、唯一、非标点锚点，错误结果会附带最多 2,000 字节的当前附近片段，Agent 用这些精确字节直接重试。弱/重复锚点仍要求读取。SQL 质量 gate 同时修正 USD 与 cents 等价表格的 oracle，避免把正确 Artifact 错判为失败。
14. Office 创建路由会按需开放 `install_npm_packages` 与 `extract_attachment`，禁止把 `openpyxl/python-docx/python-pptx` 误交给 npm 或绕到 pip。生成脚本只使用 workspace-relative 路径，写后用同一库做业务断言，再由独立 OOXML parser 复核工作表顺序、公式目标、缓存值和内容后才允许 `present_file`；质量 gate 另从 ZIP XML 验证冻结行、精确筛选范围、列宽、样式和带美元符号的 number format。
15. 文本 Agent 与 trajectory-blind Judge 的 DeepSeek 请求默认显式发送 `temperature:0`；`DEEPSEEK_TEMPERATURE` 仅接受 0–2，用于明确需要采样多样性时覆盖。该改动把此前依赖 provider 默认温度的连续全量波动收敛为当前 18/18 gate。
16. Browser `open` 的 `width/height` 原先会被静默忽略，导致模型以为在 1200×800 验收、实际仍是默认 1440×900；现在两者成对传入时会真实设置 viewport，缺一项时结构化失败。视觉 Web 任务同时冻结 DOM 与 vision 的证据优先级：Browser snapshot/action 是精确文本、控件状态和 ref 的 authority，vision OCR 只判断布局/颜色/间距/裁切/重叠；`inspect_image` 会约束为短答，对 `finish_reason=length` 只做一次内部压缩恢复，并把两次 usage/call count 聚合到原工具调用。最新完整批次视觉任务为 12 model requests/calls、9 tools、0 failed tool、59.173 s、`$0.01776528`、0.942333 similarity，质量为 100。
17. New Chat 不再在点击时创建 idle Session。`/agent` 直接访问、刷新、Search/Leaderboard 返回和文件选择都停留在浏览器内存草稿；首次普通提交才创建一个 Session、按顺序上传本地 `File`、发送 Arena-shaped message 并进入 `/agent/:id`。若上传中途失败，已上传路径与剩余本地文件一起迁移到这个 Session，重试复用同一 ID。production Chromium smoke 和 62-state v28 capture 分别冻结失败恢复/字节持久化与桌面 route/无历史选中边界。
18. `web_search` 优先走 Tavily，`fetch_page` 优先走 Firecrawl；`.env` 同时接受既有拼写 `TAVILY_API_KRY` 与标准 `TAVILY_API_KEY`，并接受 `FIRECRAWL_API_KEY`。两者均有 SSRF/redirect/final-URL 校验、取消、脱敏、turn-scoped cache、同 URL 并发去重和 `provider.usage`；搜索结果自身也逐条通过公网 URL/DNS/凭据准入，再按无 fragment canonical URL 去重并重排 citation ID。真实 canary 证明 Tavily 搜索结果准入、Firecrawl 多页抓取与跨 turn cache 边界。网页内容一律是不可信证据，冲突必须列出双方精确值并按来源权威性与时效消解。
19. 工具参数预检只对已启用、已有合法 `path`、唯一缺少 `content` 的 `write_file` 做一次模型内部修复；原调用不会产生 durable `tool.started` 或副作用，修复请求完整计入 model calls/tokens/cost，只有修复后的真实调用持久化。其他工具、JSON 或 schema 错误保持 Arena-shaped 公开失败语义，避免预检范围改变 provider-backed duplicate settlement。
20. PDF 与计算生成的 first-pass 约束已收敛：所有可见 PDF 字符串只在 page specification 定义一次，renderer 不硬编码第二份；footer/page number helper 必须被实际调用，横向累计布局在生成前断言，未变化 PDF 禁止重复提取。分组计算从完整 source dimension 建表，过滤后仍输出并断言零值组。最终 PDF 为 6 model/5 tool 理想路径，财务零值区域和对抗 Web 研究均在同一 18/18 报告通过。
21. 工具 batch 原先会把同一 model step 中所有 parallel-safe 调用一次性启动，异常输出可放大为 Tavily/Firecrawl/Vision 等 provider 的突发请求；单 step 与单 run 也没有独立调用预算。现在默认最多 6 路并发、16 calls/step、96 calls/run，resource-key 队列仍保证同 URL `fetch_page` 串行且 mutation 继续作为顺序屏障。超额调用持久化 `tool.started → tool.failed(tool_budget_exceeded, notExecuted)` 并回灌一一对应的 tool message，底层 executor 零调用；空 provider call id 只在持久化前合成无冲突 ID，重复非空 ID 保留为同一个 exactly-once provider 计量关联。新增专项回归冻结峰值、输入结果顺序、屏障、跨 step/run 预算和全部 response slots；最新真实 DeepSeek 9/9 综合报告在默认边界下没有非预期失败。
22. npm 可能在大量 `TAR_ENTRY_ERROR` 后仍以 exit 0 返回，旧 installer 因而会把不完整的依赖树误报为成功。现在公开工具只有在逐个读取 `node_modules/<package>/package.json`、确认 package name 匹配且 version 非空后才成功；scoped/unscoped/versioned spec 均被解析。exit-0 伪成功和 transient npm 错误在同一公开调用内部最多重试一次，不产生第二个 Agent tool call。确定性回归覆盖缺失 manifest、错误 name、scoped package 和首轮伪成功；真实 `reports/real-smokes/package-install-2026-08-30T09-31-12-836Z.json` 证明精确一次 registry installer、物理 `vite@5.4.19`、manifest/lockfile/canonical trace 全通过，4 model/3 tool、67.027 s、`$0.00401527`。
23. 研究 Final 原先完全依赖 prompt 自觉，随后第一版 gate 又把“研究意图”错误等同于“已有成功 ledger”，并让任意 Artifact mutation 绕过 Final surface。现在 citation-bearing research intent 与来源 ledger 独立判定：零成功 `web_search/fetch_page/web_fetch` 时 Final 与 `present_file` 都 fail closed，并驱动模型先检索；已有 ledger 时要求至少一个 exact evidence URL 且无 unsupported URL。Artifact 只有在同任务 `present_file` 真正执行成功后才能承担引用 surface，`verification_required`、`notExecuted` 或失败结果均不能放行交付型短 Final。显式 exact-only 输出约束仍优先，纠正诊断不进入 Arena canonical trace。专项集成覆盖零 ledger Final→检索→唯一 grounded Final，以及 Artifact 首次发布被阻止→检索→定向 edit→恰好一次真实 present→Final；旧 `active-research` live run 只作为无需纠正的正常 provider 路径证据。
24. Cancel 后 abort-ignoring provider tool 可以在 50 ms 有界边界后让 run 快速落成 terminal，同时底层稍后返回的 usage 仍以稳定 identity exactly-once 结算。晚到结算现在由独立 background settlement 集合跟踪；`shutdown()` drain 已开始的 settlement 和 per-session usage queue，保证返回后 SessionStore 可安全关闭/删除，但不等待永不返回的 tool 或 60 秒 deadline。新增确定性断言要求 shutdown 后所有已观察到的 settlement 都写回 `usageEventId`，修复了全量并发测试中的临时目录 `ENOTEMPTY` teardown 竞态。

25. Coding 过去复用普通 Agent system prompt，并把 repo/base 信息作为 user message 尾注；这既弱化 authority boundary，也缺少 Arena 固定工作分支、GitHub 操作和 closed-session 约束。现在独立 builder 按公开模板投影 owner/name、cwd、base branch/SHA、Arena branch、patchset caps 和 status；bootstrap 在验证 base SHA 后实际 `checkout --no-track -b arena/...`，provider 每一步按持久 Session 重建 prompt。`closed|pr_merged` 注入公开 local-only guidance；运行时 overlay 对当前 Anera 能力进一步 fail-closed：`active|pr_open` 的精确固定分支 push 会取得一次性 askpass/network lease；严格 shell-word parser 接受限定的 PR、issue、run、workflow 与 release 形式，从受信 Session 注入 repo/base/head/branch/ref/target，并统一重建 shell command。不可信 value 以单一 `--flag=value` 传递，command 长度与 word 数有硬上限。PR create 只有真实成功才持久切换 `pr_open`。Disconnect 撤销 lease，Git/gh 子进程关闭交互、pager、editor、hooks、helper 和 proxy；完整字符串、token 不泄漏、legacy fallback、错误远程命令零底层调用与 live/local drift 均有确定性回归。

26. 高影响 GitHub mutation 不再用“从 denylist 删除”来冒充支持。Broker 首次解析只返回操作专属 Approval 文案；用户拒绝时 credential lease 和底层 command 都是零，批准后 ToolExecutor 重新读取 Session 状态并再次经过同一 parser。普通 mutation 在等待中关闭、closed-reopen 在等待中变为 `pr_merged`、或 connection 断开都会零命令 fail-closed；只有 Session 仍为未合并 `closed` 时，当前固定 repo/head 的 `gh pr reopen`（可带一个 `--comment`）才可在审批后执行并于成功时恢复 `pr_open`。PR/issue 的 edit/close/reopen/comment/review/merge、run rerun/cancel/delete、workflow run/enable/disable 与 release create/edit/delete/upload 都走该两阶段状态机。Release create/upload 仅接受最多 16 个 Workspace-relative 普通文件（50 MiB/个、200 MiB 总计），预检和批准后二次校验均拒绝 symlink、目录、内部路径、宿主/绝对/遍历路径、glob、`#` label 和重复 asset name；实际 CLI 只看到 `.tmp` 下从已打开文件句柄复制的 0600 快照，完成后删除。PR merge 成功后使用同一可撤销 token 查询固定 repo/head/base；只有 `merged_at` 可信响应写入 `pr_merged`，即时 merge 在 oracle 失败时回退 `closed`，auto/disable-auto 保持 `pr_open`。桌面 Approval card 现消费服务端 title/description，而不是统一显示 generic external request。以上已有 broker、ToolExecutor、Agent/API 与客户端投影回归；`npm run canary:github-writeback` 也已实现真实 production 路径、PR close→reopen→reclose 双 oracle、marker oracle、部分成功资源再发现和 best-effort cleanup，但当前缺专用可删除私库配置，不能把 blocked report 记作通过。

27. Active `image_search` 不再把 Pexels 缺失视为媒体能力缺失。Pexels 不可用、失败或没有合格结果时，Harness 调用 Tavily `include_images`；下载 URL、缩略图、来源页和每个 redirect hop 都经过公网 HTTP(S)、无凭据和 DNS/private-network 准入，fragment canonicalization 后去重，只允许字节嗅探为 PNG/JPEG/WebP/GIF 的内容进入 Workspace/Artifact。查询无法生成 ASCII 文件名时使用稳定 hash，Tavily calls/bytes/outcome 进入独立 `webProviderUsage`。schema 1.2 live canary `reports/real-smokes/web-provider-cache-2026-08-31T15-04-12.389Z.json` 已通过：显式绕过 Pexels 后仅调用 Tavily 1 次、读取 44,440 B，并把 5 张通过 URL 与图片 magic 检查的结果持久化到 Workspace；外部成本保持 `null/not_available`。
28. Managed Process 端口不再使用 Session 级 union。Linux 从 `/proc` 校验 guardian 的 managed ID、PID/start-time identity、process group/后代 socket inode，并要求进程树和 listener 的双侧快照稳定；`listeningPorts`、`newPorts`、preferred `port` 和 Website Preview 都只引用该 Process。ownership 结果先以独立 `process.updated` snapshot 持久化，写入完成前不暴露 Preview port；持久化失败会清空内存 port/listeners。ownership unknown（包括 Windows）一律 fail closed，同 Session、cross-Session 或宿主 decoy port 都不得借用。确定性回归覆盖 IPv4/IPv6 descendant listener、torn snapshot authoritative clear、持久化失败和三类 decoy；真实 Linux `/proc`/Bubblewrap canary 已实现同类 oracle，但尚未在适用 Linux 主机运行。
29. Approval 与结构化 HITL 已从“服务重启即过期”升级为 durable pause/resume。checkpoint 保存原 turn/step/call、请求、阶段、first-write-wins 的决定/回答、预分配 required/resolved event ID、call index 和媒体候选；resolver 在发布 `hitl.required` 前已注册，因此 required-event 监听器内立即响应也不会丢失。重启可幂等补齐 torn event，按原 assistant tool-call 顺序重建 mixed HITL + completed-tool batch；parallel HITL 必须全部完成后才恢复整个 batch，不插入 operator Continue。duplicate/cross-turn call ID 以 durable turn/call-index 边界配对，不能串到错误 terminal。`generate_image` 直接复用候选字节；`add_voice` 在等待重启期间保留 audition 文件（含下划线路径），选择恢复后再安全清理。Deny 保证零外部执行；已批准且进入外部执行、但缺少 durable terminal 的请求明确返回 `outcome_unknown` 并禁止盲重放；Deployment 仅在已有可信 terminal projection 时恢复已知结果。旧执行帧缺失的 pre-HITL media usage 会显式标记 unavailable，不伪造计量。

30. 任务级恢复回归补齐 M04/S05：provider rejection 后两个上传附件仍保留，普通同 Session retry 可直接读取并完成；取消前台 Bash 会终止 guardian tree、保留取消前已产生的输出，并允许下一条普通 follow-up 直接开始，不要求 Resume。

31. 并行 `add_voice` 不再在 SessionStore 临界区外从旧 voices snapshot 推导 ID。`settleHitl` 现在把 `voice_id` 分配与 first-writer human response 放进同一个 per-Session durable transaction，`commitVoiceSelection` 再原子写入 provider/language 与 `sourceCallId` owner。相同 call replay 幂等；不同 call 即使 provider/language 相同也不能复用 public ID；legacy 无 owner 记录只有在仍存在同 call/ID durable reservation 时才可认领。并行双卡、同卡竞争响应、pending 清理后冲突、落盘后 shutdown/restart 与真实 speech routing 均有回归。

32. 桌面对话输出新增显式 follow state。长 Session hydration 默认定位最新输出；靠近底部时 SSE、流式文字和卡片 resize 继续贴底；wheel/touch/key/pointer 上滚会暂停跟随并保留阅读位置，显示 icon-only `Scroll to bottom`，点击后恢复跟随。reduced-motion 使用即时滚动，Final 进入对话 viewport 后的 Task Completion 入口保持原语义。[`reports/ui-execution-log-v28/`](evidence/ui-state-coverage-summary.json) 将 hydration、top/expanded control、阅读位置、点击到底/隐藏、SSE resize 与 Task Completion ingress 共 8 项纳入 passed/exit gate，并冻结 Bash 自动展开、Workspace 文件 docked viewer、A01/A02 persistence 交错、reload、右栏 ownership、运行/终态 Thought 以及 HTML 内联与 docked Preview。pending/resolved-after-reload HITL 的唯一 answer bubble 仍紧随 card，selected label/custom response 可见，刷新后持久且不重复。

33. `read_file` 的 Arena frozen definition 与 Anera runtime capability 不再混写。公开审计继续冻结 `read_file(path)` schema；实际 provider-facing registry 保持相同 19 个名称和顺序，为文本读取叠加 1-based `offset`/`limit` 和 0-based UTF-8 `content_offset`。普通页仍按完整行预算推进；超长单行在 code-point boundary 上分片，以严格前进的 `nextContentOffset` 续读，完成该行后切换到 `nextOffset`，terminal page 去掉两类 cursor。普通 result compaction 在 newest page 尚未被后续 Assistant step 消费时保留同 current-task/path 全链。production bundle + real DeepSeek 的 schema-1.2 live smoke 对 170,021 B 单行走完两个 byte continuation 后继续至 EOF，五次读取无回读并发布精确 marker pair。

34. `list_files` 与桌面 Workspace 不再分别处于“Agent 无界返回”和“UI 500 项静默截断”的相反失效模式。Arena frozen definition/result validator 保持 `list_files(path?)`/原 union；Anera runtime 独立增加 `cursor/limit` 和 runtime result validator。首次扫描生成 Workspace 外、Session/path/mode/limit 绑定的 immutable manifest，按 UTF-8 path bytes 排序，cursor 可幂等重试并跨进程重启，损坏、过期、跨 Session、换 path/limit 全部 fail closed；每页同时受 500 items 与 64 KiB JSON 限制，10K file cap 与 20K entry traversal cap 独立显式标记。HTTP snapshot 和 Load more endpoint 使用隔离 namespace，UI 只把已加载 tree 用于展示/HTML picker，并显式呈现 hasMore、lower-bound、Load more、Retry 和 Refresh。普通 compaction 会保留 newest 未消费 cursor 的精确 predecessor chain。

35. 录屏暴露的长文件 `Writing` 体验已按安全事务边界实现：DeepSeek 工具参数 fragment 以 `assistant.tool_call.delta` 耐久投影，客户端可增量解码未闭合 JSON string、显示最后代码行与 UTF-8 byte 数，并在 Workspace 中把未提交路径作为 running draft；真实文件仍只在完整参数通过校验、`write_file` 成功后经原有原子路径提交。可见 draft 后 provider 断流不重放，失败/中断 draft 不成为文件或 Artifact，canonical trace normalizer 也不把瞬态 delta 计入 Arena 对齐。确定性回归覆盖顶层 JSON 字段识别、partial escaped JSON、surrogate 中置分块、中文 UTF-8 byte、provider 断流不落盘、Workspace provisional 计数和 draft→durable handoff；`ui-execution-log-v28` 另以录屏原文件名冻结了 422 行写入的末尾 8 行、Workspace UTF-8 字节和运行态控件。

36. 对用户提供的 259.660 秒完整 Arena 录屏已完成逐帧审计，证据、时间轴和 F/I/U 边界见 [`reports/arena-video-audit-2026-08-31-071811/report-source.md`](evidence/arena-video-audit-summary.md)。审计发现的 research-Artifact 来源缺口已在 `present_file` admission 关闭：研究意图即使零 ledger 也 fail closed，任务内成功检索结果形成 URL allowlist，文本/HTML/数据 Artifact 受 2 MiB 首尾扫描，PDF/OOXML 要求同文件提取验证，缺失或 unsupported citation 返回 `verification_required` 且不发布。按当前普通 Agent Mode v1 范围，P0=0、P1=0；`HTML Slides` 确定性意图路由、完成 gate 与原提示 production-bundle live E2E 均已通过，报告以 source/package-lock/bundle fingerprint 绑定最终实现。Arena A/B 成对比较仍为 N/A；完整 GitHub Connector、移动端和 exact pixel/trace parity 继续明确延期，不反向阻断主体封板。

37. 三条独立 P2 inventory 债务已关闭。`findWebsiteEntry` 不再复用 500 项 UI tree，而是以 20K 明确预算、`AbortSignal` 和目录增量遍历发现 live build entry，回归覆盖第 500 项之后的 HTML、预算耗尽与取消。作为 legacy compatibility surface、并不进入当前 Arena active 19 的 `grep_files`/`glob_files` 改为 async-generator 逐文件消费，达到 match/file/result cap 就停止，确定性 traversal-budget 回归证明不会先物化后续目录。Shell reconciliation 的 `workspaceFileSnapshot` 默认完整捕获，不再静默停在 2,000 个文件；显式 support limit 会 fail closed，并支持取消，2,005 文件回归冻结最后路径仍被纳入。三条路径继续跳过依赖、缓存、凭据与 symlink，不以扩大扫描面换取正确性。

38. 旧 `npm run test:search-tools` 仍要求非 active 的 `glob_files→grep_files`，真实 DeepSeek 因而正确收到 `not enabled` 后改走其他工具，造成 smoke 假红。现在它冻结当前 Arena active 路径 `list_files→bash grep→Final`：production bundle 报告 `reports/real-smokes/active-search-tools-2026-08-31T12-05-52-470Z.json` 为全部 checks true，4 model/2 tool、0 failed tool、30,482 tokens、估算 `$0.00244130`，精确参数、工具顺序与唯一 Final 全部通过。该证据验证 Anera active routing 与效率，不把 legacy search extensions 误计入 Arena parity。

39. 普通 Agent 的文件与 Shell live smoke 已从 legacy surface 迁移到当前 active 19。`test:shell-protocol` 现在只允许 `write_file→bash→Final`，报告 `reports/real-smokes/shell-protocol-ses_eb7b0dea15e243e0b8ff.json` 为 4 model/2 tool、0 failed tool、`$0.00434412`，并验证 Bash 参数、结构化 stdout、canonical shell 与唯一 Final。`test:file-tools` 只使用 `write_file/edit_file/bash/list_files/read_file`；最新 production-server 报告 `reports/real-smokes/file-tools-ses_eb11a66914364f6c9409.json` 为 `passed:true`、9 model/9 tool、0 failed tool、94,297 tokens、`$0.00764929`，最终 `config.ts`/`summary.txt` 精确字节、删除结果、active tool sequence 与 canonical trace 均通过。该 smoke 现在自动落盘完整机器可读报告。`create_file/apply_patch/delete_file/shell_command` 只保留兼容性或故障注入用途，不再被普通 active smoke 当作成功标准。

40. production SSRF 策略仍拒绝 loopback provider；为验证真实进程级关闭而新增的 DeepSeek-only 测试 seam 仅在 `NODE_ENV=test`、`ANERA_TEST_LOOPBACK_DEEPSEEK_PROVIDER=true`、synthetic API key 和显式端口的无凭据 `127.0.0.1`/`[::1]` HTTP URL 同时满足时生效。它只替换 DeepSeek text/vision provider fetch，不放宽 Web/Search/HTTP 工具的 `fetchPublicUrl` 准入；config 与 network-policy 合计 46/46 专项测试通过。最新组合资源 production smoke `reports/real-smokes/graceful-resource-shutdown-ses_4d74b3bdb0ce4608a22a.json` 为 `passed:true`：28 ms/code 0，provider/SSE、受管 Process、Website port、guardian、Chromium Browser/Context 和 9 个后代均关闭；恢复态为单一 `interrupted`、Process `stopped`、Website `asleep`、0 Final、0 Review。ProcessManager 另以一次性 shutdown admission barrier 保证资源快照之后不再接受 Restart 的 late start；API 竞态回归验证不会新增 child/port 或 `process.started`，Website 只落成 `failed/restart_failed`。

41. 2026-08-31T13:03:05Z 对 Arena deployment `dpl_A1Vbar2TqcbueSZE1rhwVjSrYyPY` 重跑 schema-v15 public contract audit。第一次 gate 精确捕获 Anera Web Search 运行态仍为旧 `Searching the web...`，而 completed-route 公开契约为 `Searching…`；本地文案与 94 项 Client 回归同步修正后，第二次 audit 为 PASS / `No drift was found`。最新 `2026-08-31T19:05:51.083Z` 又对新 deployment `dpl_GGVhypREjaK9mQC8Qik8L6iAbjnc` 抓取 59 个公开脚本资产与 1 个 completed route，active 19、参数 schema/description、三份 prompt SHA-256、上传/create-chat transport、反馈、Undo、Preview/Raw source accessible label 与 completed-route UI strings 仍为 PASS / 0 issues。该 audit 是公开 bundle 契约对齐，不扩张为私有后端或像素 parity 声明。

42. Web Search running label 修正后的 production client 已重新生成 [`reports/ui-execution-log-v28/`](evidence/ui-state-coverage-summary.json)：62/62 个 `1440×900` desktop-only 状态 `passed:true`，0 console error、0 horizontal/outer-shell vertical overflow；8/8 conversation-follow、执行组自动展开、Workspace docked file viewer、active Writing、结构化 HITL、Website/Workspace ownership、四阶段 persistence、feedback/Undo 与 Preview element picker 全部门禁通过，并由 schema-v3 指纹绑定当前 verifier、fixture 与 runtime。该证据证明当前桌面工作台的结构和交互回归未漂移，但仍不是同任务 Arena PNG pixel parity。

43. 长上下文 live smokes 已按 active surface 重新校准并闭环。`context-compaction-ses_893aa2afa3254c19943f.json` 完整消费 81,039-byte attachment 后把后续最大 prompt 降至消费轮的 0.37679（门限 `<0.6`），以 `write_file→read_file→bash→present_file` 完成 900 行/双 sentinel/七类计数 oracle，5 model/5 tool、0 failure、`$0.01285259`；相对比率避免把 active 19 约 11K-token 固定 system/tool surface 误判为未裁剪结果。`token-pressure-checkpoint-ses_f4f1a9813d2f4b2f9126.json` 的 signed-delta 113,835 tokens 越过 102,400，非-forced checkpoint 将 361,053 B→137,081 B、独立估算 190,535→78,277，再以 active `write_file→read_file` 通过 58-byte oracle，5 model/2 tool、0 compaction failure、`$0.01968399`。该真实压力任务还暴露并修复了 `PRESSURE-CURRENT-593` 一类 hyphen/underscore code marker 被当作自然语言“current”联网意图的问题；路由现在先剥离完整 code-like identifier，新增回归保留真实自然语言时效研究，同时不再违背显式 `Do not use the Web` 的本地验证任务。

44. 当前 active Vision 任务已用 production bundle 和真实 DeepSeek 重新闭环。最新 [`reports/real-smokes/vision-task-2026-08-31T15-37-13-719Z.json`](evidence/live-vision-summary.json) 精确使用 `deepseek-v4-flash-vision-exp`，两次 `inspect_image` 各有 1 physical/1 metered call 和正 token usage；完整工具序列为 `inspect_image→write_file→start_process→browser×3→inspect_image→present_file`，10 model/8 tool、108,420 tokens、84.821 s、`$0.01512895`，0 failed tool。Vision 分量合计 4,612 tokens、估算 `$0.00255728`。`dashboard.html`、running Website、1200×800 Browser 检查、console 和二次视觉缺陷检查全部通过。更早的 immutable Vision report 继续证明 bounded `length` recovery 与旁路价格重算，本次报告证明当前 active 工具面仍不依赖 legacy `create_file/build_and_start`。

45. Deployment smoke 的成功标准已从“仅 deploy_project 不失败”提升为整条最短轨迹无失败。第一次迁移后复跑暴露模糊 marker edit 必然 context miss、额外 read 和两次补救 edit，旧 oracle 却误记通过；现在第二轮冻结两个已知精确 `old_text/new_text`，并把任意 failed/timed-out tool、额外读取和序列偏差设为 hard fail。最新 `reports/real-smokes/deploy-ses_ce544368fa7d4899a018.json` 为 `write_file→deploy_project→edit_file×2→deploy_project`、6 model/5 tool、0 failed tool、8.536 s、`$0.00523985`；两次可见 Approval、revision 1→2、stable ID/URL、content hash 变化、V2 原子替换 V1 和 6 个 canonical Deployment events 均通过。visibility 仍为 `local`，不把该证据表述为 Arena 公网托管 parity。

46. Browser 关闭现在与 Process 使用同级的永久 admission barrier。`BrowserManager.shutdown()` 在快照 pending Context/Chromium launch 之前先拒绝新的 `open/snapshot/click/...` 工作，并复用唯一 drain promise；已经跨过边界的创建 promise 必须进入关闭集合。永久 shutdown 与可复用 `closeEverything()` 分离，先关闭共享 Browser transport 以解除未完成 page/Context 操作，再 drain 各 Context，避免被卡住的 Context teardown 反向阻塞服务退出。真实 Chromium 竞态回归把 Browser close 精确暂停，验证 transport 调用先于 Context drain、shutdown 中 late `open/snapshot` fail closed、释放后 Browser/Context/pending 三项归零，返回后也不能重新创建 Chromium。AgentService 的幂等 shutdown 已改走该边界；最新原提示 production canary 在这一实现上仍以 16/16 checks、10 model requests/calls、10 tools、0 failure 完成，并由 schema-v3 指纹 `56a56ce8…d099` 绑定。

## 7. 下一批 Harness 收敛项

这些不是 Arena parity 项，而是 Anera 自身生产能力仍需关闭的边界：

1. 按 `PRODUCTION_CANARY_RUNBOOK.md` 配置 Pexels 与图片/语音 provider，运行 `npm run canary:media-providers`，再对同一报告运行离线账单对账；最新 production-bundle 负向报告为 `reports/production-canaries/media-providers-2026-08-31T19-11-12.148Z.json`：五项均在任何 provider 调用前明确 `blocked_configuration`，估算外部成本为 0，不能记作真实媒体通过。
2. GitHub Connector 的继续完善和真实账户覆盖已按当前范围明确暂缓。已有生产 adapter、注入 API fixture 与 fail-closed canary 保留；阻塞证据仍是 `reports/production-canaries/github-private-repository-2026-08-29T15-56-59.887Z.json` 和 `reports/production-canaries/github-writeback-2026-08-30T14-44-59.152Z.json`，不得记作通过，也不阻塞本轮普通 Agent Mode 主体封板。
3. 在 Linux + Bubblewrap 主机运行 `npm run canary:linux-bubblewrap`；当前 macOS 证据 `reports/production-canaries/linux-bubblewrap-2026-08-29T15-56-59.897Z.json` 明确为 `blocked_host`，Windows/无 Bubblewrap Linux 继续为 policy-only。
4. 在当前十八类自然/对抗 oracle 之外继续扩大开放域任务分布，并对 5 个 trajectory-blind Judge 做多样本一致性监控；当前 18/18 gate 证明的是已冻结任务上的一般完成能力，不代表开放域质量上限。

Arena 65 题、逐事件 trace、同 viewport DOM/PNG 和成对成本比较继续保留，但不进入当前 Harness 收敛 gate。当前只读 corpus 已提供 9/65 条历史 v1.x 结构化 reference；当前 v2.0 eligible reference 与 exact-version candidate 均为 0，不计算正式 parity。
