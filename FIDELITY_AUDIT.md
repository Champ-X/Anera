# Anera ↔ Arena Agent Mode 完成度审计

更新日期：2026-08-31

## 判定

New Chat 懒物化的可机读专项证据为 `reports/agent-draft-route-v1/latest.json`；其 schema 为 `anera-agent-draft-route/1.1`，当前 `passed:true`。

最新公开契约门禁为 schema v15：该 audit 只把 frozen `list_files(path?)` 与 `read_file(path)` 计入 Arena contract；runtime `list_files(cursor,limit)` 与 `read_file(offset,content_offset,limit)` overlays 独立验证，不计作 Arena parity。`reports/arena-public-contract/latest.{json,md}` 对当前 deployment `dpl_A1Vbar2TqcbueSZE1rhwVjSrYyPY` 为 PASS，除 active 19 工具/UI/feedback/upload policy 外，机器校验 icon-only `Preview`/`Raw source`、New Chat 原子 create-chat/UUIDv7/signed PUT/CAS/已有 Session 独立 envelope，以及完成态执行日志文案；endpoint/PUT/UUID/CAS/existing-envelope 五类负向 mutation 均会使门禁失败。当前最终工程门禁为 51 个 test files / 908 项测试，typecheck、production build 与 `npm audit --audit-level=high` / 0 vulnerabilities 全部通过。

最新公开契约审计时间为 `2026-08-30T16:48:28.336Z`。

2026-08-31 封板快照（覆盖下文保留的 v21/875 项历史计数）：[`reports/ui-execution-log-v23/capture-summary.json`](evidence/ui-state-coverage-summary.json) 在最新 production bundle 上完成 61/61 个 `1440×900` desktop-only 状态，`passed:true`，0 console error、0 horizontal overflow、0 outer vertical overflow，8/8 conversation-follow 与全部 persistence/ownership/HITL 交互通过。运行态 Thought 默认展开，所有完成态都没有残留 `Thinking...`；`completed-desktop-top.png` 同时冻结 HTML 内联 renderer 和 docked Preview。终态投影回归已修复同一步 provisional progress 与 durable Final 重叠时的假运行态。用户提供的 `logo.png` 在全部状态的左栏、Leaderboard Ranking/Pareto 顶栏等比显示，无裁切、拉伸或布局漂移，并与动态 favicon 复用同一 production asset URL。流式 `write_file` 另由 partial escaped JSON、中文 UTF-8 byte、Workspace provisional 和 draft→durable handoff 测试覆盖；active-write 独立截图仅为 P2 证据增强。

用户提供的完整 Arena 录屏已形成 [`reports/arena-video-audit-2026-08-31-071811/report-source.md`](evidence/arena-video-audit-summary.md)。它把可见事实、合理推断和黑盒不可知项分离；审计发现的 research-Artifact 来源缺口已由 `present_file` 前 fail-closed URL-ledger gate 关闭，因此按当前范围普通 Agent Mode v1 `P0=0、P1=0`，可以封板。`HTML Slides` 确定性意图路由与静态集成 gate 已实现，相关定向测试通过；真实同原提示/provider E2E 与 Arena A/B 成对比较仍为 N/A。移动端、完整 GitHub Connector 及 exact trace/pixel parity 仍明确延期。因此本审计仍不使用“一模一样”或 Arena 内部同构措辞。

schema v15 还解析完整公开 `SYSTEM_PROMPT_TEMPLATE`（5,124 字符）、`CODING_SYSTEM_PROMPT_TEMPLATE`（3,021 字符）与 `CODING_CLOSED_SESSION_GUIDANCE`（666 字符），冻结原文 SHA-256，并把普通、Coding-active、Coding-closed 三个本地 builder 输出做整段字节级比较。Anera 现在从已验证 Session metadata 在 system 层注入 repo owner/name、base branch/SHA、session-fixed `arena/...` branch、cwd 与 Coding status，不再把 repository authority 放进用户消息；新 clone 会实际创建该工作分支，旧 Session 安全回退到现有 base branch。运行时另以 Anera overlay 诚实声明能力边界：active/pr_open Session 可发送独立的精确固定分支 push，以及限定的 PR、issue、run、workflow 和 release 读写形式；未合并的 `closed` Session 只有 approval-gated `gh pr reopen` 这一条固定 repo/head 例外，成功后恢复 `pr_open`，`pr_merged` 永久封闭。高影响 metadata/comment/review/merge/close/reopen、workflow dispatch/state、run rerun/cancel/delete 与 release create/edit/delete/upload 先进入 durable Approval；批准前零 credential、零 command、零 release staging，批准后重新读取 Session/connection/scope 并再次解析。repo/base/head/branch/ref/target 只来自受信 Session，不可信 option value 以单一 `--flag=value` argv 重建。Release asset 仅接受 Workspace 内普通文件并在批准后复制为临时快照，拒绝 symlink、目录、内部/宿主/绝对/遍历路径、glob 与 `#` label。PR create 只有成功才进入 `pr_open`；PR merge 只有固定 repo/head/base 的 GitHub oracle 返回 `merged_at` 才进入 `pr_merged`，即时 merge 的 oracle 不可用时保守进入 `closed`。其他 Bash、remote、branch、组合命令、scope override、PR number/URL、未建模的 `gh` 操作、`closed` 下的其他远程操作及全部 merged Session 均 fail-closed。该证据证明公开 prompt 投影与本地权限边界，不证明真实外部 writeback、Arena 私有服务端附加上下文或登录态像素 parity。

当前工程已经形成可运行的 TypeScript Agent 产品纵切面，但尚不能把目标判为“与 Arena 一模一样”。已证明的是 Anera 自身的功能、生命周期、视觉形态和一组真实 DeepSeek 任务；当前只读 corpus 清点了 43 个 task 目录 / 50 个 run 目录，并从中形成 9 条历史 task-version Arena canonical reference / 231 个 canonical 事件（当前 v2.0 eligible reference 为 0），但尚无同题 Anera candidate、完整 65 题 reference、Arena 用量与同 viewport DOM/PNG 对，因此没有证据证明逐事件行为、任务质量、延迟和成本相对 Arena 已经等价。

这批历史 reference 足以纠正主体能力的若干错误假设：A02/U03 的 Bash 非零 exit 是失败，展开体按 `COMMAND/STDOUT/STDERR` 组织；A03 的 `ask_user` 是同 episode 的 awaiting-user/恢复状态；U02 显示 Website/Process 的用户名称、命令、process ID、OS PID 与端口，并以官方 34.2KB/6-files 保存态和六成员 ZIP 明确排除 `dist/node_modules`；U03 证明 refresh/history 可恢复 trace、Final、Workspace 与 Review，但未恢复的 Website 不能被猜测重建；A01 明确显示 Review 与 `Workspace Updating` 同屏，所以 Review 不能被建模为必须等待 `Workspace saved`。这些是历史行为锚点，不是 v2 exact parity 声明。

U02 的保存边界现已成为本地端到端 oracle：一个共享 snapshot policy 同时约束 Workspace tree、Shell reconciliation、Artifact、终态 bytes/file count、root search/list traversal 与 Workspace ZIP，排除 Arena 列出的 generated/cache/dependency 目录和 credential suffix；这些字节不会被删除，`dist`、`node_modules` 等仍可服务当前 managed process。回归固定六个源码成员、生成物零 `file.changed`/Artifact、`blobCount:0/local_durable`，不把本地投影冒充 Arena 的远程 CAS 实现。

当前最新的桌面视觉回归是 `reports/ui-execution-log-v21/`：61 个 `1440×900` desktop-only 状态全部通过，0 console error、0 horizontal/outer vertical overflow，8/8 conversation-follow checks 全部为 true，且 Workspace persistence、reload high-water/no-replay、Website/Workspace ownership、`customFeedbackArenaTransport:true` 与结构化 HITL 完成态均闭合。相对 v20 contract diff 为 PASS，overall 99.7%，全部 state ≥95%。v21 保留 command/file execution group、current active 19 工具、长 Session 输出跟随、Bash 无预点击自动展开、Workspace 文件页内 docked viewer、A01/A02 两种 persistence/Review 合法交错与完成态刷新证据，并新增 `ask_user` pending 与 resolved-after-reload 两个状态；它仍是 Anera 自身回归，不是 Arena pixel parity。包含 `list_files`/`read_file` pagination overlays、Office upload overlay 与 HITL 投影收敛在内的最终工程门禁为 50 test files / 875 tests，typecheck、production build 和 `npm audit` 0 vulnerabilities 全部通过。production Chromium `/agent` draft-route smoke 证明直接进入/刷新/New Chat/选附件均不创建 Session；首次带附件提交先走 Arena-shaped signed upload/CAS，预上传失败留下 0 个 partial Session并保留文本与本地文件，成功重试只发一次原子 `/nextjs-api/stream/create-chat`、创建一个 Session并逐字节持久化附件；Search/Leaderboard/Back/Forward round trip 均无额外 Session。schema `anera-harness-convergence/1.1` 的真实 DeepSeek 综合 Harness 报告 `reports/real-smokes/harness-convergence-2026-08-30T20-24-27-664Z.json` 为 9/9 场景、19/19 active tools、42 tool calls 全部通过；58 次模型调用中包含 Browser screenshot→vision、语音场景的两次试听和一次最终合成，active duration 71.395 s，496,034 prompt / 4,724 completion / 480,768 cached prompt tokens，cache hit 96.92%，账本估算 `$0.04313908`，shutdown 次序修复后命令以 exit 0 完成。同批默认启用 16 calls/step、96 calls/run 与 6 路 parallel-safe 背压，0 个非预期工具失败。schema `anera-harness-quality/1.9` 的最新不可变完整报告 `reports/quality-benchmarks/harness-quality-2026-08-30T09-44-00-127Z.json` 为 18/18、平均质量 99.44、critical checks 与 efficiency budgets 全通过、118 次工具调用均无失败；候选合计 115 model/118 tool、517.760 s active duration、95.01% cache hit、估算 `$0.18169895`。唯一 90 分来自 evaluator 未识别 Artifact 中名词 `Exclusions` 的假阴性；oracle 修复后的数据分析单题 `reports/quality-benchmarks/harness-quality-2026-08-30T09-45-37-971Z.json` 为 100、8 model/7 tool、14.476 s、零失败，完整历史报告保持不变。图片理解→可编辑 HTML→真实 Browser 交互→像素对照任务在 1200×800 下达到 0.96252 similarity，11 model/8 tool、64.367 s、零失败、`$0.01856406`；`inspect_image` 的短答契约和一次有界 `length` 恢复避免失败后原样重试，并精确聚合两次视觉 provider usage/call count。XLSX、DOCX、PPTX 与两页 selectable-text/vector PDF 均以独立结构/语义/渲染 oracle 复核产物；完整批次的 XLSX 以 6 model/5 tool、62.832 s 的 `install → write → bash → extract → present` 理想路径通过，npm 修复后的冷安装单题 `reports/quality-benchmarks/harness-quality-2026-08-30T09-34-20-752Z.json` 也为 100。`install_npm_packages` 现在只有在物理 package manifest 的 name/version 验证后才成功，exit-0 不完整树或 transient npm 错误只在同一公开调用内重试一次；真实 `reports/real-smokes/package-install-2026-08-30T09-31-12-836Z.json` 证明精确一次 registry install 和物理 `vite@5.4.19`。新增对抗 Web 研究要求抓取两个当前第一方页面和一个陈旧 reseller 页面，显式调和 `$35/99.99%` 与 `$48/99.97%` 冲突，拒绝页面注入并给出唯一合格推荐。附件、长文档、事故、普通研究和对抗 Web 研究五项直接 `tools=[]`、trajectory-blind Judge 单独记入 `evaluationUsage`（合计 5 model/0 tool、8.715 s、`$0.00262835`），不污染候选效率预算；Agent 与 Judge 默认显式使用 `DEEPSEEK_TEMPERATURE=0`。因此 Anera 自身的内部能力与冻结任务质量 gate 已通过；schema 1.2 的 `reports/real-smokes/web-provider-cache-2026-08-30T20-19-19.570Z.json` 已以 `passed:true` 闭环真实 Tavily Web Search、Pexels-bypassed Tavily 图片回退与 Firecrawl turn-cache：分别为 1 次/3,909 B、1 次/56,771 B 并持久化 5 张已准入图片、769,266 B/4 chunks，同 turn 重复/continuation 为 0 外部调用且下一 turn 重新抓取，成本保持 `null/not_available`。其他外部 provider 仍按各自边界跟踪。DeepSeek 视觉主路径另由精确模型 `deepseek-v4-flash-vision-exp` 的 live provider 报告闭环。移动端与完整 GitHub Connector/真实账户覆盖按当前范围明确延期，不实现、不测试、不截图，也不进入本轮主体能力 gate；已有 9 条历史版本 Arena canonical reference、当前 v2.0 eligible reference 为 0，且尚无同题 candidate。精确 parity 按当前优先级暂缓，不能据此声称“一模一样”。下文保留的更早 smoke 编号和中间测试计数仅是历史证据，不覆盖本快照。

生产门禁现已具备四个禁止 fixture 的可执行入口和专用退出语义。当前负向证据分别为媒体 `reports/production-canaries/media-providers-2026-08-29T15-52-42.835Z.json`（缺 Pexels/图片语音凭据）、GitHub 只读 `reports/production-canaries/github-private-repository-2026-08-29T15-56-59.887Z.json`（缺 App installation/私库 oracle）、GitHub writeback `reports/production-canaries/github-writeback-2026-08-30T14-44-59.152Z.json`（缺专用可删除私库配置）和 Linux `reports/production-canaries/linux-bubblewrap-2026-08-29T15-56-59.897Z.json`（当前主机为 macOS）。它们证明 fail-closed 和报告链路，不证明真实 provider/账户/主机 gate 已通过；GitHub Connector 深化按当前范围暂缓。媒体成功执行后的账单使用源报告 SHA-256 绑定的离线 reconciliation，避免为了录入账单再次产生 provider 调用。GitHub adapter 同时修正了 App installation token 错用 `/user/repos` 的问题，现使用 `/installation/repositories`；真实 DeepSeek + 注入 API fixture 的新证据 `reports/real-smokes/github-agent-connector-ses_bb5dd0d56bc443d398f7.json` 明确经过该端点并以 5 model/3 tool、零失败完成。完整运行要求见 `PRODUCTION_CANARY_RUNBOOK.md`。

## 目标要求与当前证据

| 要求 | 当前证据 | 判定 |
|---|---|---|
| TypeScript Agent Harness | `src/server/` 的模型/tool loop、工作区、Web、Shell、浏览器、附件、视觉、审批、持久化与恢复；默认工具面已迁移到完成态 active 19 registry，legacy/shared 能力只按任务条件作为兼容扩展 | active Harness 已实现并由真实 DeepSeek 主路径验证；仍待 Arena 成对 parity |
| DeepSeek 真实模型 | `.env` 配置的文本模型与按官方接口接入的 `deepseek-v4-flash-vision-exp` 已用于真实任务；Vision live report 精确记录物理请求、metered calls、token 与成本 | 文本主路径与视觉 live provider 闭环均已验证；不推断 Arena 使用相同模型/路由 |
| Arena 风格网页 | 纸白三栏、无 Session 副作用的 `/agent` New Chat draft、原生 `contenteditable` Composer、running Thought/Tool、独立 Plan、Stop、Artifact、Final、独立 `check_in`/`task_completion_bar` 终态反馈、No 后 Undo、task-completion 感谢动画、两个桌面 custom-feedback treatment、Workspace、显式打开的 40:60 docked Website/Artifact Preview、Website Asleep/Restart、Deployment、Processes、审批、Continue、模型选择、token-limit、Credits、GitHub Coding，真实 `/history/search` 搜索 route，以及 `/leaderboard/agent` 桌面 route、Ranking/Pareto、筛选、Models/Labs、搜索、列编辑和证据边界；Bash `COMMAND/STDOUT/STDERR`、`ask_user` awaiting-user 卡片及完成态 answer bubble、Workspace/Preview ownership、Workspace 文件页内 docked viewer、Bash 自动展开与双 persistence/Review 时序；61 个确定性桌面状态；DOM contract 与 PNG diff CLI | 主体实现并回归，待 exact-version Arena reference 校准；当前不声称全量视觉 parity |
| 任务完成质量 | 数据分析、对抗 SQL 数据库分析、对抗财务对账、单文件代码修复、多文件功能、依赖升级、真实 XLSX/DOCX/PPTX、状态机功能、双 PDF 附件决策、长文档综合、对抗事故交接、桌面单文件网站、图片到可编辑网页视觉重建、真实矢量 PDF、普通来源决策和对抗 Web 研究十八类任务有确定性产物/行为 oracle；Office/PDF 有独立结构/语义/渲染验证，视觉任务有真实 Browser/DOM/交互/PNG oracle，附件、长文档、事故与两类研究另有只看任务/事实/Artifact 的 `tools=[]` 轨迹不可见盲评 | 最新完整批次 18/18、平均 99.44，critical checks 与 efficiency budgets 全通过，`failedToolCalls:0`；唯一 evaluator 假阴性修复后的同题独立复跑为 100；未与 Arena 成对评分 |
| 效率与成本 | 统一记录文本/视觉/图片生成/语音/compaction token、cache-hit token、provider/tool calls、活跃时延和估算成本；排除 Approval/用户等待 | DeepSeek/视觉/带 usage 的图片生成使用 provider usage；语音持久化物理计量并以明确方法估算 token/费用；Tavily/Firecrawl 另持久化物理调用、响应字节和 cache hit，外部美元成本无权威账单时保持 `null/not_available`。真实外部账单仍需 canary 对账，未证明与 Arena 等价 | UsageTotals 现把 `modelRequests` 定义为所有物理 provider dispatch，把 `modelCalls` 定义为有可归因 metering 的调用：通常使用 provider usage，Speech 因二进制 API 无 usage 而保留显式 heuristic 例外；`estimatedCostStatus=partial|unknown` 时，当前数值（包括 `$0`）不是完整已知成本。
| Canonical 同题成对评测 | Anera normalizer/API、Arena `events.md` + `metadata.yaml` importer、单线程 global outcome、序列对齐、终态审阅 lifecycle/operator action、行为/效率/成本单题报告，以及不可被平均分绕过的 65-run suite release gates | 已实现并自检；已有 9 条历史版本 partial reference、当前 v2.0 eligible reference 为 0，等待同题 candidate 与完整 65 题 |
| 长上下文 | 已消费大型 tool result 的确定性压缩、模型生成 checkpoint 与 provider overflow 强制恢复；真实长附件任务持续正确完成 | 已验证三条恢复/降本路径 |
| 生命周期 | Stop、整棵进程树 SIGTERM/SIGKILL、全工具/active-run timeout、忽略 AbortSignal 的工具快速脱离、流式 Final/工具输出、Continue、`awaiting_user`、SSE replay、刷新/历史、服务重启中普通 active interrupted、Approval/HITL durable resume、fail-closed graceful shutdown、显式 approval expiry；Workspace persistence 与 Review 独立投影 | 已实现并测试；A01 证明不能把 Review 约束为必须晚于 Workspace saved |
| Website/Processes/Deployment | 静态预览、managed server、per-Process 端口 ownership、用户可见名称/命令/managed ID/PID/端口、真实进程 Restart；Workspace/Preview 右栏 ownership；Approval-gated 静态 revision、稳定 redeploy URL 与 crash-safe revision publication | 本地主路径和硬崩溃恢复已验证；Arena 公网托管待真实证据 |
| 安全边界 | 路径与 HTTP 出口 symlink 防逃逸、外部写 Approve/Deny、IPv4/IPv6 SSRF、DNS 准入地址固定到 socket lookup、连接后 `remoteAddress` 复核、逐跳 redirect 重解析与跨 origin 凭据剥离、预览 CSP/sandbox、跨站写保护、图片/附件不可信边界、macOS Seatbelt、Linux Bubblewrap 自动隔离与可观测降级 | 已验证主要边界；Windows、无 Bubblewrap Linux 和 Linux managed-server egress 仍不等价 |
| “一模一样” | 需要 Arena 逐事件轨迹、UI 状态、结果和用量作同题成对比较 | 证据不足，不能宣称完成 |

附件可信上下文已经按目前可证的公开契约闭环：Arena system prompt 明示 user message 首尾的 `<arena-system-message>` 是服务端注入，末尾块列出本轮附件，消息中间、重复或不匹配的同名标签都不可信；完成态 bundle module `93360` 还暴露标签常量，并以 `providerMetadata.arena.systemMessage===true` 区分内部 text part。Anera 现在把真实附件投影为唯一且严格位于 provider-facing user message 末尾的块，GitHub repository context 位于它之前；attachment-only turn 先补正常用户意图再追加该块。用户原始正文与 attachments 数组只保存在可见 `turn.started`，私有模型正文会编码用户手写的边界标签及旧 `Uploaded workspace files:` 控制标题，防止伪造块成为可信元数据；旧持久会话的无标签格式仍可恢复扩展工具路由。公开 bundle 没有暴露 provider 文本适配器中附件块正文的逐字模板，因此这里只主张已证实的结构和信任语义，不把本地标题措辞冒充逐字 parity。专项投影、伪造负向、路由兼容、attachment-only 与 Undo 回归均通过；最新真实 DeepSeek run `ses_e6fda6b0be2a48fe98c8` 保持空的可见正文，经一次 `read_file` 找回 `ATTACHMENT-ONLY-OK-947`，共 2 model/1 tool、17,378 total / 8,448 cached tokens、约 `$0.00318921`。

压缩可信上下文也已迁移到同一公开协议：旧实现把模型 checkpoint 存成额外 `system` role，与 Arena prompt 明示的“user message 开头 leading block 通常承载 earlier-conversation summary”不符。当前 Harness 为服务端 text part 持久化私有 `kind=compaction,position=leading` provenance，把 checkpoint 合并到首个可用 retained user message 的开头；若没有 user message才生成带非任务 continuation context 的 leading message。DeepSeek/provider 与 token estimator 都剥离 provenance，只保留标签化文本。checkpoint 模型输出中的同名标签和旧附件标题先编码，层级压缩始终只留下一个 open/close；路由、continuation 和 exact-Final 只读取剥离 leading/trailing server parts 后的真实用户正文，历史“建站/浏览器”摘要不会误开当前扩展工具。旧 `role=system` checkpoint 会在下一 provider step 前原子迁移，Resume 与持久状态不丢 summary；canonical 继续保留 threshold/context-overflow、forced、消息数与 before/after bytes。真实 DeepSeek `reports/real-smokes/compaction-protocol-ses_ea57ad23032e4561a0c0.json` 以 2 model/0 tool、12,795 tokens（8,064 cached）、6.875 s、约 `$0.00226764` 完成：25 条消息压缩 `28,660→8,461` bytes，Final 命中 `ARENA-LEADING-CHECKPOINT-OK-731`，持久上下文为 1 个 leading message、1 对边界、0 legacy system checkpoint，历史伪标签未越界。公开证据仍未暴露 summary block 正文的逐字模板，所以这里只主张结构、provenance 和信任语义对齐。

`agentic-custom-feedback` 已按公开完成态 bundle module `93360` 的可观察协议实现为独立的新 Agent turn，而不是把普通 Composer 文本伪装成 feedback。已有 Session 的普通文本、上传和 custom-feedback 现在共用一个 request builder：无附件发送 `message:{text}`；全部上传描述进入 `message.metadata.uploads`，只有图片再生成 `file` part；part 顺序严格为 feedback→images→text；outer metadata 为 `{timezone,submissionSource:"chat_input"}`，并发送 `v2Source:"agentic_chat_submit"`，结构化请求不再保留 sibling `attachments`。服务端逐项验证 upload key/filename/MIME/Session URL、唯一性和 part 顺序，并继续核对 exact marker、非空 `reviewedNodeId`、最新 Final、已完成 Session 与已提交终态评价；扁平/结构化冲突、伪造 URL、重复 upload、未 trim 正文和 malformed part 均拒绝。provider-facing user message 的第一 part 是受信 marker `The next message part will be the user providing feedback about the previous message.`，真实 feedback 正文紧随其后，附件仍只进入 trailing trusted block；compaction 不会把 checkpoint 塞进该 leading feedback part。`turn.started` 持久化 Final correlation，canonical 以 `custom_feedback_submitted` 表达。桌面两个 treatment、Feedback chip、失败恢复与成功历史卡片保持不变。真实 DeepSeek `reports/real-smokes/custom-feedback-protocol-ses_ef81a4685d494d368c45.json` 证明模型链路；这些仍不替代尚未采集的 Arena 同状态轨迹、DOM、PNG 和网络 reference。

2026-08-30 对当前公开 deployment `dpl_A1Vbar2TqcbueSZE1rhwVjSrYyPY` 的 landing、existing-session 与 upload bundle 重新核验，关闭了 New Chat transport 的明确差异。Arena 首次提交不是先创建空 Session再复用已有 turn envelope，而是先向 `/api/storage/generate-agent-upload-url` 提交 SHA-256 base64url hash、MIME 和 size，以 signed PUT 上传对象并把 image 映射为 `/api/chat/workspace/cas/user/{hash}`，随后单次 `POST /nextjs-api/stream/create-chat`；body 为 client UUIDv7 的 `message:{id,role:"user",parts,metadata?}`、recaptcha token、timezone 和可选 `modelId`，响应只读取 `id`。Anera 已按该分支实现持久 local-user CAS scope、带时效 token 的有界二进制 PUT、hash/size/MIME/总量复核、CAS immutable GET、严格 parts/metadata/key/URL 校验和未发布 Session rollback。已有 Session 的普通/custom-feedback turn 仍使用当前 Arena bundle 中的 `{message,metadata:{timezone,submissionSource},v2Source}`，没有被错误迁移。bundle URL、字节数、SHA-256 和字段级结论冻结在 `reports/arena-public-bundle/agent-create-chat-2026-08-30.json`；这份证据不含登录 cookie/storage，也不替代真实 Arena request/response 或 DOM/PNG reference。

## 已通过的基线

- Coding system-prompt 分流：普通 Agent 与 Coding 走独立公开模板；每个 provider step 都从持久 Session 重建选择，因此 later turn、Resume 和 compaction 后仍保持 repo/base/arena branch/cwd/status。`closed|pr_merged` 注入公开 remote-operation prohibition，同时允许本地读写、测试与 commit。专项回归还要求 provider prompt、Session、event 和 Git 命令均不含 GitHub token。

- 真实 Arena Agent Leaderboard 校准：公开桌面 route 为 `/leaderboard/agent`，与 Agent 侧栏的 `New Chat → Leaderboard → Search` 共存；首屏包含 Overview/Agent/Chat/Code/Image/Video 产品导航、Ranking/Pareto、Overall/Code/Chat/Work、All/Proprietary/Open Source、`Agent Arena`、Models/Labs、搜索、编辑列、12 列排名表、Signal Leaders、FAQ 和 Try Agent Mode。Anera 已把原惰性 Leaderboard button 升级为同 route 的 SPA 页面；1440×900 冻结 240 px 会话栏、232 px 筛选栏、44 px 内容间距及两种主视图。动态排名数据没有伪造：当前只投影 `anera-harness-quality/1.9` 可证明的 18-session evidence，P50 cost `$0.0093`、P50 output 2,957 tokens，Arena 不可比指标显示 `—`，页面明确写明不是 live Arena data 或 Arena parity。当前 Browser runtime 返回 0 个可用实例，因此本轮没有新的登录态 Arena DOM/PNG reference；实现依据此前成功读取的公开结构，像素与动态数据仍待后续 reference 校准。
- 桌面会话搜索：历史栏的 `Search` 已从惰性视觉控件升级为真实的 Session 检索闭环。查询对 title/last message 做 NFKC、大小写和连续空白归一化，要求所有词命中并把标题命中排在摘要命中之前；面板支持鼠标、上下键、Enter、Escape、backdrop 与 `⌘/Ctrl+K`，打开后进入 durable `/history/search`，选择后切换 `/agent/:id`，直接刷新不创建 Session；关闭/Escape 恢复来源 route，Back/Forward 保持 overlay history，不创建新 turn。production bundle 浏览器验收另验证 `aria-current=page`、快捷键 route、直接 hydration、Session-count 不变和 history round trip，并实际返回 3 个 HTTP 103 结果、用 ArrowDown 选中第二项并切换 Session，随后验证 Escape、快捷键自动聚焦和 0 console error；两个纯 helper 回归冻结多词/排序/空查询语义。该功能是 Anera 自身桌面可用性闭环，不作为 Arena 搜索像素或后端 parity 证据。
- 单元/集成：当前最终门禁为 50 个 test files / 875 项测试全部通过；`npm run typecheck`、production build、覆盖 519 dependencies / 0 vulnerabilities 的 `npm audit`、真实 Shell Stop、partial-stream SIGKILL、生产进程级 graceful-shutdown/combined-resource smokes 与 schema-v15 `npm run audit:arena-public-contract` 同时通过。该 public audit 冻结 `list_files(path?)` 与 `read_file(path)`；runtime `list_files(cursor,limit)` 与 `read_file(offset,content_offset,limit)` overlays 由独立 runtime gate 覆盖，不计作 Arena parity。v15 在原有 UI/transport 门禁上继续冻结当前 icon-only `Preview`/`Raw source` accessible-name contract、New Chat 原子 create-chat envelope 和当前完成态执行日志文案；五类 transport 负向 mutation 继续验证门禁。综合收敛 smoke `reports/real-smokes/harness-convergence-2026-08-30T20-24-27-664Z.json` 使用真实 DeepSeek 控制循环和可重复 external-provider fixtures，9/9 场景覆盖 active 19 工具，并在 shutdown 次序修复后以 exit 0 完成；文件/Shell/Process/compaction、search→fetch→artifact、image search→vision bridge→image edit→present、Browser screenshot→任务内动态 vision、provider-backed voice audition→speech、ask_user→plan revise/accept、工具失败换策略和动态 connector 均通过。视觉场景以 8 model/6 tool、一次 screenshot/一次同路径 inspect 和零意外失败工具闭环；snapshot 只暴露真实可见且有尺寸的 interactive refs，陈旧隐藏 ref 立即失败。失败、路径不匹配或历史 screenshot Artifact 不会扩张工具面。它是 Anera 自证，不是 Arena parity 或真实 Pexels/OpenAI/GitHub 账户证据。原 active-Harness、connector、GitHub adapter、Office 与 Website idle-sleep smoke 继续提供更细生命周期和 UI 证据。
- 语音契约与计量：`generate_speech` 公开的 `.mp3/.wav/.ogg/.opus/.flac/.aac/.m4a/.aiff` 八种扩展名现在都生成可解析的真实音频容器；`.ogg` 请求 provider 的 Ogg Opus，`.m4a` 把 AAC 转封装，`.aiff` 把 WAV 转码，后两者使用项目固定的 `ffmpeg-static`，不依赖宿主安装。最终字节由 `music-metadata` 校验容器和实测时长，空数据、伪音频、无时长和超出字节上限均在发布前失败。两次 `add_voice` audition 与最终 synthesis 分别记录 provider calls、输入字符、provider/delivered bytes 和实测 duration；稳定 turn/step/call/source identity 把成功或已发生的部分失败 exactly-once 结算进 usage event、Session totals、Credits 与 session token limit。OpenAI 官方 [Speech 输出格式](https://developers.openai.com/api/docs/guides/text-to-speech/#supported-output-formats) 不包含二进制响应 usage，因此 Anera 没有伪造权威 token：文本 token 使用显式 heuristic，音频 token 使用官方 [Realtime 成本说明](https://developers.openai.com/api/docs/guides/realtime-costs/) 中约 `1 token / 50 ms` 的关系并持久化 `estimationMethod=text_heuristic_and_50ms_audio_tokens`；`gpt-4o-mini-tts` 默认费率取自官方 [API Pricing](https://developers.openai.com/api/docs/pricing/)。物理计量与估算分离，仍需真实 provider canary 和账单对账。 媒体调用另有 durable turn 预算：`offer_options≤3`、`generate_speech≤10`；它从 `tool.started` 按 call ID 去重重建，跨 executor/process restart 保持，新 turn 才重置，越限不会触达 provider。Image battle 必须配置两个 distinct model ID 并实际分流，单/重复 route 在请求前 fail closed。Voice 的 audition 与 synthesis 统一 canonical BCP-47，持久化试听语言；显式 script/region 变化无法证明兼容时在合成前拒绝，`audio.generated` 同时记录 requested/audition language。
- 自然/对抗任务质量：schema `anera-harness-quality/1.9` 的不可变完整报告 `reports/quality-benchmarks/harness-quality-2026-08-30T09-44-00-127Z.json` 使用显式 `temperature=0` 的真实 DeepSeek 和确定性任务 oracle，数据分析、SQL 数据库分析、财务对账、单文件代码修复、多文件功能、依赖升级、真实 XLSX/DOCX/PPTX、状态机功能、双 PDF 附件决策、长文档综合、事故交接、桌面单文件网站、视觉网页重建、矢量 PDF、普通来源决策和对抗 Web 研究 18/18、平均质量 99.44，critical/efficiency 全通过、`failedToolCalls:0`；候选合计 115 model/118 tool、517.760 s、估算 `$0.18169895`。数据分析 Artifact 已正确写出 `Exclusions`，但旧 oracle 未识别名词形式而给 90；修复后的独立单题 `reports/quality-benchmarks/harness-quality-2026-08-30T09-45-37-971Z.json` 为 100，历史完整报告不修改。视觉任务要求先 inspect 1200×800 参考图，再生成语义化、自包含、可编辑 HTML，验证 DOM、三卡同排、七根 bar、24h 交互、颜色、无溢出/console error，并独立逐像素比较；最新完整批次为 11 model/8 tool、0.96252 similarity、零失败。三个 Office 任务和两页 vector PDF 分别做独立结构、语义与渲染验证，并要求真实 `present_file` 成功；XLSX 在完整批次以 6 model/5 tool、62.832 s 理想路径通过，npm 安装修复后的冷安装复跑也为 100。SQL、财务与事故 fixture 覆盖陈旧编辑上下文、CSV 指令注入和伪 `<arena-system-message>` 等恢复/对抗场景；财务 helper 从完整 source dimension 保留零值分组。对抗 Web fixture 覆盖当前第一方/陈旧 reseller 冲突、精确冲突值披露和网页提示注入。附件、长文档、事故与两类研究 Judge 均直接使用 `tools=[]`，只看任务、权威事实和 Artifact；合计 5 model/0 tool、8.715 s、`$0.00262835` 单列为 `evaluationUsage`。网站评测器只排除 Chromium 自动 favicon 404，真实脚本/资源错误仍计失败。这是可重复的 Anera internal quality gate，不是 Arena 同题对照或开放域质量上限。
- 单文件 Web 收敛：明确的 Web + 单文件/自包含请求在未要求计划时直接执行；首次完整 HTML 写入锁定 canonical path，后续移除重复整文件写入和无效重读但保留 `edit_file`，再走最短 Browser 验收链。大型成功 mutation 的正文会保留到至少一个后续 Assistant step 真正消费；之后历史 payload 才移出 `content/old_text/new_text` 并压缩为独立 `_historicalMutation` 元数据，避免 marker/hash 被复制回 edit context。高置信度 context miss 返回不自动应用的有界当前近似片段；只有没有可用片段时才临时开放一次诊断读。Browser 验收要求在持久 UI 状态变化后继续验证相关控件，避免只测孤立 happy path。Managed server 根路径为目录列表时自动选择 effective cwd 下唯一 HTML，支持子目录 entry、basename Browser 锚定与 `/home/user`/`~/` Workspace 命令映射；多 HTML 不盲猜。最新完整 gate 中网站为 `10 model / 8 tool / 36.225 s / $0.01533950`，质量 100、零失败工具且 critical/efficiency 全通过；真实浏览器状态、交互 oracle 与 Final 摘要均通过。
- Active 公网研究 canary：`reports/real-smokes/active-research-2026-08-30T10-05-17.356Z.json` 使用真实 DeepSeek 和 Tavily/Firecrawl-configured 生产路径读取公网 RFC 8297/MDN，完成 `web_search→fetch_page×2`，3 model/3 tool、22.644 s、估算 `$0.00773433`；两个 exact source URL、Final 引用、active result schema 与 canonical `search×1/fetch×2` 全部通过。3 条独立 `provider.usage` 事件精确证明 1 次 Tavily + 2 次 Firecrawl 共读取 34,561 B，价格因无权威账单保持 `null/not_available`。schema `anera-web-provider-cache-canary/1.2` 的 `reports/real-smokes/web-provider-cache-2026-08-30T20-19-19.570Z.json` 为 `passed:true`：Tavily Web Search 以 1 次请求读取 3,909 B，返回 URL 全部经过公网 HTTP(S)/凭据/DNS 准入、fragment canonicalization、去重与稳定重编号；显式绕过 Pexels 后，Tavily `include_images` 以 1 次请求读取 56,771 B，并把 5 张通过 URL、redirect 与图片 magic 检查的结果持久化到 Workspace；Firecrawl 首次 scrape 读取 769,266 B 并形成 4 chunks，同 turn 重复首块和 `chunkIndex=1` 都为 0 物理调用，下一 turn 才重新 scrape。所有外部成本均保持 `null/not_available`。citation-bearing research Final 还会使用当前任务成功工具结果建立 source ledger：无 exact evidence URL 或带 unsupported URL 时在发布前只允许一次有计量纠正；exact-only 请求优先，Artifact 流不误拦，内部 `model.final.repair` 不进入 canonical。生产实现兼容现有 `TAVILY_API_KRY` 拼写；Tavily、Firecrawl、直抓和模型外网请求都使用同一 pinned transport，保留 Host/TLS SNI、复核实际 peer、每个 redirect 重新准入，从连接层关闭 DNS rebinding/TOCTOU；缺钥或 provider 失败保留安全 fallback，取消不触发 fallback，凭据在 tool result 前精确脱敏。报告不保存密钥或页面正文；这些是 Anera 生产研究证据，不是 Arena parity。
- 视觉 contract：当前 desktop-only 捕获为 `reports/ui-execution-log-v21/`；61 个确定性 `1440×900` 桌面状态覆盖无 Session 的 `/agent` New Chat draft、Running、Completed、Website Preview/Asleep/Restart、Preview/Raw source、Markdown/Office Artifact viewers、两类 terminal feedback、custom feedback、结构化 `ask_user` pending/resolved-after-reload、Approval、timeout/token-limit、Composer ingress、GitHub/Coding、Credits、Search、Leaderboard，以及 Workspace persistence 两种合法交错和完成态 reload。v21 保留相邻 Bash/Shell command group、相邻 `write_file/edit_file` file group 和 collapsed/expanded/running/timed-out 子项门禁；`completed-desktop-top/expanded`、`running-tool-desktop` 和 `timed-out-desktop` 均进入 DOM contract。Workspace 继续冻结 248 px 桌面列、53 px header、12 px card offset 和 15 px global toggle inset。报告为 `passed:true`、0 console error、0 horizontal overflow、0 outer-shell vertical overflow，且 8/8 conversation-follow checks 全部为 true；`hitlInteraction` 进一步证明两个问题/四个选项/两个自定义输入均可见，完成态恰有一个 answer bubble、紧邻原卡片、selected label 与 custom response 同时可见并在刷新后保持。element picker、draft route、Search、Leaderboard、Review/Task Completion、custom-feedback transport、Undo、Office viewer 与 optimistic rollback 门禁也全部保留。三类 OOXML 真实 DeepSeek smoke 继续提供生产交互证据。该报告只证明 Anera 的确定性桌面回归和公开 bundle 结构/文案对齐；尚未取得同一任务、同一 viewport 的 Arena DOM/PNG，因此不替代 Arena reference，也不是像素 parity。
- 完成态行级视觉校准：`reports/arena-live-completed-desktop-2026-08-30.md` 保存已登录 Arena 当前完成任务的只读结构/几何证据。Anera v12 据此把 Thought/普通工具改为约 30 px 的无边框轨迹行，把全文件 mutation 拆成 `Write + filename + line count + open`，Artifact 改成短 filename/type 行；Bash 在模型结果之外显示真实成功/失败、exit code 与 `duration_ms`/本地观测耗时，Final 采用微型完成标记和 icon-only Copy，普通完成态 Composer 调整为约 138 px。该证据只校准共享桌面结构，不替代 34 题同任务 A/B parity。
- Arena 手工 trace 主体校准：`reports/arena-reference-corpus-2026-08-30/` 从 43 个 task / 50 个 run 中收录 9 条结构化历史 reference、231 个 canonical 事件。A02 与 U03 都记录了 `exit 127` 的失败 Bash，A02 的展开正文还分别保存 `COMMAND + STDERR` 和 `COMMAND + STDOUT`；Anera 因而保持 process payload `status=completed` 与 tool terminal failed 的双层事实，非零 exit 不再被绿色成功图标吞掉，展开卡按 `COMMAND/STDOUT/STDERR` 分栏并分别复制。A03 冻结 `ask_user` 的问题/选项/Skip/custom/Submit、`assistant_awaiting_user` 与同 conversation 恢复；Anera 对应为 durable `hitl.required`、`run.status=awaiting_user` 和原 turn/step/call 的 batch resume。U02 冻结 user-facing Process/Website name、command、managed process ID、PID 与 port；Anera 只显示 ownership-verified port，hint 或其他 listener 不得替代。以上都是候选契约校准，不代表当前 v2 paired parity。
- Workspace 持久化投影：A02/A03 显示 `Scanning workspace... → Uploading N workspace blobs... → Saving workspace... → Workspace saved`。Anera 的成功终态现以 `workspace.persistence.started/updated/completed` 发布同四阶段，完成前执行不受 change-detection 上限影响的实际 Workspace regular-file 扫描，跳过 symlink/内部路径，并以 `bytes/fileCount` 更新 materialized summary。实现当前明确为 `persistenceMode:"local_durable"`；没有远端 immutable CAS，所以 `blobCount` 固定为 `0`，文档、UI 和 canonical 都不得暗示已经上传内容寻址 blob。A01 同时证明 Review 在 `Workspace Updating` 阶段即可出现；两套投影彼此独立，当前 Anera 本地终态事件顺序不能被提升为 Arena 的唯一合法顺序。
- 桌面 Workspace/Preview ownership：有 `turn.started` 的 Session 才自动拥有 Workspace；同 Session 已打开 Preview 时，snapshot/SSE 不会抢回右栏，手动关闭 Workspace 后也不会因 refresh/SSE 重开。打开 Website/Artifact Preview 会隐藏 Workspace，关闭 Preview 只在 Workspace 原先拥有右栏且未被手动关闭时恢复。Website 行使用关联 Process 的用户名称和 ownership-verified port；Processes 展开项显示 name、command、managed ID、OS PID、port 和 status。该状态机关闭的是桌面主体可用性，不是同 viewport Arena 像素等价。
- Undo/感谢提示公开契约：只读公开 bundle 证明 Undo 只在 `check_in` 的 No 成功后进入 offer，transport 为 `{type:"undo",sessionNodeId,recaptchaV3Token}`，普通提交/feedback/Undo 状态分别为 `Preparing message...`、`Saving feedback...`、`Undoing last turn...`，失败文案为 `Failed to undo message`；成功 compaction/`checkpointApplied` 禁止 Undo。Anera 在 Store queue 中先原子 rewind 私有 model messages，再用 durable checkpoint exactly-once 发布 append-only `turn.undone`；客户端立即隐藏整个目标 user-turn group、恢复原 prompt、清除 selected attachments，500 时条件恢复 timeline/draft/attachments/offer，刷新后由 durable event 继续隐藏。浏览器 gate 还验证 action body 精确字段、幂等 retry、compaction 不显示；canonical 映射为 `operator_action/undo_last_turn`。公开证据没有证明 Workspace revision rollback，因此响应和 event 都明确 `workspaceReverted:false`，不伪称回退文件。`task_completion_bar` 默认 arm 在反馈请求前进入 `in`，成功后 2000 ms 进入 `out`、200 ms 后隐藏，失败立即隐藏，`treatment-2` 不显示；两个动画相位由隔离 fake clock 截图验证。
- Composer 契约：原来的受控 `textarea` 已迁移为受控纯文本 `contenteditable`，使用 `data-placeholder`/`aria-placeholder`、93–180 px 自适应高度和 `pre-wrap`；图片 paste、文件 picker/drag-drop、同批上传规则、coding mode、session/daily-credit 锁定和发送失败草稿恢复保持不变。IME composition 的 Enter 不发送，普通 Enter 发送，Shift+Enter 插入恰好一个 LF；浏览器 contract 把 tag、role、`contenteditable` 与 `aria-multiline` 纳入结构 diff。当前完成态 bundle 明确在组装消息前执行正文 `trim()`；桌面普通文本、上传和 custom-feedback 都经同一 builder 发送 trimmed text，内部 LF 保留，attachment-only 省略空 text part。旧 flat API 只作为服务端兼容入口，不再代表桌面 transport。
- 移动端范围：按当前产品目标明确排除，不参与实现完成度、Arena 视觉 parity 或 release gate。历史响应式代码与旧截图仅作为非门禁兼容性保留，不再投入功能或像素校准。
- 真实 Arena New Chat 校准：通过此前用户已登录 Chrome 对 `https://arena.ai/agent` 做正常可见 DOM 与截图检查，1510×941 页面实测左栏约 240 px、header 约 53 px、标题为 `What would you like to do?`，Composer 为 x=539/y=428/w=672/h≈139，Workspace 默认收起；空白态没有 Anera 旧版的居中 Logo、说明、三张 starter 卡、可见模型 selector、Shift+Enter hint 或底部免责声明，Composer 底栏显示 Add files、Connections、credits/send。Anera 已改为同一结构：1440×900 确定性 capture 中 Composer 为 x=504/y=408/w=672/h=140，标题和整组相对位置按 viewport 等比吻合，关闭 Workspace 时不再残留 306 px 空 grid column。左栏改为 240 px、Arena serif brand、Today/Yesterday/Earlier 分组；Workspace 改为 header folder icon 且默认关闭。New Chat 现在也遵守真实 route 生命周期：`/agent` 是无持久 Session 的浏览器草稿，文本与所选 `File` 在首次提交前都不进入后端；首次提交创建一个 `/agent/:id`，预上传失败保持无 Session draft并保留文本/本地 File；成功重试经 CAS + 单次原子 create-chat 创建一个 `/agent/:id`。`npm run test:agent-draft-route` 用 production client、真实 Express/Store 与 Chromium 对 hydration、reload、附件、本地→持久化字节、合成 400 pre-upload rollback、retry、Search、Leaderboard 和 Back/Forward 做逐项断言。最新 `reports/ui-execution-log-v21/` 61 个状态全部 0 console error、0 横向/外层纵向 overflow，8/8 conversation-follow checks 与 persistence/ownership/HITL checks 全部为 true，Composer 的 contenteditable、拖放、粘贴、附件-only、Connections/Coding、Review 和 execution groups 的桌面全流程仍通过；尚未重新采集同任务/同 viewport 的登录态 Arena reference。
- 真实 Arena Website Asleep 校准：通过用户已登录 Chrome 对真实完成态 D01 会话做正常可见 DOM/截图检查，1512×941 页面右侧 Workspace 约 248 px，卡片从 53 px header 下方 12 px 开始，把 Website/Processes/Files 放在同一面板；folder toggle 位于全局 header 右侧，公开 DOM label 为 `Toggle workspace sidebar`。已释放的 Website 行显示精确状态 `asleep` 和独立 Restart，公开 e2b URL 的 403 正文则作为普通可复制代码块保留在对话中，没有伪装成功。Anera 已迁移到同一桌面结构，并把真正非零退出继续保留为 `failed`，把 deliberate/zero exit、服务恢复与终态空闲释放分离为 durable/canonical `asleep`；默认 `ANERA_WEBSITE_IDLE_SLEEP_MS=300000`，新任务取消计时，完成与 Restart 重新计时。最新确定性状态和真实 DeepSeek 两次 sleep/一次 Restart 证据分别位于 `reports/custom-feedback-desktop-gate/` 与 `reports/real-smokes/website-idle-sleep-ses_dbb1c55457454bbb941e.json`。
- SSE/快照单调投影：Artifact、Process、Website、Deployment、Plan 与 Workspace terminal bytes 现在由 append-only event 立即投影，不再等待 120 ms snapshot 才更新侧栏。重复或乱序 SSE 只补事件证据、不允许旧状态回滚；refresh 响应必须匹配当前 Chat 且 latest seq 不低于当前 UI，否则丢弃。服务端 snapshot 的 materialized state 与 events 即使跨越一个并发写边界，客户端也会按完整事件序列重放面板投影后再显示。首次 hydration 先取得 snapshot 的最大 event seq，再以 `after=<high-water>` 建立 EventSource；因此不会漏掉 hydration 窗口的新事件，也不会把已完成 Session 的历史 `Scanning/Uploading/Saving/Saved` 当成新的保存动画重播。reducer 与浏览器门禁冻结即时更新、duplicate/out-of-order 幂等、旧 Chat/旧 seq 拒绝、non-atomic hydration、high-water 续订和 reload 持久化；`reports/ui-live-projection-v1/` 的 25 个历史状态仍为 0 console error、0 横向/外层纵向 overflow，DOM contract score `1.0`，与冻结 reference 的 25 张 PNG SHA-256 全部逐文件一致。该 reference 是 Anera 自身冻结图，不是 Arena PNG。
- Plan event/state durability：生产 `update_plan` 不再 state-first 双写，而是先 append 含完整 item ID/status/version 的 `plan.updated`，再物化 Plan state。启动恢复按 version、`updatedAt` 和完整 items 双向对账：event-only 投影恢复 state，legacy state-only 投影补一条 `projection_repaired`，已一致的 Plan 不产生恢复噪声；`session.recovered.planReconciliation` 保留修复方向，二次重启不重复事件。专项回归同时冻结两个撕裂方向和幂等性。
- Artifact/file/state durability：每次 Agent/Shell/build/image/browser 文件变更的 `file.changed` 现在内嵌完整 Artifact 投影和预分配的 create/remove event ID；`artifact.created/removed` 改为 event-first，再物化列表。若服务只写到文件边界，启动会用原 ID 补终态；普通 event-only/state-only 也会双向恢复，并用 symlink-safe 的实际 backing file 检查清除已消失文件的假 Artifact。恢复事件记录 `file_boundary_replayed|projection_repaired|missing_file_reconciled`，state、SSE、刷新和 canonical 不再因中间崩溃永久分叉。3 项专项测试覆盖两个双写方向、create/remove torn tail、文件消失和二次重启幂等。
- Session creation durability：新 Session 在对外返回前先原子持久化 workspace、state 与 `pendingCreation`，其中预分配未来 `session.created` ID/data；正常 append 后才清 checkpoint。启动同时覆盖“state 已写而事件未写”“事件已写而 marker 未清”和 legacy state-only 三种窗口，原 ID 只发布一次，第二次重启不再追加。3 项确定性回归与真实 OS smoke `ses_22de94371969423ba43f` 覆盖零事件 failpoint、`SIGKILL`、同一 Session ID/idle 状态恢复和二次重启幂等；证据见 `reports/real-smokes/session-creation-crash-ses_22de94371969423ba43f.json`。
- Composer upload durability：旧路径按“最终文件 → `workspaceBytes` state → `file.changed`”三次写入，任一间隙崩溃都会产生孤儿文件、错误容量或不可提交附件。上传现在先把完整字节写到 Session 私有 staging，持久化含 preferred/final path、bytes、MIME、SHA-256 与预分配 event ID 的 `pendingUploads`，再用同文件系统原子 hard link 发布唯一终名；event append 后按实际 symlink-safe Workspace 树重算容量并清 checkpoint/staging。启动可续完 event-missing、清理 event-already-written、放弃缺失/损坏 bytes、清除无 checkpoint staging，并修正 legacy `workspaceBytes` 漂移；同名并发仍保留 `name`/`name (2)` 且不占用用户 `.tmp`。3 项新增 Store 回归、既有 API/并发安全测试与最新真实 OS smoke `ses_24008a893cfd42b4b769` 覆盖 27-byte 双 link durable 后、事件前 `SIGKILL`：重启恢复原 path/event ID、容量 0→27、私有 staging 删除，第二次重启事件数保持 3。证据见 `reports/real-smokes/upload-crash-ses_24008a893cfd42b4b769.json`。
- Direct workspace-mutation durability：`create_file`、`edit_file`、`delete_file`、`generate_image` 和 browser screenshot 过去都先直接改磁盘，再依次写 `file.changed`、Artifact event/list 与 `workspaceBytes`；OS crash 会留下无法判定的文件/轨迹分叉。五条路径现在统一先在 Session 私有目录保存完整 post-image，或为 delete 保存 before bytes/SHA-256，再原子持久化 `pendingWorkspaceMutations`、完整 Artifact 投影、原 turn/step/call 和预分配 file/Artifact ID；create 使用排他 hard link，replace 使用私有 install link + atomic rename，delete 在 unlink 前复核 before hash。启动对 final-matched/event-missing、event-already-written、delete-missing、尚未安装、staging 损坏/缺失和 orphan residue 分别续完或清理；create/replace/delete 遇到不同字节时安全放弃，绝不覆盖较新的冲突文件，所有结果进入 `session.recovered.workspaceMutationReconciliation`，二次重启幂等。5 项 Store 回归和 1 项 browser 集成回归冻结写/删两种 torn tail、Artifact 尾部、冲突、损坏与孤儿路径；最新真实 OS smoke `ses_d1e93562dabf4b078156` 在 30-byte final/stage 已落盘而 file/Artifact 事件均为 0 时 `SIGKILL`，重启补齐两事件、Artifact、容量并清空 staging，第二次重启事件数保持 4。证据见 `reports/real-smokes/workspace-mutation-crash-ses_d1e93562dabf4b078156.json`。
- Multi-file patch crash atomicity：旧 `apply_patch` 只在同一进程的 throw/abort 中尝试回滚；若 OS 在 originals 已移入临时备份、只安装部分新文件时杀进程，Workspace 会永久半落盘，且事务占用用户 `.tmp`。现在每批 patch 在 Session 私有目录生成不可变 stage、hard-linked before-image 和原子 `prepared|committed` manifest，逐文件原子替换后复验所有 SHA-256；普通失败只有在回滚验证成功后才删除证据，回滚本身失败则保留完整事务供启动恢复。启动清理 manifest 尚未发布的无副作用 residue，对 prepared 一律幂等回滚，对 committed 完整字节保留并清理，对 committed 篡改使用 before-image 回滚，所有动作进入 `session.recovered.workspacePatchReconciliation`，用户 `.tmp` 不再参与。4 项新增 patch 回归与 1 项 Session 集成回归覆盖 unprepared residue、partial install、committed verify、committed corruption、二次重启；最新 OS smoke `ses_47c54f68ebae4403bc61` 在 2 文件中只安装第 1 个后 `SIGKILL`，重启恢复两份原文、27-byte 容量、0 transaction residue，第二次重启事件数保持 2。证据见 `reports/real-smokes/workspace-patch-crash-ses_47c54f68ebae4403bc61.json`。
- Committed patch event-batch durability：事务级原子性只保证 Workspace 的最终字节，不保证 commit 后依次 append 的多份 `file.changed`/`artifact.created` 全部可见。`apply_patch` 现在在任何目标安装前持久化完整 `pendingWorkspaceEventBatches`，包含每个 post-image SHA-256、Artifact 投影与预分配事件 ID；成功 commit 后按固定顺序发布，普通回滚确认成功才放弃 batch，publication/rollback failure 则保留 checkpoint。启动先验证全部目标 post-image，再只补缺失事件尾部；不完整、冲突或损坏批次不会伪造事件。3 项 Session 回归覆盖零发布、已发布前缀和 rollback discard；最新 OS smoke `ses_98ba0d3ab749466daa2f` 在两文件/25 B 已 commit、patch residue 为 0、file/Artifact 事件均为 0 时 `SIGKILL`，重启精确补齐 2 file + 2 Artifact，第二次重启事件数保持 6。证据见 `reports/real-smokes/workspace-event-batch-crash-ses_98ba0d3ab749466daa2f.json`。
- Foreground Shell side-effect durability：普通 `bash`/`shell_command` 和 package/build helper 过去直接启动 foreground shell，再在命令结束时扫描 stat 快照；服务硬崩溃既可能留下完整子进程树，也无法区分同 size/mtime 的字节变化，更无法恢复尚未发布的 Workspace/Artifact 事件。当前路径在启动前持久化完整 SHA-256 Workspace snapshot 和唯一 `cmd_*` guardian ID；guardian 先进入 `--await-start`，只有 `onGuardianReady` 把 PID/`armed` checkpoint durable 后才收到 `START`。父进程失联会使 guardian TERM→KILL真实命令组；正常结束或启动恢复在树稳定后比较 before/current hash，并原子转换为统一 Workspace event batch，零变化则静默清 checkpoint。2 项新 ProcessManager 回归冻结 durable callback 前零启动和 callback 失败时零 Workspace 副作用，2 项 Session 回归冻结 create/modify/delete 与 no-diff 幂等；最新 OS smoke `ses_edce99504cba49ebb5aa` 在 immediate 10-byte 文件已写、延迟 descendant 已启动、Shell file event 为 0、checkpoint armed 时对服务 child `SIGKILL`，2.5 秒后 `late.txt` 仍不存在，重启恢复 1 个 `created-by-shell` + 1 个 Artifact、容量 10 B，checkpoint/batch 清空且第二次重启事件数保持 5。证据见 `reports/real-smokes/shell-reconciliation-crash-ses_edce99504cba49ebb5aa.json`。真实 DeepSeek 正常路径 `ses_6554b211b8964653a836` 又严格执行 `create_file→bash→shell_command`，两种 Shell 各 1 次且 canonical 成功事件为 2，3 model/3 tool、10,666 tokens（10,240 cached）、3.091 s active、约 `$0.00104098`；smoke 现在会自动落盘报告，证据见 `reports/real-smokes/shell-protocol-ses_6554b211b8964653a836.json`。
- Canonical evaluator：真实 59-event 浏览器 run 完成 JSONL 落盘/重载/self-diff，20 个工具状态事件、12 个生命周期/产品状态事件和全部数值效率指标均为 100% 自一致。
- 视觉任务：最新完整质量 run `ses_023a0b71f2694e3e924e` 为 64.367 s active、11 次模型（含视觉）、8 次成功工具、106,029 aggregate tokens、配置价格下约 `$0.01856406`；输出 HTML、桌面预览、交互和 0.96252 similarity 均通过。`inspect_image` 随视觉描述返回本地解析的 MIME、字节数和 PNG/JPEG/GIF/WebP 尺寸，真实 run 不再为尺寸调用 Bash/PIL。
- 视觉完成边界与失败计量：视觉 provider 仍只有非空 `finish_reason=stop` 才能产生可消费描述；`content_filter`、缺失 finish reason、空正文和永久错误保持 `tool.failed`，不会把部分或过滤文本送回 Agent。`length` recovery 与 transient retry 是两条独立且有界的恢复路径：前者只在 `inspect_image` 内部触发一次更严格的短答重启，不产生第二个 Agent 工具调用；后者在两个可能的 length attempt 间共享预算（默认 2、配置硬上限 5），仅接受官方 [error-code guide](https://api-docs.deepseek.com/quick_start/error_codes) 指示的 HTTP 500/503、明确 network transient 与 `insufficient_system_resource`，HTTP 400/401/402/422 即使携带 `Retry-After` 也不重试。指数退避、`Retry-After` 秒数/HTTP-date 与 `retry-after-ms` 均受 60 s 上限约束且等待可 abort。每次 dispatch 都增加 physical request；只有权威 usage 才增加 metered call，并将该 attempt 的 token 与 dispatch-time cost 纳入原 call ID 的累计结算，所以失败重试可以 physical 大于 metered。定向真实门禁以 52.432 s、11 model/8 tool、质量 100、零失败通过；最新完整门禁以 64.367 s 再次通过。单测覆盖 length 成功/耗尽、network+503/ISR 恢复、预算耗尽、永久 4xx、Retry-After abort、filter/empty/missing 以及 usage/request/call/cost 透传。
- 真实 DeepSeek Vision provider：不可变的 [`vision-task-2026-08-30T20-29-46-421Z.json`](evidence/live-vision-summary.json) 为 `passed:true`，按官方 [Vision guide](https://api-docs.deepseek.com/guides/vision) 和 [`/chat/completions` schema](https://api-docs.deepseek.com/api/create-chat-completion)，以 `image_url` data URL 和精确模型 `deepseek-v4-flash-vision-exp` 执行 2 次 `inspect_image`；其中一次触发单次 bounded `length` recovery，合计 3 physical / 3 metered Vision calls、11,570 Vision tokens。原 runtime 记录 Vision `$0.01138157` 与 Session `$0.02556954`；报告区间全部位于 Sunday UTC，按官方 [六档 Vision 价格](https://api-docs.deepseek.com/quick_start/pricing) 的 off-peak hit/miss/output `$0.007/$0.22/$0.66` 重算为 Vision `$0.00692296`，原值高 `+64.403%`；只替换 Vision 分量后的 Session 历史估算为 `$0.02111093`，差 `$0.00445861`。peak 的三档价格为 `$0.014/$0.44/$1.32`，只在工作日 UTC `[01:00,04:00)`、`[06:00,10:00)` 生效。原报告未回写；SHA-256、`0 hit + 1,621 miss + 9,949 output` 公式及来源见旁路 [`vision-pricing-reconciliation-2026-08-31.json`](evidence/live-vision-summary.json)，调整值不是新的 provider bill。本地准入按字节嗅探 PNG/JPEG/GIF/WebP，decoded image 上限为 `min(ANERA_MAX_VISION_IMAGE_BYTES, 32 MiB)` 且读后复核，宽高均不得超过 8,192；最终 serialized inline JSON body 另受 48 MiB 上限约束，违规输入在 provider dispatch 前失败。该 live 报告证明 Anera 视觉主路径和计量闭环，不证明 Arena 使用同一模型、路由或成本策略。
- 长上下文：正常 checkpoint 不再由固定序列化字节数触发。单次成功 provider 请求把真实 prompt token 和当时的 provider-visible message surface 作为私有 anchor；之后用同模型真实 token 加 signed surface delta 投影压力，模型改变或尚无 anchor 时才估算完整 system/messages/tools envelope。默认 context window 为 128,000 token、checkpoint 阈值为 102,400 token，另保留独立 1,000,000 B 序列化 hard limit。checkpoint 请求自身限制为 `window − 1,800 output − 2,048 safety`：若全部旧历史放不下，只选择能完整放入的最老 group 前缀；最近 8 个 retained groups 合计过大时动态缩短尾部；若一次严格缩小后仍在压力线上，最多继续 8 批分层 checkpoint。每一批都必须同时严格降低 bytes 和独立 token estimate，并且只有完整 `finish_reason=stop`、零 tool call、非空摘要才能替换历史；`length`、异常 tool call、`content_filter`/其他 finish、空或非减小结果都会先结算 usage、记录 compaction failure，再把完整原上下文交给主模型。单个不可分割 group 自身放不下时保留原文并让 provider 明确 overflow，不截断用户证据。anchor 只写入私有 `state.json`，不进入 Session API、canonical trace 或 provider payload；tool result 的私有成功状态也会从普通请求和嵌入式 checkpoint 记录中剥离。11 项新增/调整测试冻结正负 delta、CJK/emoji 保守 surface、CJK surface 高于真实 token 时仍使用 anchor、模型切换 fallback、私有字段排除/API/canonical 隔离、有界多批 checkpoint/逐批 usage-event 结算、不可分组历史保护，以及三种非完整 checkpoint 终态。
- 长上下文真实压力路径：`ses_92621c03f03445ee84f0` 的首轮真实 anchor 为 45,756 prompt tokens（45,696 cached）；追加第二轮后 signed-delta 投影为 113,431，确实越过 102,400 阈值，而无 anchor 的独立完整 envelope 估算为 199,736。一次非 forced threshold checkpoint 把 2 条旧消息压缩并保留当前消息，序列化上下文 391,107 B→136,956 B、独立估算 199,736→72,450 tokens；compaction usage 正常结算且零失败。后续严格完成 `create_file→read_file`，`pressure-proof.txt` 的 58 B 精确 oracle 通过；全 run 为 11.609 s active、5 model/2 tool、168,238 aggregate tokens（141,568 cached）、约 $0.017600。证据为 `reports/real-smokes/token-pressure-checkpoint-ses_92621c03f03445ee84f0.json`。
- 长结果价格路径：缓存价格门后的冻结真实 run `ses_e81afda3c75d4e37be0f` 为 13.586 s、5 次模型、5 次工具、57,797 aggregate tokens（27,904 cached）、约 $0.011222。`extract_attachment` 的 81,039 B 结果首次完整消费用了 29,745 prompt tokens，随后持久化为 5,207 B；后三次请求为 6,920/7,487/7,743 prompt tokens，后两次分别有 7,040/7,552 cache hit，无 checkpoint、无 compaction failure，`digest.md` 的 900 行、首尾 sentinel、7 类计数和独立 Shell 复核全部通过。证据为 `reports/real-smokes/context-compaction-cache-aware-ses_e81afda3c75d4e37be0f.json`。本轮改为 token trigger 后的独立回归 `ses_fedc9de314e743779f2c` 同样走 consumed-result compaction：81,039 B 结果被完整消费，后续 prompts 为 6,654/6,927/7,536/7,700，零 checkpoint/失败，全部 Artifact、900 行、sentinel、分类计数与 Shell oracle 通过；14.956 s、6 model/6 tool、64,392 tokens、约 $0.011535。旧模型 checkpoint 路径也保留真实证据：26.6 s、8 次模型（含 1 次 compaction）、7 次工具、81,989 tokens，一次将持久化上下文从 84,644 B 压到 4,750 B 后继续正确完成。`test:compaction` 接受且区分价格裁剪与模型 checkpoint 两条合法路径。
- Context overflow recovery：provider 在零可见增量时明确返回 context/prompt/input token overflow，Harness 会强制只保留当前 message group、checkpoint 更早记录并最多重放主请求一次；成功与失败事件用 `reason=context_overflow,forced=true` 区分阈值压缩。任意正文/Thought delta、单一不可切分组、第二次 overflow 或未严格降低序列化字节数都会停止恢复并保留原始错误；compaction 已发生的 token/cost 仍结算。6 项专项测试冻结成功恢复只有一个 `assistant.started`、零重复输出、单组/非减小摘要、错误分类与空 checkpoint usage。
- 历史 tool-result 保护与缓存价格门：模型尚未消费的最新结果始终保留完整原文，即使失败后追加 Continue 用户消息也不会被误判为已消费；最新尚无成功恢复证据的失败诊断同样受保护。已消费结果只有在“压缩后按 uncached input 价格仍比原结果按 cache-hit 价格便宜”时才改写为 UTF-8 安全的首尾、原始字节数、遗漏字节数与 SHA-256；达到 102,400-token 投影压力、1,000,000 B 序列化 hard limit 或 provider overflow 时绕过价格门立即缩减，避免为了保 cache 阻断恢复。持久消息只重写一次，事件与 Artifact 不变，parallel batch 的 call/result pairing 不变。真实 DeepSeek 隔离对照表明：8 KB 温热结果主动裁剪虽少 1,613 prompt tokens，却使 cache hit 4,608→0、输入成本变为 2.37×；24 KB 与 80 KB 裁剪分别降到保留成本的 0.84× 与 0.26×，据此淘汰固定 6 KB 策略。证据见 `reports/real-smokes/tool-result-cache-benchmark-20260828.json`；它是 Anera provider 策略证据，不是 Arena 私有实现证据。
- Tool-result 成功边界：大型 `create_file/edit_file/apply_patch/bash/shell_command` 参数不再因为“存在匹配 tool message”就被误写成 successful 并压缩。每条新 tool message 在私有持久上下文记录 `tool_result_status=succeeded|failed`；只有明确成功的调用才能压缩大型 mutation arguments，失败调用保留原始代码/patch/command 供恢复。旧 session 仅在结构化 result 明确 `status=success` 时兼容识别。该私有字段在 DeepSeek HTTP payload 序列化前强制剥离，不改变公开 provider schema。2 项专项测试冻结失败参数保留与 provider payload 无泄漏；真实 run `ses_8d1d0229526948758651` 完成 `create_file→bash→shell_command→Final`，本地三条 status 均为 succeeded，真实 provider 后续轮次全部接受，2 model/3 tool、3.663 s、9,393 tokens（8,832 cached）、约 $0.001033，证据见 `reports/real-smokes/tool-result-status-ses_8d1d0229526948758651.json`。
- Mutation-history 消费边界：旧逻辑在刚写完大型 HTML、模型尚未看到成功结果后的下一规划步之前就压缩 tool arguments，导致模型丢失自己刚生成的正文，并尝试用 `bash/fetch_page/read_file` 重读，甚至把 `content` 字段中的压缩 marker 复制到 `edit_file.old_text`。现在只有 matching success 后已经存在更晚 Assistant step 的 mutation 才能压缩；历史正文、edit context/replacement 和 patch input 全部移出内容形字段，统一存为带 operation/schema/bytes/SHA-256 的 `_historicalMutation` 元数据。单文件 canonical 模式明确禁止把元数据当正文；context miss 若能找到唯一且至少 50% 逐行吻合的窗口，会返回最多 2,000 字符的当前近似片段但绝不自动应用语义模糊 edit，只有无片段时才临时开放一次 `read_file`。单测同时冻结“首个后续 step 保留原文”“消费后元数据化”“active/legacy edit schema”“近似片段不落盘”和诊断读边界；最新网站定向 gate 以 100 分、零失败工具和 `$0.01642476` 通过 `$0.025` 成本门槛。
- 连续工具调用保护：只追踪相邻 model turn 中唯一且参数递归等价的 tool call；不同调用、并行/多工具 batch 与 output-truncated call 会重置计数。只有前三次执行结果也完全相同，才在第 4 次副作用前返回 `notExecuted + repeated_identical_tool_call`；结果持续变化的合法轮询不会被误杀，同时第 12 次相同参数调用设成本/步数硬上限。结果等价比较规范化 JSON key order 并包含 error/timeout 状态；拦截事件区分 `unchanged_result|hard_ceiling`。6 项专项回归覆盖结果不变拦截、8 次变化结果放行、第 12 次硬上限、非连续重复、多工具边界和 nested key order，并冻结 usage/event/Final correlation。
- Arena package/build/Website 协议：当前公开 bundle 的模型面已精确迁移为 `install_npm_packages({packages})`、`build_project({})` 与 `build_and_start({description?})`，成功/错误 payload 与 Arena discriminated union 对齐；legacy `package_install/start_process/preview_website` 不再暴露。安装命令只接受 npm registry spec，拒绝 option、URL、路径和 shell payload，始终禁用 lifecycle/audit/funding。源码变更进入 Artifact ledger，而共享 snapshot policy 排除 `dist/node_modules` 等 generated/dependency 目录的 reconciliation/保存投影，managed Process、Website、Restart 和服务重载状态持久化；新启动成功后才停止被替换的旧 Website process，build/healthcheck 失败会清理新进程。macOS Seatbelt 在不开放网络的情况下只读放行当前 Node/npm runtime；Linux 有 Bubblewrap 时自动切换为只读宿主根、遮蔽 HOME/host temp/runtime、Workspace 唯一可持久写和 foreground 私有网络 namespace，且不再硬编码 Linux 常常不存在的 `/bin/zsh`。Website 为让宿主 Preview 可访问而保留 shared network，该限制通过 `/api/health` 明示；`ANERA_REQUIRE_OS_SANDBOX=true` 可让生产环境在无 Seatbelt/Bubblewrap 时拒绝启动。package script 中的固定端口会作为 hint 传给 npm wrapper process 并跨 Restart 保留，避免无启动日志的服务器被误判。真实安装 run `ses_a5ecbe482e234af8b0e6` 为 24.138 s、4 model/3 tool、16,329 tokens、约 $0.001621，安装 Vite 5.4.19、生成 lockfile，canonical 映射为 `package_install`。真实 build/start run `ses_0a188f0b11944053ac9a` 为 14.368 s、8 model/8 tool、37,635 tokens、约 $0.003918，`build_project→build_and_start→browser open/click/console→list_processes` 零失败；Restart 前后 process ID 改变、端口 43129 和页面保持可用，canonical 映射为 `build` 与 `website_start`。
- Arena Deployment 协议：公开 bundle 的 `deploy_project({}) → {status:"success"}|error` 已进入模型面，并使用独立 Approval。build 后只复制确定性、无 symlink/隐藏/依赖/源码泄漏的静态产物，限制 2,000 文件、50 MB 总量与 20 MB 单文件；revision、hash、入口和 stable ID/URL 持久化。每个 revision 只有在全部目标字节写完后才原子发布位于服务目录外的 sidecar manifest；启动恢复会重新拒绝 root/manifest/子项 symlink、特殊文件、超限文件集并复算 path-framed SHA-256、bytes、file count 与 entry。`building/build_failed` 改为 event-first，snapshotting/ready 使用持久 checkpoint 和预分配 deploying/completion event ID：完整 manifest 在崩溃后晋升，缺失/半成品/篡改 revision 被删除并投影 `deploy_interrupted`，已发布 completion 不重复，redeploy 恢复保持 ID/URL 并保留上一版；恢复出的确定结果同时闭合 provider tool message 和 terminal event。失败 redeploy 不覆盖上一版快照，manifest 不进入部署 route，页面继续套严格 active-content CSP。canonical 将 tool 映射为 `deploy`，并把 building/deploying/deployed/redeployed 作为 first-class deployment states。真实模型 run `ses_8f659986e0424fd5b451` 为 7.849 s active、6 model/5 tool、26,136 tokens（21,376 cached）、约 $0.003270；两次正常 Approval 后 revision 1→2、ID/URL 稳定、hash 改变且 V2 替换 V1。最新 OS 级 crash run `ses_c9715d3714b24077b45f` 在 snapshot manifest 已 durable、completion 尚未发布时对 child 发送 `SIGKILL`；重启后 manifest 校验通过，状态由 deploying 晋升 revision 1 deployed，building/deploying/deployed、恢复 tool completion 和 provider tool message 各一份，第二次重启事件数保持 10。证据见 `reports/real-smokes/deployment-crash-ses_c9715d3714b24077b45f.json`。这些证据仅为 Anera 本地部署与 durability；未配置真实公网 reverse proxy，不能宣称 Arena 公网托管 parity。
- Arena Shell 协议：公开 bundle 的 passthrough `bash({command,description?,timeout?,workdir?})` 与严格 `shell_command({command,workdir?})` 已同时进入模型面；两者都返回 Arena `success(stdout?,stderr?) | error(message,stdout?,stderr?)` union。cwd 限于现有 workspace 目录并拒绝 symlink/越界，timeout/cancel 终止完整进程组，Shell 文件变更继续进入 Artifact ledger；UI 输出隐藏宿主 workspace 绝对前缀。canonical 将两者统一映射为 `shell` 并解析结构化 result。首事件 deadline 上线后的真实 run `ses_76494679b41e4b2188f7` 精确执行 `create_file→bash→shell_command`，3 model/3 tool、10,675 tokens（10,240 cached）、3.576 s active、约 $0.00105586，零失败；证据为 `reports/real-smokes/shell-protocol-ses_76494679b41e4b2188f7.json`。
- Arena Web 协议：公开 bundle 的 `web_search({query,depth:"1"|"2"|"3"})` 与 `web_fetch({url,format?:"markdown"|"text"|"html"})` 已进入模型面，active registry 另使用 `fetch_page({url,chunkIndex?})`，旧 `search_web` 只保留在历史 trace canonical alias。search 返回 `success + results[{id,title,url,description,pageAge?}]`，active fetch 支持完整 UTF-8 chunk continuation，legacy fetch 返回 `success + title + content`，失败都投影为结构化 error。`web_search` 优先 Tavily，`fetch_page` 优先 Firecrawl Markdown；两者失败时保留安全 fallback，且不改变 provider-visible 名称、顺序、schema 或 result union。Tavily/Bing/DuckDuckGo 的每个结果 URL 先经过与 fetch 相同的公网 HTTP(S)、无凭据、DNS/private-network 准入，再清除 fragment、按 canonical destination 去重并稳定重排 citation ID；全被拒绝的 provider 结果视为空并继续安全 fallback。成功页面快照使用 `session+turn+canonical URL+provider` key 的条目/字节双界 LRU，后续 chunk 只切分缓存，失败、超限与跨 turn 不复用；同一 model batch 内相同 canonical URL 的 fetch 进入资源级顺序队列，不同 URL/本地读仍并行，避免并发 cache miss 重复付费。每次请求仍验证入口 URL，首次 Firecrawl 结果仍验证 final URL；citation-bearing research Final 另以本任务成功工具结果形成 source ledger，无 exact evidence URL 或含 unsupported URL 时在发布前只允许一次有计量纠正，exact-only/Artifact 流保持各自契约。物理 provider calls、响应字节、cache hit 与 outcome 进入独立事件，不冒充模型 token 或 Arena canonical 事件。最新 active production run `ses_4411525ab5684219b8e8` 精确完成 Tavily/Firecrawl-configured `web_search→fetch_page×2`，3 model/3 tool、22.644 s active、约 `$0.00773433`，零失败；结果 union、1 个 canonical `search`、2 个 canonical `fetch`、双来源引用和 3 次物理请求 oracle 全部通过。schema 1.1 四分块 RFC 9110 独立 canary 另证明 4 个真实 Tavily URL 全部准入且同 turn 后续 chunk 为 0 外部请求。 直抓 PDF 还使用双重类型合同：声明 PDF 必须在前 1,024 字节内含 `%PDF-`，generic MIME 可由同一 magic sniff 识别；伪造、损坏或超过 `maxReadBytes×10` 的响应 fail closed。解析最多 30 页，丢弃页/源字节以明确 marker 报告；`hasMore` 只表示已准入、已提取文本仍有下一个 chunk，不暗示第 31 页或被截断源仍可读取。
- Arena 媒体协议：公开 bundle 的 `fetch_media({query,media_type?="both",count?=6,orientation?="any",size?="any",locale?})` 与 `generate_image({file_path,prompt})` 已进入模型面，结果字段与 discriminated union 对齐。Pexels adapter 覆盖 image/video/both、署名、尺寸、缩略图与推荐 video file；图片生成 adapter 支持 base64 或经逐跳公网校验的签名 URL、12 MB 上限、PNG/JPEG 签名与扩展名一致性，并通过普通 Workspace/Artifact ledger 发布。OpenAI Images API 的[官方 response schema](https://developers.openai.com/api/reference/resources/images/methods/generate/)证明 `gpt-image-1` 响应可返回 input/output/total token usage，[官方价格表](https://developers.openai.com/api/docs/pricing/#image-generation)给出默认模型的文本输入与图像输出价格；Anera 据此把 usage 以独立 `source=image_generation`、模型名和 call ID 纳入统一 ledger，默认 `$5/M` 输入、`$40/M` 输出，并允许兼容供应商通过环境配置覆盖。provider 已完成但随后字节校验、下载或落盘失败时仍结算；工具 timeout 先投影失败，晚到 usage 后补账且 abort 再检查禁止迟到文件写入。canonical 分别映射工具为 `media_fetch` 与 `image_generate`。注入式 HTTP 集成测试覆盖成功、redirect、供应商错误、无凭据、无效图片、生成后 abort 和晚到 timeout usage；当前 `.env` 未配置 Pexels/图片生成 key，因此尚无真实外部 provider smoke，不能把 mock 证据描述成端到端供应商 parity。 `offer_options` 依赖至少两个不同 provider model route，并把两候选实际发送到不同 model ID；每 turn 最多三次，超限与配置不足均在 provider 前失败。
- Provider usage exactly-once/restart consistency：Agent、checkpoint、`inspect_image`、`generate_image` 与 speech 五类 usage 全部先在原子 materialized state 中登记 durable settlement，再结算 Credits 并发布 append-only SSE。每笔登记同时预分配 `usage.updated` 和可选 `session.limit.reached` event ID；SessionStore 对同 ID 的并发/重启重放只追加一次，多笔同毫秒登记另以持久 application order 恢复，累计 totals 不会在新 seq 上倒退。视觉与图片工具额外使用 turn/step/call/source 派生的稳定 settlement identity，因此同一 provider 响应的并发重复回调只保留一份 token、model call、cost 与事件。Vision inspector 还把每个 physical request 按 dispatch UTC 固化并累加后的 `estimatedCostUsd` 作为 settlement cost override 一并持久化；跨峰谷 retry、晚到回调和 restart replay 均不按结算时钟重选价格，Credits 消费同一已固化累计值。若服务在状态落盘后退出，启动时补结算/补事件；若事件已追加但 marker 尚未回写，则只补 marker，不重复扣费或发事件。晚到 usage 可以在 run 已 completed 后触发 session limit，但不改写 canonical outcome；下一轮仍被拒绝。3 项新增测试覆盖 preallocated event 并发幂等、普通 Agent/tool 两类重启窗口和同时间多笔恢复顺序，既有 late/duplicate 测试继续冻结 Credits、limit、SSE 与 terminal consistency。每个 settlement 同时保留物理 `modelRequestCount`、有可归因 metering 的 `modelCallCount` 与 `estimatedCostStatus`；缺 usage 的 provider HTTP/transport/断流请求不伪造 token，Session/canonical 输出以 `partial|unknown` 限定当前成本。
- Durable usage 真实 provider anchor：生产构建 run `ses_4a2646713b6d481d8382` 用真实 `deepseek-chat` 精确返回 `LEDGER-SMOKE-OK-829`，1 model/0 tool、4,408 total tokens（4,352 cached）、0.809 s active、约 `$0.00032806`；session、最后 usage event 与 canonical outcome 三处数字完全相同。私有 settlement 预分配/发布 event ID 均为 `evt_800968dd84ca4d84a07f`；关闭并重启同一 production data root 后仍为 completed、1 条同 ID usage event、2,499 Credits，无重复事件或扣费。证据为 `reports/real-smokes/provider-usage-durable-settlement-ses_4a2646713b6d481d8382.json`；它验证真实 Agent usage 与 clean restart，不替代 crash 注入/工具 usage 的确定性回归，也不是 Arena parity 证据。
- 公开工具面闭环：同一 Arena bundle 暴露的 19 个 Harness 工具名为 `create_file/edit_file/read_file/list_files/delete_file/install_npm_packages/build_project/build_and_start/deploy_project/apply_patch/bash/shell_command/update_plan/grep_files/glob_files/web_search/web_fetch/fetch_media/generate_image`；Anera 模型面已全部包含，并以测试冻结该集合。公开 argument Zod parser 的类型、必填/可选、enum、default、嵌套 object/array、`web_search.depth` number→string preprocess、`bash` catch/passthrough 已形成语义投影；公共调用在持久化/执行前递归剥离普通 object 的未知字段、保留 bash 扩展、显式补齐 `fetch_media` 默认值并规范化 numeric depth，畸形 JSON 不被修复，扩展工具不被改写。19 个工具的完整 success variant、公共 `error(status,message,stdout?,stderr?)` union、`build_and_start` 的 `stage/logTail` error 扩展，以及 `read_file/grep_files/web_search/fetch_media` 的嵌套结构均由运行时结果门禁校验，包括 `status/isError` 一致性与禁止额外字段；本地实现若产生畸形公开结果，会在进入模型上下文前被替换为合法 Arena error union。Harness 自己生成的 timeout、output-truncated 未执行与重复调用拦截也统一经过同一 error 构造器，公开工具不再收到私有纯文本结果，非公开扩展工具才保留兼容文本。5 项结果专项测试冻结成功、错误、嵌套、畸形降级与 19 工具合成错误；参数专项测试继续冻结 live/local schema 独立 drift 与五类运行时规范化。该参数证据明确限定为公开 parser，不推断私有 server prompt 或额外 backend validation。Anera 额外提供附件、视觉、浏览器、进程和外部写审批能力，这些扩展不会替换或改名公开 Arena 工具。
- Tool surface 路由与成本：此前 6 个 Anera 扩展在所有请求常驻，使普通问答也携带 25 个 schema（12,733 serialized bytes），既偏离当前 Arena 公开 19-tool surface，也改变模型策略与成本。主 Agent 现在默认严格按公开证据的 19 工具顺序发送 8,801 B schema；只有当前任务的可信上传路径、明确语义、当前任务由 `generate_image`/`image_search` 产生的图片 Artifact，或当前任务实际启动且仍 running 的 Website 才加入 `extract_attachment/inspect_image/browser/list_processes/stop_process/http_request`。选中扩展在当前 episode 内单调保留以稳定 prefix cache；显式多轮继续会沿连续 continuation user turns 回溯到最初真实任务边界，并从完整任务段的可信上传、语义和真实 tool calls 恢复扩展。旧实现只回看一轮，导致“PDF 工具→零工具中间轮→继续读下一页”在第三轮丢失能力；同时裸 `Confirm/确认` 被过宽当成 continuation，会把旧 PDF schema 泄漏到一个新的算术核验任务。当前只把 Continue/Resume、同一/上一/刚才以及带明确副作用对象的 confirm/approve 当作续接，generic verification 是新任务；文档、图片、网页、进程和外部写意图均支持 action↔object 两种中英文顺序。动态 system instructions 与 tool surface 均进入 versioned context-pressure anchor，老 anchor 保守 fallback。provider 若返回未启用扩展，会产生 `notExecuted/tool_not_enabled` 并以失败 tool result 恢复，零 ToolExecutor 副作用。新增 7 项失败回归后，全套 selector 测试覆盖默认顺序、十类首轮/语序路由、episode/new-turn/多 hop 显式继续、generic confirm 负向边界、system/tool signed delta、旧 anchor fallback 和越权 provider call。真实同题 warm A/B 的 Final/10 completion tokens 不变，prompt 4,398→3,220（-26.8%）、total 4,408→3,230（-26.7%）、配置成本 `$0.00032806`→`$0.00024040`（-26.7%）、active 809→748 ms（-7.5%）；首个新 prefix 的 0 cache cold run独立记录且不与 warm baseline 混算。证据为 `reports/real-smokes/adaptive-tool-routing-comparison.json`。旧双轮 PDF 候选 `ses_25557b905cef4d27b57b` 以 4 model/2 tool、15,142 tokens、4.303 s 精确读取两页；新的三轮 `ses_c9e659f8012e49f294b8` 在 T01 精确读取 page 1、T02 0 tool 且 exact bridge Final、T03 不重复 PDF/附件/path 词而沿两跳 continuation 恢复 `extract_attachment` 并精确读取 page 2，三个 Final 均逐字通过，零 Shell/Web；6 model/2 tool、19,152 tokens（18,432 cached）、6.039 s、约 `$0.00166392`。证据为 `reports/real-smokes/atomic-final-fast-path-comparison.json` 与 `reports/real-smokes/multiturn-tool-routing-ses_c9e659f8012e49f294b8.json`。新增 stale state 负向边界又冻结 `running|asleep|failed` Website 和历史 image Artifact 均不得向无关新任务泄漏 `browser/inspect_image`；真实 DeepSeek Website smoke 的第二轮以零工具精确返回 `42`，并避免 475 个估算 tool-surface tokens。这些结果证明 Anera 自身效率与多轮正确性改进，不证明 Arena 私有 router 相同。
- 公开 bundle 持续审计：`npm run audit:arena-public-contract` 以无 Cookie、无 cookie jar 的只读方式抓取当前 `/agent`、55 个主路由公开 JS asset，并把一个无需 Cookie 可访问的完成态 `/agent/[id]` 作为 supplemental evidence；主路由负责 deployment、19-tool surface、全部 observable argument schemas、全部 19 个工具的完整 success/error result schemas（含嵌套字段与工具特定错误扩展）、empty/Composer/Connections/Coding 文案、`coding-repo-connect-panel` 客户端状态 literal 与 25/10/50 MiB 上传契约，完成态路由负责 Review action mapping、Workspace/Preview、terminal feedback，以及 `Running/Ran commands`、`Editing/Edited files`、搜索/搜图/图片生成/语音生成/进程输出等 15 个执行日志字符串。两类证据与冻结 contract 及本地 TypeScript 投影分别差分；11 项离线 parser/diff fixture 不依赖网络。schema v15 的 2026-08-30 live 证据 `reports/arena-public-contract/latest.{json,md}` 对 `dpl_A1Vbar2TqcbueSZE1rhwVjSrYyPY` 最终为 PASS。审计先后准确暴露了独立配置菜单、attached repository strip、共用 `Ask anything…`/Add files surface、连接验证失败、connected/disconnected outage、loading/error/empty 文案、完成态 Review action/layout 和 execution-log 文案差异，均修复真实结构后才通过，没有用隐藏字符串规避差分。
- GitHub Coding/Agent connection：已实现公开 bundle 中的 connection/status/connect/install/disconnect/repos/branches/coding-session 路径与精确 repo/branch schema；Composer 锚定的 224 px Connections popover 现在与公开结构相同，仅含 GitHub 行及 Connect/仓库开关。账户 `connected` 与本对话 `enabled` 分离，任务内跨模型步骤、Resume 和 compaction 保持，下一真实任务重新采样；只有成功的 `list_connector_tools` 才加载 GitHub 动态 schema。App installation 使用 9 分钟 RSA-SHA256 JWT 换短期 token，只以 `0600` 持久化 installation ID；Disconnect 清缓存并撤销所有 active lease，开发 token/OAuth 只作 fallback。Bootstrap 验证 repo/branch/commit、拒绝 symlink/submodule/大小越界，并实际创建 session-fixed `arena/...` branch。Coding remote broker 在 `active|pr_open` 接受独立固定分支 push，以及固定 scope 的 PR create/status/view/checks/diff/list/merge/edit/close/reopen/comment/review、issue create/status/list/view/edit/close/reopen/comment、run list/view/watch/rerun/cancel/delete、workflow list/view/run/enable/disable、release list/view/create/edit/delete/upload；`closed` 只接受当前固定 repo/head 的审批式 `gh pr reopen`，成功后迁移 `pr_open`；`pr_merged` 和其他 closed 远程命令仍拒绝。repo/base/head/branch/ref/target 均来自受信 Session，PR number/URL targeting、shell composition、scope override、fetch/pull、api/auth/config/extension/secrets/variables 等未建模操作继续拒绝。高影响操作走两阶段 durable Approval：首次只返回具体风险文案，Deny、等待中状态变为不允许或 Disconnect 均为零底层 command；批准后重新读取状态、重新解析，再取得一次性 lease。Release asset 首次只预检，不建 staging；批准后从 Workspace 内已验证普通文件句柄复制最多 16 个、50 MiB/个、200 MiB 总计的 0600 隐藏快照，拒绝 symlink、目录、内部/宿主/绝对/遍历路径、glob、`#` label 和重复 basename，CLI 从不读取原路径，完成后清理快照。PR create 成功才进入 `pr_open`；PR close/reopen 分别进入 `closed|pr_open`；PR merge exit 0 后用同一 lease 查询固定 repo/head/base，只有 `merged_at` 进入 `pr_merged`，即时 merge 的 oracle 不可用时保守进入 `closed`，auto/disable-auto 保持 `pr_open`。桌面 Approval card 会投影 broker 的 title/description。26 项 GitHub 专项测试、97 项 ToolExecutor 测试与 Agent/API/客户端投影回归冻结 scope 重建、option-smuggling、staging、二次校验、closed reopen、状态 oracle、disconnect 和 token 对 Session/event/model 零泄漏。生产 App smoke 已覆盖真实模型/事件/UI 和注入 API，但尚无真实外部固定分支 push、PR/issue/workflow/release writeback 证据；因此不能声称完整 Coding parity。
- GitHub production writeback gate：`npm run canary:github-writeback` 现以独立可注入 orchestration core 驱动 production Connector、Bootstrapper、Coding broker 和 ToolExecutor，要求显式 enabled、installation、精确 repository 二次确认、base、专用 workflow 与 marker SHA-256。通过条件包含双 `arena/...` branch push/远端 SHA、第一条 PR 的 close→reopen→reclose 及 Session/远端双状态 oracle、Issue close、workflow dispatch/run、独立 release upload 的远端 bytes/hash、release delete、真实 merge 后的可信 `pr_merged`，以及按 title/branch/head SHA/tag/ID 重新发现部分成功资源的 identity-checked cleanup。真实 merge 会在专用可删除 repo 的 base 留下审计 commit；脚本不 force-push 掩盖。当前只有 `reports/production-canaries/github-writeback-2026-08-30T14-44-59.152Z.json` 的默认 `blocked_configuration` 证据，没有真实账户 passed 报告；因此上一条“尚无真实外部 writeback 证据”仍成立。
- Arena Credits 产品状态：公开 bundle 中的 `GET /api/billing/balance`、`GET /api/me/pulse`、2,500 daily default、UTC reset、free-session、低于 10% pulse、zero block、Battle fallback 和帮助链接已实现。owner-only 原子 ledger 按 session 累计成本的整数 credit 增量结算，重启、重复 usage、并发和跨日不重复扣费；Message/Continue/coding submit 在 episode/bootstrap 前以 `429 + daily_credit_limit` 拒绝，New Chat 不重置全局余额，free session 绕过且不扣费，已准入 run 消耗最后 credit 后仍完成。8 项 store、5 项 API/Agent 和 3 项 UI helper 专项测试通过；5 个 Credits 视觉状态在固定时钟下独立复捕获完全一致。尚无登录 Arena 的真实余额/免费会话/耗尽账户截图，因此具体尺寸与账户侧 reset 行为仍需校准。
- Agent Mode 终态审阅：Arena 公开完成态 route/i18n bundle 确认标题 key `Was this task successful?`、中文“此任务成功了吗？”、三项 action mapping `approve/Yes`、`disapprove/No`、`edit/Keep working`、Esc dismiss、`Close review panel` 和 `Open workspace`；可见组件结构是带分隔线的三行纵向 full-width action list，Final 下方没有常驻 Like/Dislike。Anera 已改为同一状态机与结构：最新稳定 Final 完成后用面板替代 Composer，Yes/No 绑定 Final，Continue working 与 Close/Esc 进入 durable `review.dismissed`，并以 `review.requested` 保留面板出现证据；这些动作均不创建 episode，恢复 Composer且刷新后不重现。A01 的真实录屏进一步显示 Review 可在 `Workspace Updating` 尚未结束时出现，所以这里只约束 Review 与 Final 的 correlation，不把 `Workspace saved` 设为 Review 前置门槛。6 项 API、4 项 projection、2 项 normalizer/importer、public-contract fixture、结构断言和五条真实浏览器 UI→API→Store→refresh 交互链覆盖关联、幂等、旧 Final 隔离、新 Final 重开、canonical action 区分和纵向布局；仍需 R01/A05 等 raw-only 状态以及 current-version 同 viewport DOM/PNG/请求证据来校准精确像素、交错时序和服务端持久化。
- 浏览器表单：stable refs `e1–e4` 完成 fill/select/check/click，并验证 `Alice|pro|accepted`、scroll 与 console。
- 研究：最新真实 Web-protocol run 即上面的 `ses_6290f0a14c8540999b73`；Final 只使用实际读取的 RFC/MDN，且没有把搜索 snippet 当证据。
- Continue：前台命令取消后不重跑，Continue episode 创建并验证 `resumed.txt`，UI 与 Artifact ledger 均恢复。
- 进程树：managed Stop 与 Bash 自动 timeout 均用延迟写文件的孙进程探针验证；终止后没有孤儿产物。
- Office/PDF 输入：Arena frozen baseline 仍保持当前 15-MIME 白名单，M06 的 DOCX/XLSX/PPTX 拒绝继续作为 Arena 产品观察；Anera runtime Composer 另行开放三类标准 OOXML MIME。每个 Office 输入必须通过扩展名/MIME 精确配对、ZIP 结构检查和类型必需 entry 校验（DOCX `word/document.xml`、XLSX `xl/workbook.xml`、PPTX `ppt/presentation.xml`）。signed PUT 在 CAS 发布前拒绝伪 ZIP，真实 descriptor 在 Session staging 前再次校验；验证后的附件会按受信路径动态开放 `extract_attachment`，并保留完整 item 边界分页。signed-upload 的冻结 `hash/contentType/size` envelope 未改变；这是 Anera capability overlay，不计作 Arena attachment parity。PDF 仍是 frozen baseline 支持的分页附件。
- 附件输入入口：公开 Arena composer bundle 明确允许 `text.trim()` 为空但附件非空的提交，并提供 `Upload files`、`Add files`、`Drop files...` 和剪贴板图片入口；MIME 顺序、25 MiB 单文件、10 MiB PDF、50 MiB 每消息以及顺序部分接受规则均已冻结。Anera 已支持点击/拖放/粘贴、附件-only send、空 user bubble + file chip 和 `image-1.png` Workspace 投影；3 项共享策略测试、3 项 HTTP API 测试与客户端粘贴测试覆盖拒绝和恢复。真实 attachment-only run `ses_dc7124389d794e9ba6a4` 的 `turn.started.content` 保持空字符串，只调用一次 `read_file`，2 model/1 tool、5,678 total / 3,200 cached tokens、约 $0.00104，并找回 `ATTACHMENT-ONLY-OK-947`。
- 秘密脱敏：真实 run `ses_200d57f5ee864daaa3ad` 在 `.env` 内保留原始合成 token，Final 返回正确 SHA-256，Thought/Final、工具参数/结果、API/canonical 投影均不泄漏；敏感 session 禁用可重组的 delta/live-output 事件。该任务 12.7 s、7 model/6 tool、21,325 total / 19,456 cached tokens、约 $0.00274。最新回归进一步覆盖秘密首次来自工具结果、Shell/Process 输出或拆分的模型流：可见事件被动态脱敏，私有模型上下文仍保留原值。
- Final streaming 与缓存计量：最新生产构建的真实 DeepSeek 单句任务产生 5 个 `assistant.final.delta`，均早于 `assistant.final`；1 次模型调用、0 次工具、0.855 s active duration、2,225 total tokens，其中 2,176 cached prompt tokens，约 $0.00017；session 与 canonical outcome 数字一致。
- 最新构建 smoke：真实无工具控制任务 `ses_f9b12eda923d4c17a42b` 以 1 次模型、0 次工具完成并正确返回 `9193`，2,438 total tokens、约 $0.00021；较早文件任务 `ses_a5d6556d60274caca2d3` 以 2 次模型、1 次 legacy `write_file` 完成，下载内容精确为 `anera-schema-ok\n`，5,114 total tokens、约 $0.00053。`/api/health` 同时确认 production 服务与模型配置可用。
- Arena 文件工具协议：frozen active 模型面保持 `list_files(path?)`、`read_file(path)`、`create_file`、`edit_file(context,replacement)`、`delete_file`、`apply_patch`、`grep_files` 与 `glob_files`；edit 精确复现 Arena bundle 的 exact/trailing-space/whitespace/indentation 唯一匹配链，patch 在全部解析/匹配成功后才事务提交并支持 add/update/move/delete。最新真实 run `ses_6e4af46085f3452ab7d1` 在 11.496 s 内以 9 model/8 tool、46,045 tokens、约 $0.00424576 完成 create→fuzzy edit→2-file patch→delete→list/read，最终 `config.ts` 与 `summary.txt` 精确命中 smoke oracle、无 Bash、零工具失败。较早的结构化失败恢复 run `ses_97e25b5301e143ff9af3` 与并行 list/read run `ses_0f4cfdf0788e4a229a08` 继续作为补充证据。
- Arena Plan 协议：模型面提供精确的 `update_plan({explanation?,plan:[{step,status}]})`，成功/失败均返回结构化 status；计划持久化到 session state，每次变化追加 `plan.updated`，跨刷新、历史与服务重载恢复，同文案 item 跨状态更新保持稳定 ID。UI 将工具事件投影为独立 Plan 卡片而不重复显示普通工具卡；canonical 同时保留 `plan_update` tool 与 plan snapshot。真实 run `ses_311dcd81f30c4043b603` 以 10 model/9 tool、47,983 tokens、约 $0.00561 完成四步 P03，5 次 plan call/5 个快照零失败，每个 item 都经历 `in_progress→completed` 且 ID 稳定，最终 JSON oracle 正确。
- Workspace exploration 分组：相邻 `list_files/read_file/grep_files/glob_files` 在 UI 中合并为单个可展开探索组，写入、Thought、Artifact 或新 turn 均形成边界；组内保留开始顺序、独立参数/结果与 running/failed/timed-out 状态，并在终态 replay 时关闭悬挂子项。真实 run `ses_895f3190ded24960b3b6` 以 2 model/2 tool、8,514 tokens、约 $0.00177 完成 glob→grep；生产 UI 实测为 1 个 exploration group、0 个重复普通工具卡、展开后 2 个准确子项，console/横向/外层纵向溢出均为 0。展开视觉检查同时发现并修复了 CSS Grid min-content 撑高整个 shell 的旧问题，视觉 capture 现将 outer vertical overflow/window scroll 作为硬失败。
- Command/file execution 分组：当前 completed-route bundle 公开证明相邻命令使用 `Running commands`/`Ran commands`，相邻文件 mutation 使用 `Editing files`/`Edited files`。Anera 将 Bash/Shell 与 `write_file/edit_file` 分别聚合，保留每个子调用的开始顺序、参数、结果、running/failed/timed-out 状态与展开行为；Web/Image Search、图片/语音生成和 process output 也按 live 文案切换。`reports/ui-execution-log-v16/` 在该演进阶段首次用四个关键状态冻结 1 个完成 file group、1 个 running command group 和 1 个 timed-out command child，且没有复用 Workspace exploration 的 DOM class；当前 v21 继续保留这些断言。证据来自无 Cookie 公开 bundle 和 Anera capture，不是登录态 Arena 像素 reference。
- `read_file` runtime pagination overlay：公开 Arena baseline 与 Anera runtime 能力被刻意分层保存。冻结的 `ARENA_ACTIVE_AGENT_TOOL_DEFINITIONS` 仍保持 active 19 工具顺序，且 `read_file` 参数只有 `path`；公共 Arena normalizer 会剥离 `offset/content_offset/limit`。Anera provider runtime 在不改名、不改顺序的前提下增加 1-based `offset`、最大 5,000 行的 `limit` 和 0-based UTF-8 `content_offset`。普通页按完整行、byte/line 双预算以严格前进的 `nextOffset` 续读；若首行本身超过默认 80,000-byte 页面，则在 Unicode code-point boundary 上返回同一行 fragment 与严格前进的 `nextContentOffset`，完成该行后才切到 `nextOffset`，terminal page 去掉两类 cursor。非法、越界、非字符边界和不前进 cursor 均 fail closed。这是 Anera robustness overlay，不属于冻结 Arena exact baseline。最新 live `reports/real-smokes/read-file-pagination-2026-08-30T22-51-36-367Z.json` 为 `passed:true`：真实 DeepSeek 只用 5 次 `read_file` 读取 249,158 B / 1,200 行文件，其中第 601 行为 170,021 B；主链精确为 `offset 1→601`、`content_offset 0→80000→160000`、`offset 602→EOF`，零回读，7 model/5 tool、317,777 tokens（199,424 cached）、41.249 s、估算 `$0.04634327`，Final 逐字为 `PAGINATION-MARKER-731|PAGINATION-TAIL-926`。旧 3,200 行三页报告保留为 complete-line 历史证据。
- `list_files` / Workspace inventory overlay：冻结 Arena schema/result union 继续保持 `path?` 与既有 shape，Anera runtime 独立增加 `cursor/limit` 和 runtime validator。首次调用按共享 snapshot exclusion、symlink skip 与 UTF-8 raw-byte path 顺序生成 Session/path/mode/limit-bound immutable manifest；文件保存在 visible Workspace 外，24 小时内可重试并跨服务重启续页，cursor HMAC 绑定 manifest/offset/expiration，损坏、过期、跨 Session、换 path 或换 limit 均 fail closed。每页同时受 200-default/500-max count 与 64 KiB JSON 预算；10,000-file cap 和 20,000-entry traversal cap 分开标记，terminal `truncated` 不会伪装为完整。Agent normal compaction 保护 newest 未消费 cursor 的精确 predecessor chain。HTTP snapshot 返回首批 500 tree + 同一 metadata，root-only continuation endpoint 使用隔离 manifest namespace；客户端顺序合并 flat pages，显式提供 Load more/Retry/Refresh/lower-bound 文案，HTML picker只列已加载项并说明未完整。503-file、10,001-file、cross-session、TTL、manifest corruption、concurrent mutation/retry 与 UI stale-request/session-switch 路径均有确定性集成回归。
- 文件搜索：Arena 当前公开 bundle 核验到真实工具名与 schema 为 `grep_files` / `glob_files`；`grep_files` 支持 `content|files_with_matches|count`、glob、`-i` 与 `-B/-A/-C/context`。Anera 使用 Node 工作区遍历实现，不依赖系统 `rg/find`，跳过二进制、依赖/Git 内部目录与 symlink，并覆盖 Unicode/空格路径及 Arena 的 pattern/match/file/line/result caps。真实 run `ses_a288d6502c1549cc9054` 精确执行 glob→grep，3 model/2 tool、10,909 aggregate tokens、约 $0.00186，无 Bash，Final 与 canonical trace 均命中 file/line/context oracle。
- 模型选择与 session limit：`GET /api/agent-models`、显式模型与 Auto sampled 路由都持久化实际模型，并进入 `turn.started`、usage 与 canonical trace。Arena 同文案的 session token limit 跨重启持久化，达限后拒绝 Message/Continue 为 `409 + session_token_limit`，不会创建伪 turn；真实 smoke `ses_d172f93497814e04ae56` 在达限后正确拒绝第二轮。
- 调度与 timeout：连续 read-only tools 保持真实并发，写/Bash/浏览器变更使用顺序 barrier；挂起的任意工具产生 `tool.timed_out` 后 Agent 可恢复，批准后的外部 HTTP 执行也有独立 tool deadline；挂起模型产生 run `timed_out`；Approval 等待超过两种测试预算仍保持 pending，拒绝后正常继续。Approval resolved/run-resumed 事件保留原 turn/step/call 关联。
- 模型流边界：provider 返回 `finish_reason=length` 且携带工具调用时，Harness 将所有潜在截断调用标记为 `notExecuted/model_output_truncated`，不执行副作用，并要求模型完整重发；HTTP 408/409/425/429/5xx、传输故障和零可见输出的无终止流才允许重试，已有可见增量的失败不会自动重放。正常 `stop` 但没有任何正文、Thought 或 tool call 且 output token ≤1 时最多指数退避重试 2 次；真实 DeepSeek 曾把内部协议错误 `✋ Error: A message with role 'tool' found without preceding user message.` 作为普通 content + `stop` 返回，Harness 现在也在 256 字符未发布尾部中识别该精确 provider sentinel，复用同一隐藏重试预算。被丢弃的空响应和 provider sentinel 都不进入 UI，但其 token、cache token、成本与完成调用数全部累计；耗尽后进入 failed，不制造占位 Final 或 Task Review。只有 reasoning 而没有答案/tool 的响应同样不能成为 completed。长度续写若只产生 reasoning 而没有任何新增正文，仍保持 `finish_reason=length`，空白 suffix 不进入 partial answer。任何可见增量都会关闭 replay 路径。若后续请求以非重试错误失败或退避被 Cancel/timeout 中止，错误会携带此前已完成响应的 usage；现在，同一失败请求在 terminal error 或缺失 `finish_reason` 之前已经返回的 provider usage 也会以该次调用精确加入 aggregate，再决定隐藏重试或失败。没有 provider usage 的失败仍保持未知，不用字符数猜 token。3 项新增 SSE 回归分别冻结隐藏失败→成功重试的两次合并结算、可见 partial 后失败的单次结算、无终止标记的单次结算，并确认 provider sentinel 不会被重复计费；主 Agent 与 compaction catch 会在进入失败/恢复状态前结算 aggregate。真实正常流 `ses_ce5652571b92423295e5` 仍以 1 model/0 tool 精确返回 `STREAM-USAGE-REGRESSION-829`，3,229 tokens（768 cached）、1 条 durable usage event、1.502 s active、约 `$0.00072653`。证据见 `reports/real-smokes/provider-failed-stream-usage-accounting-20260829.json`。SSE 支持 LF/CRLF 与无尾部分隔符，但没有 finish reason 的半截流绝不伪装为成功；retry 同时遵循 `x-should-retry`、`Retry-After` 和最大等待预算。若 provider 忽略 abort 并在 run timeout 后迟到返回，Harness 仍记录已发生的 model usage，但在持久化 assistant/tool call 之前再次检查终止信号，禁止迟到的工具或 Approval 在 timed-out 终态后启动。
- Provider finish 状态机：普通文本只有 `finish_reason=stop` 才能成为 Final；tool calls 只有 `tool_calls` 或已观察的兼容 `stop` 才能进入执行分支；`length` 只走既有续写/截断协议。`content_filter`、空值和其他未知终态都会在 usage 结算后进入 failed：已显示正文按原字节保存为 partial 供 Continue，但不生成 Final/Task Review；异常终态携带的 tool call 在 `tool.started` 前被拦截，零副作用。Exact Final formatter 同样只接受 `stop`。2 项 Agent 集成测试和 formatter/helper 断言冻结 filtered text、filtered side effect 与 `tool_calls`/零调用不一致。
- 纯 Final output-length 恢复：无 tool call 的 `finish_reason=length` 不再把半截答案标成 completed。Harness 最多按 `ANERA_MAX_LENGTH_CONTINUATIONS` 有界续写；首段保持流式但暂存最多 256 个字符的未稳定尾部，`length` 时只把最后一个换行/词边界之前的字节发布并纳入下一次 prefix，避免 `0016|AN` 与下一次 `0017|...` 被硬拼。后续调用在发布前消除完整重启、部分重启、尾首重叠和分叉重启；任何已经显示的 prefix 都不可回撤或静默替换。System prompt 同时禁止模型仅因无害输出机械、长或重复而改成摘要/拒绝。续写 HTTP 请求保留相同工具 schema 以复用 DeepSeek prefix cache，但把 `tool_choice` 从 `auto` 改为 `none`，禁止纯文本后缀中途切换工具；所有调用的 usage/cache/cost/model-call 合并结算。预算耗尽则保留已发布的安全 partial、进入 failed、`partialResponsePersisted=true`，不生成 Final/Task Review，并允许正常 Continue。11 项专项测试覆盖普通续写、预算耗尽、空/reasoning-only suffix、完整/部分/分叉重启、半行安全边界、provider payload/sentinel 和 Agent 诚实终态。最新真实 A07 run `ses_00ae106ff7374cfb82cd` 在 `max_output_tokens=4096` 下以 8 次模型、0 工具精确生成 2,500 行/62,499 B；流式正文、Final 与预期 SHA-256 完全相同，115.484 s、156,833 aggregate tokens（126,208 cached）、约 $0.042026，证据见 `reports/real-smokes/length-continuation-a07-tool-choice-none-ses_00ae106ff7374cfb82cd.json`。与同题原始 `auto+tools` 基线相比，prompt tokens -15.6%、total tokens -13.0%、active duration -5.1%、成本 -13.6%；完全移除 tools 的 rejected variant 虽同样减少 token，却使 cache-hit 降至 81,152、成本反升 4.9%，三路证据与选择理由固化在 `reports/real-smokes/length-continuation-a07-strategy-comparison.json`。最新独立耗尽 run `ses_7ef79582f54249c4b7a4` 在 `max_output_tokens=16`、1 次续写预算下以 2 次模型进入 failed，保留前 2 个完整行/50 B，0 Final、0 Task Review、1 个带 persisted 标志的 error，且 smoke 输出 `passed=true/violations=[]`；证据见 `reports/real-smokes/length-continuation-exhausted-ses_7ef79582f54249c4b7a4.json`。旧 239 B run 保留为历史低负载证据，不再作为 A07 完整覆盖证明。
- 精确 Final 格式门：单靠 System prompt 无法稳定保证 DeepSeek 遵守“最终只回答 marker”；合法 PDF 分页基线 `ses_4607dc0b2fcc45ca9fd6` 虽正确读取 cursor 和 marker，Final 仍多出解释与 Markdown。当前 Harness 仅在最新 user turn 明确匹配“最终只/仅回答、输出、回复、返回、报告”或英文 answer/output-only 语义时暂存小型候选 Final。若请求明确限定为单个 marker/token/hash/number 且 draft 已经是最多 512 B 的无空白、无 Markdown、无 prose 原子字面量，Harness 直接发布该值；路径、多行、JSON、带空格文本、泛化“只报告结果”和任何包装文本仍使用无工具 JSON 格式器删除禁止外层内容并检查覆盖。一般 Final 和当前 A07 请求继续走原流式路径，候选一旦超过 8,000 UTF-8 bytes 就不可逆地发布已缓冲 prefix、恢复普通流式并禁用该 step 的 rewrite。格式器 token/cache/cost/model-call 全量计费，只有合法非空 `{"final":"..."}` 才发布；无效、证据不足、length、tool call、Cancel 或 timeout 会诚实 failed、0 Final、0 Task Review，私有 draft 供 Continue，迟到结果不得发布。6 项专项测试冻结原子快速路径、包装/多字段/空白/Markdown 拒绝、识别/解析、草稿不可见、失败恢复、timeout 竞态和大输出回退。真实 PDF 同题 `ses_918dd4e4dd5b47fd84ab` 与 `ses_1df155262d50405fafdd` 连续两次严格输出 `PAGE-CURSOR-OK-731`；warm candidate 为 4 model/2 tool、3.918 s、35,020 tokens（13,440 cached）、约 $0.006945。相对未通过格式 oracle 的 baseline，total tokens +0.98%、配置价格成本 -0.16%，质量由 fail 变为 pass。新双轮 marker A/B 进一步证明，已经原子正确时无需再调用 formatter：`ses_f9b4ce5a6f464376b8be`→`ses_25557b905cef4d27b57b` 保持两轮逐字 Final 与页码工具 oracle，model calls 6→4。证据见 `reports/real-smokes/attachment-pagination-final-format-comparison.json` 与 `reports/real-smokes/atomic-final-fast-path-comparison.json`。这仍是 Anera 自证，不代表已证明 Arena 使用相同隐藏步骤。
- Partial-stream 恢复：provider 在正文 delta 已进入 UI 后断流、报错、Cancel、timeout 或正常 SIGTERM 时，Harness 会在失败终态前把同一份原始 partial assistant 正文写入私有 conversation context，并在 error 事件标记 `partialResponsePersisted=true`。此前仍存在 `SIGKILL` 窗口：UI 的 append-only `assistant.final.delta` 已 durable，但完整 `ModelResult`、assistant provider message 与 terminal checkpoint 均未形成，重启只显示 partial，Continue 的模型上下文却从旧 user turn 开始。SessionStore 现在只选择 active turn 的最新 `assistant.started` step，拼接该 step 已实际显示的 delta；若末尾 assistant 尚未物化则精确补一份，已经物化则不重复，早期 turn/step 的 delta 永不混入。同时以原 turn/step 发布一份 `turn.completed(interrupted)`，error 标记 partial 已恢复，并把 event count、visible bytes 与补写方向记入 `session.recovered.partialResponseReconciliation`；二次重启不追加消息或事件。2 项 Store 回归覆盖补写和 already-materialized 分支。production smoke `ses_b5e164a9b18d49c59bb2` 在 260 个可见字节落盘后直接 `SIGKILL`：重启为 interrupted，Resume provider payload 精确为 `system→user→assistant(partial)→user(Continue)`，随后得到 `HARD-CRASH-CONTINUE-CONTEXT-OK`、一份 Final/Review，partial message、interrupted turn 与 recovery event 均恰好一份。证据见 `reports/real-smokes/partial-stream-crash-ses_b5e164a9b18d49c59bb2.json`。
- 崩溃中的悬空工具调用恢复：Harness 先持久化 assistant `tool_calls`，而工具结果批次可能在副作用发生后、provider tool message 落盘前因进程退出而缺失。SessionStore 启动时现在只闭合最新 tail tool-call group，并按同一 turn/step 的 durable events 区分三类事实：已有 terminal event 时重建真实成功/失败结果；已有 `tool.started` 但无 terminal 时写入 `outcome_unknown` 合成失败并禁止盲目重放状态变更；连 `tool.started` 都没有时写入确定的 `not_started` 失败，允许按需重发。后两类分别追加 `tool_outcome_unknown_after_restart` / `tool_not_started_after_restart`，恢复映射写入 `session.recovered.repairedToolCalls`，重复重启不再追加第二份结果或事件。2 项确定性回归冻结三分支、provider 消息配对、事件归因、tool 计数与幂等；引入 durable turn-start checkpoint 后的最新真实 DeepSeek crash-window run `ses_5c3d4ed1f87b43e48356` 在 `create_file` 已实际落盘但终态完全缺失后恢复为 `outcome_unknown`，Continue 只调用 `read_file` 验证、没有盲目再次创建，最终以 2 model/2 tool、7,206 tokens（6,912 cached）、2.233 s、约 `$0.00067278` 完成。证据见 `reports/real-smokes/crash-tool-recovery-ses_5c3d4ed1f87b43e48356.json`；较早 run 保留为历史证据。这是 Anera 的崩溃安全证据，不推断 Arena 私有恢复实现相同。
- 模型分发前 semantic checkpoint：Submit/Continue 不再以“state 先写 user message、event 稍后追加”的无标记窗口进入 provider。Harness 先原子持久化 provider message、`queued` 状态和带预分配 event ID 的 `pendingStart`，正常路径在相同 ID 的 `turn.started`/`run.resumed` 落盘后才原子切换到 running；启动恢复会先用同一 ID 重放缺失或被截断的 start event，再清除 checkpoint 并进入 interrupted。这样既不会丢失已经接受的请求，也不会因重启或事件已写出而制造第二个 user turn/start event；恢复后的 error/turn terminal/run status 继续绑定原 turn。2 项专项回归覆盖 torn JSONL submit 与已发布 resume 的互斥窗口和二次重启幂等。真实进程级 run `ses_24f16c7afd474990ba6f` 在 checkpoint 后、`turn.started` 前对 child 发送 `SIGKILL`；重启后只恢复 1 份原 prompt/1 个原 event ID，真实 DeepSeek Continue 以 1 model/0 tool、3,269 tokens（3,200 cached）、0.838 s、约 `$0.00025176` 精确返回 `REQUEST-DISPATCH-CRASH-OK-829`。证据见 `reports/real-smokes/request-dispatch-crash-ses_24f16c7afd474990ba6f.json`；这验证 Anera 的 semantic durability，不声称 Arena 采用相同日志布局。
- Terminal publication checkpoint：完整 Final 过去要依次写 assistant provider message、`assistant.final`、`turn.completed`、materialized completed、`run.status` 和 `review.requested`；中途崩溃可能把已完成任务降级为 interrupted，或留下 completed 但无 Task Review。现在成功终态把 assistant message 与单个 `pendingTerminal` 原子落盘：`events` 是 Final/turn/run/Review lane，`workspacePersistenceEvents` 是 scanning/uploading/saving/saved lane；失败/取消/timeout 则保留单 terminal lane。所有事件在同一 checkpoint 预分配 ID，`publishWorkspacePersistenceStarted` 先 durable 发布 scanning，随后 `publishRunTerminal` / `publishWorkspacePersistence` 独立推进，并以各自 publication marker 记录边界。启动恢复逐 ID 补齐缺失前缀/后缀，保留先前可见 lane 的顺序，完整 Final 永不生成 interruption error，Review 的 `messageEventId` 保持指向原 Final。Store/Agent 矩阵与 production SIGKILL smoke 覆盖零发布、25 组 lane prefix、单 lane committed、二次重启和 `completedAt` 稳定。真实 DeepSeek child run `ses_818fd4476e3342d1be02` 在 provider Final/usage/pendingTerminal 已持久、四个终态事件均为零时被 `SIGKILL`；重启后恢复为 completed，四事件各 1 份、Review/Final ID 相同、0 interruption error，1 model/0 tool、3,240 tokens（3,200 cached）、约 `$0.00024642`。证据见 `reports/real-smokes/terminal-crash-ses_818fd4476e3342d1be02.json`；这是 Anera terminal durability 证据，不推断 Arena 的私有事务实现。
- Managed Process/Website 硬崩溃安全：`build_and_start` 的 detached child 过去可能在 PID、`process.started` 与 materialized Process state 尚未全部落盘时被服务 `SIGKILL`，留下不可追踪的孤儿或仍显示 running 的假 Website；即使 Process 已恢复为 interrupted，Restart 也只认识旧 AgentService 的内存记录而必然失败。ProcessManager 现在通过 sanitized parent-death guardian 启动真实命令，父管道关闭即 TERM→KILL 完整目标进程组；start 在 append start event 与物化 state 都完成前不返回，stdout/stderr/stop 写入按进程串行。SessionStore 从 `process.started/output/updated/stopped` 对账 event-only、state-only 和 stale-state 撕裂；其中 `process.updated` 是 authoritative ownership snapshot，省略 port 且 listener 数组为空会在重启时明确清除旧 port。恢复清理仍只在 `/bin/ps` 命令含该记录随机 `process_id` 时允许向持久 PID 发信号，PID reuse/mismatch 明确 `not_killed`，然后幂等发布单一 recovered stop 与 Website `asleep` 投影；真正非零退出仍立即把对应 Website 置 `failed`，deliberate/zero exit 与服务恢复则进入 `asleep`。所有静态/managed 发布、Restart starting/success/failure 与 process-stop 投影现在统一走 event-first `recordWebsiteUpdate`，启动按 Website 自身 `updatedAt` 双向对账：event-only 会物化 state，legacy state-only 会补事件，managed state-only running 会先恢复丢失的 published 边界再发布 `asleep`，二次重启均不重复。每条 Process 另保留不进入 canonical/UI 比较、仅用于在 ownership-verified listeners 中排序和重启命令的 durable port hint；hint 本身绝不是 ownership fallback。HTTP Restart 在 live record 缺失时从持久 command 启动新 Process ID，固定端口、页面内容、restart count 与原事件归因均恢复。新增回归覆盖 durable-start 等待、Process/Website 双向 event↔state、身份不匹配零信号、自然 exit 3 投影、自动 idle sleep、admission 取消计时、HTTP durable restart 和重复重启。历史 OS run `ses_b28cec4d5f0a487c82f4` 在 `asleep` 校准前记录了 Process `interrupted/SERVER_RESTART` 与 Website `failed`；当前状态语义由 530 项全量回归和最新 production `website-idle-sleep-ses_dbb1c55457454bbb941e` 两次 `asleep` 证据取代，历史 guardian/零孤儿证据仍有效。 Preview 端口归属现在是独立 durable boundary：Linux 通过 `/proc` 同时校验 guardian managed ID、PID/start-time identity、process group/后代 socket inode，并要求进程树与监听器的前后快照稳定。探测结果先发布并等待独立 `process.updated` ownership snapshot；提交前不暴露 Preview port，提交失败会清空内存 port/listeners，重启还会把 torn `process.updated` 当作 authoritative clear。ownership unknown（包括 Windows）一律 fail closed，不能借用同 Session、跨 Session 或宿主 decoy port。IPv4/IPv6 descendant listener、持久化失败、torn clear 和三类 decoy 均有确定性回归；真实 Linux `/proc`/Bubblewrap canary 已实现同类 oracle，但尚未在适用 Linux 主机运行。
- Durable HITL/recovery：`hitl.required` 发布前先注册 resolver，因此 required-event 监听器内立即回答不会丢失。checkpoint 记录 turn/step/call/call-index 和 first-write-wins 决定；重启后按原 assistant tool-call 顺序重建 mixed HITL + completed-tool batch，parallel HITL 必须全部完成后才恢复整个 batch。duplicate/cross-turn call ID 通过 durable turn/call-index 边界配对；并行 `add_voice` 的 public ID 在同一 per-Session durable queue 内分配，映射另持久化 `sourceCallId` owner：相同 call replay 幂等，不同 call 即使 provider/language 相同也不能复用 ID，legacy 无 owner 记录只能由匹配的 durable reservation 认领。audition 文件在等待重启期间保留（含下划线路径），选择恢复后清理。已批准但 terminal 不明的外部写仍返回 `outcome_unknown`，不盲重放。
- 结构化 HITL 完成态可见性：timeline projector 在 `hitl.resolved` 后确定性派生一条普通 user bubble，稳定 key 为 `hitl-answer:${hitlId}`，并始终插在对应卡片的下一项；重复 resolved 只更新同一对象，不追加第二条，完整 snapshot reload 会得到字节一致的顺序和内容。`ask_user` 同时兼容规范化的 `questionId/selectedOptionId/customResponse` 与历史 `question_id/selected/free_text`，单题显示选项 label 或自定义答案，多问题按问题逐行显示；plan revise/reject/accept、voice candidate 与 image option 生成可辨识的用户选择摘要。`skipped/dismissed`、图片 Skip 与 `hitl.expired` 不生成伪 user answer，若异常轨迹先 resolved 后 expired 也会移除派生项。v21 的 pending/resolved-after-reload 两个 desktop state 和 `hitlInteraction` 断言进一步冻结唯一性、紧邻、selected/custom 可见与刷新持久化；这是 Anera 对结构化交互历史可读性的确定性投影，不证明 Arena 使用相同内部 key 或逐字摘要。
- M04/S05 任务恢复：M04 冻结 provider rejection 后两个已上传附件仍保留，下一条普通同 Session retry 可直接读取并完成；S05 冻结取消前台 Bash 会终止 guardian tree、保留取消前已产生的输出，并允许下一条普通 follow-up 直接开始，不要求 Resume。
- 桌面输出跟随：对话区现在显式区分 `following` 与用户阅读历史位置。长 Session hydration 默认到最新输出；near-bottom 时 SSE、流式正文与工具卡 resize 继续贴底；wheel/touch/key/pointer 会中止平滑滚动并暂停跟随，显示 icon-only `Scroll to bottom`，点击后恢复。滚动不再掩盖 offscreen Final 的 Task Completion 可见性条件。`reports/ui-execution-log-v21/capture-summary.json` 将 hydration、top/expanded control、阅读位置不变、点击到底/隐藏、SSE resize 与 Task Completion ingress 8 项全部纳入 passed/exit predicate；61 个 1440×900 状态为 `passed:true`，0 console/horizontal/outer vertical overflow，并保留 persistence/reload、right-rail ownership 与结构化 HITL checks。它仍是 Anera 桌面回归，不是 Arena pixel parity。
- Session admission 与终态清理：Message/Continue 在任何异步 store、credit 或模型操作前取得 per-session reservation，两个并发请求最多只有一个能创建 user message/`turn.started`。上一 episode 的 terminal status 可能比 detached promise 的 `finally` 早一个极短窗口可见；新请求会等待该 run 的全部尾部事件和 active 清理完成，而不是误报 running 或让新旧事件交错。专项回归同时冻结“真正重叠必须拒绝”和“status completed 后立即下一轮必须成功”；该修复也消除了全量并行测试中真实复现的连续模型选择竞态。
- Graceful service shutdown：SIGINT/SIGTERM 先关闭 HTTP listener/idle connection，并从收到信号的第一毫秒启动五秒 hard deadline；AgentService 同时 fail-close 新 Message/Continue/Approval 为 `503 service_shutting_down`。已经取得 reservation 但仍停在 credit/store admission 的请求会被等待到跨入 active，再由第二次 abort 扫描终止；等待 durable Approval/HITL 的 run 保留原卡片与原 turn/step/call，其他正在模型流或工具的 run 以 `service_shutdown` 原因落成 durable `interrupted`，与用户 `cancelled` 和 Harness `timed_out` 分离。Shutdown 会停止所有 managed Process，等待适用的 active terminal publication，关闭每个 BrowserContext 和共享 Chromium，然后主动关闭 EventSource 等不会被 `closeIdleConnections()` 处理的长期 HTTP transport；等待中的 Approval/HITL 不发布 `expired`，重启后沿同一 episode 恢复。确定性回归覆盖 active、pre-active reservation、Approval/HITL 等待卡片、并行回答、first-write-wins、Deny 零副作用、媒体候选复用和已批准外部写的 `outcome_unknown` 边界。最终 production build 的端到端 smoke `ses_0c3b3ae1213142538836` 同时保持一个真实 Session SSE 客户端和已流出可见正文后挂起的 provider SSE：SIGTERM 后 1 ms 停止 admission、7 ms 正常退出（code 0），客户端 SSE 与 provider socket 都在 cleanup 前关闭；重新打开 Store 后 128 个可见 partial bytes 被精确保留一份，恰好一份 interrupted error/turn terminal/run terminal、0 Final、0 Review。再次启动 production server 后，真实 Chromium 同时验证 partial response、shutdown error 和 Continue 可见、Task Review 不存在，并在浏览器 EventSource 仍连接时以 95 ms/code 0 再次关闭。证据见 `reports/real-smokes/graceful-shutdown-ses_0c3b3ae1213142538836.json` 与 `reports/real-smokes/graceful-shutdown-ses_0c3b3ae1213142538836-recovered-ui.png`。
- Abort-ignoring tool boundary：过去 per-tool timeout 能中止合作工具，但用户 Stop、run deadline 或服务关闭若碰到收到 AbortSignal 后仍不 settle 的实现，会被拖到独立的 120 秒 tool deadline。Harness 现在先传播 abort，并给合作工具 50 ms 返回更具体错误；仍不返回时发布 Arena-shaped `tool.failed(reason=run_aborted)`，随后立即落成 `cancelled|timed_out|interrupted`，迟到的 vision/image provider usage 仍按原 call identity 幂等结算且不改写终态。2 项回归分别冻结“取消后 200 ms 才返回且带 usage”和“approval 类调用永久不返回”；真实 production Shell run `ses_08389969b311498fa162` 在 Stop 后 267 ms 进入 `cancelled`，保留更具体的 `Command cancelled`，tool failure seq 8 严格早于 run terminal seq 11。证据见 `reports/real-smokes/cancel-ses_08389969b311498fa162.json`。
- Cancel 后结算 drain：abort-ignoring provider tool 在 50 ms 有界边界后不再阻塞 `cancelled`/`timed_out` 终态，底层响应若稍后返回，usage 仍以稳定 settlement identity exactly-once 持久化。该晚到结算不再错误依附于已结束的 `ActiveRun`；AgentService 单独跟踪已开始的 background usage settlement，`shutdown()` 在允许 SessionStore 被关闭/删除前 drain settlement 与 per-session usage queue，但不等待永不返回的底层工具或其 60 秒 deadline。新增回归先观察晚到 `usage.updated`，再要求 shutdown 返回后的每个 settlement 都已有 `usageEventId`；全量并发运行不再触发临时 Session 目录 `ENOTEMPTY` teardown 竞态。
- Graceful resource shutdown：单独成功的 Manager 测试不能证明组合关闭次序，因此新增 production provider 状态机真实驱动 `create_file ×2 → build_and_start → browser open → partial stream`。最终 production build 的 run `ses_b53e268dc5354b3e907c` 在 SIGTERM 前同时持有可访问的 managed Website、1 个 guardian、完整 npm/Python server 树、1 Browser/1 Context、7 个 Chromium 后代和挂起 provider socket；服务在 38 ms 内 code 0 退出，捕获的 10 个后代 PID 全部消失、Website 端口关闭、provider socket 清零。该历史 smoke 在 Asleep 校准前重开 Store 后记录 Session `interrupted`、Process `stopped`、Website `failed`，对应 process-stop/Website-terminal/interruption 各恰好一份，0 Final、0 Review；当前同一非故障释放会投影为 `asleep`，零孤儿与关闭次序证据不变。证据见 `reports/real-smokes/graceful-resource-shutdown-ses_b53e268dc5354b3e907c.json`。
- Durable 事件归因：active run 现在持有稳定 turn identity，`cancelling`、run timeout 的进程清理、`build_and_start` 的 Process/Website/File/Artifact 子事件均保留原 turn/step/call；替换 Website、`stop_process` 与终态后的 Website Restart 把旧进程停止和新进程启动归因到实际触发操作，不再落成 canonical 中无法定位的孤立系统事件。Process manager 让 context 随长期 stdout/stderr/stop 生命周期持久传播，ToolExecutor 在入口统一注入 call ID。专项测试冻结 cancel、managed process start/output/stop、replacement 与静态/managed Website 关联；真实生产 run `ses_4ece6c7a645049e6a3d9` 在 13.623 s 内以 9 model/10 tool、49,298 tokens（46,592 cached）、约 $0.00488348 完成 build/start、端口 43129 浏览器验证与真实 Restart，Process ID 改变且 3 个 Restart 子事件全部保留终态 turn/step 归因。
- 工具协议与审批竞态：每个工具调用在执行前按其公开 JSON schema 验证 required/additional/type/enum/range，并将可修复错误返回模型；无效 JSON 即使对应可省略参数的工具也不会触发默认动作。run signal 在进入 Approval 前已取消的竞态已用迟到模型响应复现并修复，最终稳定传播为 `timed_out`，不会永久挂在 awaiting approval。
- 开源 Harness 代码级差分：本轮固定审阅了 [earendil-works/pi `853a80d`](https://github.com/earendil-works/pi/tree/853a80d26c90a14c1886f0ebb8ffaae133ca2185) 的 agent loop、effect sandwich/operation program-counter 规范，以及 [can1357/oh-my-pi `33cc6b9`](https://github.com/can1357/oh-my-pi/tree/33cc6b9a043a74e00a157e72ca909272796d8461) 的 replay policy、tool-loop guard、prompt-cache-aware pruning与 provider stream timeout。Anera 既有 durable pendingStart/terminal/tool/workspace/usage、可见输出后禁止 replay、context overflow 独立恢复、cache-price gate 和 repeated-call guard 已覆盖对应主机制；发现的实际缺口是“整个 run 有 deadline、单个 provider attempt 首事件前却能长时间悬空”。现在每个请求默认以 `ANERA_MODEL_FIRST_EVENT_TIMEOUT_MS=60000` 等待首个真实 SSE data event：超时在零可见输出时进入既有 bounded transport retry，收到首事件立即解除窄 deadline，已观察 usage 仍精确结算、未观察 usage 不猜测。2 项新增回归冻结 pre-stream stall 重试和首事件后 quiet stream 不误杀；真实 Shell protocol smoke 继续以 3 model/3 tool 零失败完成。没有迁移 PI/OMP 的多 lane、subagent、steering 或 plugin 内部结构，因为当前 Arena 公开/页面证据未显示这些是 Agent Mode 可观察契约。
- 计量：多轮 active duration 明确排除 Approval 等待，DeepSeek cache-hit token 同时进入 session ledger、usage event、canonical outcome 和成本计算。
- Web/预览安全：26 个 IPv4/IPv6 非公网分类探针；HTTP file/preview/download/ZIP 以及 attachment、vision、Website、browser screenshot 等专用工具入口均用真实 symlink 做防泄漏验证；Preview CSP、iframe sandbox、跨站 POST 拒绝和流式 response body 上限均有回归。
- Browser 工具执行边界：此前 Harness tool timeout 会返回失败并触发 AbortSignal，但 Playwright action 不消费该 signal，超时后的 click/navigation 仍可能继续；managed Website 页面还运行在无 origin 约束的宿主 Chrome 中。当前浏览器只接受 loopback preview 入口、禁用 Service Worker，并在 BrowserContext 层把 HTTP、WebSocket、redirect 与 popup 全部限制到本次 preview 的精确 origin；普通跨源链接在 click 前直接拒绝。open/snapshot/click/fill/select/check/press/scroll/viewport/screenshot 均绑定 Tool AbortSignal，Stop/timeout 会关闭目标 Session Context，禁止延迟同源副作用，后续动作再建立干净 Context。2 项真实 Chrome 回归证明同源 fetch 成功而跨源 HTTP/WebSocket/navigation 零到达，以及 click 已发生后 abort 仍阻止 150 ms 延迟请求。真实 DeepSeek run `ses_fc4002dbf8e44c1d9426` 在 13.194 s 内以 8 model/9 tool、44,754 tokens（37,760 cached）、约 `$0.005542` 完成 `open→fill→select→check→click→scroll→console` 并命中 `Alice|pro|accepted`；`ses_f71d450889ba4eaaa770` 在 18.507 s 内以 10 model/9 tool、43,933 tokens（32,512 cached）、约 `$0.006805` 完成双 click、375×700 viewport、console 与 durable screenshot。证据为 `reports/real-smokes/browser-form-ses_fc4002dbf8e44c1d9426.json` 和 `reports/real-smokes/browser-task-ses_f71d450889ba4eaaa770.json`。这关闭浏览器页面直接跨源和迟到动作，不改变 Linux managed server 自身仍共享网络的已知限制。
- Browser 多会话资源模型：旧 BrowserManager 为每个首次使用 Browser tool 的 Session 启动并保留一整个 Chromium，进程/基础内存随 Session 数线性增长，且并发首次调用可能重复创建；首轮共享 Browser 后虽把进程降为常数 1，完成态 Context 仍会随历史 Session 无界累积。当前是 service 级懒加载单一 Chromium + active Session 隔离 BrowserContext；共享 launch promise 和逐 Session creation promise 分别消除两种竞态。Context storage/page/ref/network policy 相互隔离，关闭/Abort 只终止目标 Session，其他 Context 继续工作；每个 Agent episode 在 terminal publication 完成后、active reservation 释放前关闭目标 Context，使 immediately-following turn 不会与旧 page 竞态。最后一次成功 Preview URL 作为小型私有运行态保留；下一轮 snapshot/click 若发现新 Context 的 about:blank，会重新通过 loopback/origin 验证加载页面并在操作前重建稳定 refs。shutdown 清除 Context、Browser 与 URL cache；底层 Browser 意外断开会清除 stale context record，下一动作自动 rehydrate。`/api/health.browser` 明示实例、Context 和 pending 数。真实 Chrome 回归覆盖 1 Browser/3 并发 Context、同 Session creation 去重、状态隔离、独立 close、终态 Context 1→0、下一轮不调用 open 的 snapshot rehydrate、closeEverything 和强制 disconnect 重建。生产态并发 DeepSeek run `ses_fbb15da1e7bb49709bee` 与 `ses_9bcc8f1e91e541b281f6` 分别通过表单和 screenshot oracle：同时运行时 health 峰值精确为 `browserInstances=1,sessionContexts=2`，两者终态后自动收敛为 `1/0` 且 pending 为 0。证据见 `reports/real-smokes/browser-episode-release-ses_fbb15da1e7bb49709bee-ses_9bcc8f1e91e541b281f6.json`。
- 最新 production HTTP 复核：`/api/health`、`/`、`/agent/<session-id>` 刷新入口和 canonical JSONL 均为 200；HTML preview 返回 CSP、`nosniff`、`no-referrer`，跨 origin POST 返回 403；并发同名上传原子落为 `duplicate.txt` 与 `duplicate (2).txt`，没有覆盖。

- 无分隔符长度续写：原协议只在空白/标点处提交安全 prefix；长 hash、base64、压缩 payload 或单行 token 没有分隔符时会丢弃整段 provider 输出，改为直接保留后又会让模型在边界猜下一个字符。当前 fallback 使用 Unicode-scalar 安全边界，continuation 请求显式携带最后 64 字符 overlap，模型先回显锚点再续写，Harness 确定性去重；只回显 overlap 或只产出 reasoning 的 `stop` 会继续消耗尚存的 continuation 预算，而不是提前失败。明确要求单个 marker/token/hash/number 且 draft 已是无空白原子值时，fast path 的旧 512 B 限制扩展到与 formatter gate 相同的 8,000 UTF-8 bytes，避免对已正确长 token 再做一次昂贵且可能截断的 JSON 重写。新增失败回归先分别复现“600 字符 prefix 全丢”和“overlap-only stop 提前退出”，并验证 surrogate pair 不被拆分；该阶段全量为 487 tests。真实 DeepSeek `ses_5df4baa3d2584e37a449` 在 `max_output_tokens=96` 下以 7 次 provider call、0 tool 精确生成 1,024 B 连续 token，stream/Final/预期 SHA-256 同为 `889a223776090e572d69a64caf91c2291ff24751f661300461ec1436c934fb75`，0 error、1 Review、15,691 aggregate tokens（11,008 cached）、9.016 s、约 `$0.00255787`。相对同配置修复前 completed 但漏 1 字符的 run，calls -30.0%、tokens -24.5%、active duration -35.7%、cost -19.5%；证据见 `reports/real-smokes/continuous-length-continuation-ses_5df4baa3d2584e37a449.json` 与 `reports/real-smokes/continuous-length-continuation-comparison-20260829.json`。

- Continue 的 exact-output 语义持久化：失败、取消、timeout 或重启后的 Continue 会追加内部 `[Harness operator action: Continue]` user message；旧 exact-only 检测把它当成最新真实任务并立即停止向前查找，原始“最终只输出 marker/token/JSON”约束因此丢失。仅扫描更早 model messages 也不可靠，因为长任务的原始 user group 可能已被 checkpoint 汇总。当前每个新真实 turn 在 `pendingStart` 的同一原子 state 写入中设置或清除私有 `activeTaskExactFinalRequest`；Continue 继承该字段，普通后续 turn 必须覆盖/删除，内部 operator message 仅作为 legacy fallback 被跳过。确定性集成先复现 Continue 后 formatter 未调用，再冻结 failed→Continue 的 draft 隔离、两次 usage、精确 Final，以及下一普通 turn 不继承旧 gate。真实 DeepSeek `ses_9c563a9834d849d19d4b` 首轮在 5 ms deadline 下 `timed_out`、0 model usage；服务重启同一 Store 后 Continue 以 1 次任务 draft + 1 次 exact formatter、0 tool、0 error 精确发布 `{"answer":9193}`，stream 与 Final 一致并生成 1 Review，总计 3,445 tokens（3,072 cached）、1.393 s、约 `$0.00032986`。证据见 `reports/real-smokes/exact-final-resume-ses_9c563a9834d849d19d4b.json`。

- Approval Deny 的任务级持久化：外部写操作的拒绝不再只存在于当前 `requestApproval` Promise。每次 `approval.resolved` 持久化工具名与递归稳定参数的 SHA-256 请求签名；后续执行以最近一个真实 `turn.started` 为任务边界，Continue 的 `run.resumed` 不清除此边界。若同一任务在失败、服务重启和 Continue 后再次请求语义相同的 `http_request` 或 `deploy_project`，Harness 会生成 Arena-shaped `tool.failed(notExecuted=true, reason=prior_approval_denied)`，不执行副作用且不再次弹 Approval；新的真实用户 turn 会自然建立新边界，允许用户明确重试。确定性集成测试还将 Continue 请求的 JSON key 顺序打乱，证明比较的是递归规范化语义而非 provider 字符串或 call ID，并冻结首次 Deny 1 次 Approval、跨磁盘 Store 重建的 Continue 0 次新增 Approval、新任务第 2 次 Approval 和总计 6 次模型调用。随后真实 DeepSeek smoke 首先暴露 extension router 只识别“POST→URL”而漏掉中文“向 URL 发送 POST”和显式 `http_request` 下划线的缺口；多语种双向语序已修复并新增路由回归。修复后 `ses_e4dac505cf8d4a7d81c7` 正确产生 1 次 Approval/1 次 Deny/1 次 failed tool、零重试，Final 明确报告未发送；2 model/1 tool、7,103 tokens（4,224 cached）、1.985 s、约 `$0.00118838`。证据见 `reports/real-smokes/approval-deny-ses_e4dac505cf8d4a7d81c7.json`。这些是 Anera 的安全/生命周期保证，F04 reference 已证明当前样本无 Approval、POST 直接 204；真正的 deny/Continue 分支仍不可达，不能据此降低 Anera 审批门。

## 尚未关闭的等价性缺口

在继续做 Arena 成对 parity 前，Anera 自身仍有三个外部环境/账户 gate：DeepSeek、Tavily Web Search、Pexels-bypassed Tavily 图片回退与 Firecrawl 抓取/turn-cache 路径已由 schema 1.2 live canary 闭环；Pexels、图片生成/语音 provider 尚未成功完成非 fixture canary 和实际账单对账；真实 GitHub 账户/private repository 尚未成功完成只读 canary，GitHub writeback 虽已有显式 opt-in 执行器，但还缺专用可删除私库上的外部 passed 报告；Linux + Bubblewrap 尚缺适用主机上的真实 smoke。三者的 fail-closed 执行器、报告 schema、运行参数与退出码都已就绪，但上述阻塞报告不能冒充通过证据。语音 provider 子调用、物理计量、估算费用和八种公开容器格式已经进入 Harness 闭环，不再列为实现缺口。完整分层与优先级见 `AGENT_HARNESS_CAPABILITY_MATRIX.md` 和 `PRODUCTION_CANARY_RUNBOOK.md`。

这些是后续外部环境/账户 gate，不阻塞本轮普通 Agent Mode 主体能力封板；GitHub Connector 的继续完善和真实账户覆盖按用户范围明确暂缓。

当前 `observable-parity-v2` evaluator 只对 manifest、冻结 task set、trace identity、pairing 字段、coverage 集合和 visual report 引用等结构约束 fail closed；这不表示证据语义或来源已经受信。`v2_semantic_provenance` 被强制关闭，因为 opaque raw evidence、任务 quality、AF/H/tool coverage 映射及 visual capture/provenance 仍是尚未独立验证或重算的 evidence-producer assertions。当前只有 9 条 historical reference、0 条 v2.0 eligible reference、0 条 exact-version Anera candidate，因此正式 v2 parity 必须保持关闭。

1. Canonical importer/diff 已用 9 条真实 Arena 历史版本 run 校准；确定性生成器现从 body/artifact 证据重建 231 个事件，并修正 duration、A01/A02 Review 和唯一 ID Artifact 融合。9 条 task version 为 1.1/1.6/1.7/1.8，均不是当前 v2.0 paired baseline；旧的 7 条 source canonical 不再与增强后的 curated 输出作逐字节相同声明。由于尚无 exact-version Anera candidate、完整 65 题和可见 Arena usage，当前仍不能计算正式 Arena↔Anera 分数。清单见 `reports/arena-reference-corpus-2026-08-30/`。
2. 当前真实页面已排除把 paired Battle/Vote/Reveal 当作 Agent Mode 主路径；公开完成态 bundle 已冻结 `check_in` Review、No 后 Undo 与独立 `task_completion_bar`/thank-you 的文案、action mapping、显示条件、请求形态、动画时序和可见 DOM 标识，Anera 已分别实现并禁止跨 variant 换算。A01、A04、U03 已有行为录屏/截图，R01/A05 只有 raw-only 素材；仍缺这些状态的同任务同 viewport DOM/PNG、完整网络请求和结构化持久化 reference，用于校准精确像素、动作时序及服务端持久化表现；公开客户端没有给出 Undo 时 Workspace/Artifact/Deployment 是否版本回退的证据，当前只能保守保持 Workspace 不变。
3. 当前完成态公共 bundle 已暴露 Agent/Coding system-prompt templates、工具 descriptions 和 active registry，因此“system prompt 完全私有不可得”不再准确；Anera 已迁移默认 active 19 工具，并实现 `ask_user`、`propose_plan`、`add_voice`/`generate_speech`、`list_connector_tools`、`image_search`、`present_file`、`compact` 和 active process triplet 的状态机与 UI 投影。`list_connector_tools` 现在真正执行 task-scoped list-and-load：仅 `enabled` 结果把该 slug 的注册 schema 加入下一 provider step，当前任务跨 step/Resume/compaction 保持，新真实任务按 Composer switch 重新采样，参数校验和 executor fail-closed。GitHub 已有生产 read-only Agent adapter，动态执行仓库列举、branch-head commit 归因和有界文本文件读取，并在 disconnect 后无需重启即可 fail closed；真实 DeepSeek + 签名 GitHub App + 注入 GitHub API fixture 已验证完整模型/事件/UI 路径。仍缺真实外部 GitHub installation/private-repository smoke，以及 Arena GitHub connector 的原始动态工具名/schema/授权范围和副作用 reference；当前公开 Connections UI 没有 Slack/Notion，所以它们不是待补 baseline。Arena 未公开的 feature sections、服务端附加上下文、模型路由、缓存与定价，以及这些实现的同任务策略/时序仍未知，必须由 65 题 reference 校准，不能由 Anera 自测替代。
4. Approved 外部 POST 的生产策略已有注入式 fetch 测试；本轮没有自行执行真实外部写入。Deny 分支已用真实 Agent 验证零外发。
5. macOS 使用 Seatbelt；Linux 在 Bubblewrap 可用时已有等价的宿主只读/隐藏 HOME/Workspace 可写边界与 foreground 网络 namespace，并可配置无沙箱时 fail closed。当前机器只能真实执行 Seatbelt，平台路径由 7 项确定性 invocation/降级测试覆盖，尚缺 Linux 主机上的真实越界读写 smoke；Linux managed Website 为保持宿主 Preview 可达仍共享网络，Windows 与无 Bubblewrap Linux 只有策略/净化环境，不能声称相同强度。
6. `ARENA_MANUAL_PROBE_RUNBOOK.md` 的能力封闭批次尚未采集完：目前 43/65 个任务有目录，只有 9 个历史版本结构化 run，覆盖 17/57 个 H 维度并保留 4 个 `UNMAPPED-001`；当前 v2.0 eligible reference 仍为 0。因此不能计算完整逐能力 conformance vector 或成对质量/效率差。
7. 视觉 capture/diff 已具备确定性 Anera 回归和 CI 硬门槛，但现有 Arena screenshot/video 未形成同任务、同 viewport、同状态的 DOM/PNG reference；尤其 `agentic-custom-feedback` 的 treatment-1/treatment-2 入口、chip、成功历史卡片、失败回滚和 trusted marker 网络投影仍缺 Arena 同状态 DOM、PNG、请求 body/response 与刷新持久化 reference。因此不能把 Anera self-diff 或真实 DeepSeek smoke 当成 Arena 视觉/协议等价证据。
8. 2026-08-29 对 Arena 公开 `/agent` bundle 的只读核验确认 GitHub 是独立 coding product mode，且 permission hint 明示 owner 安装 app 或授予仓库访问；Anera 已实现其可观察 API、Connections、repository/branch selector、repository-selectable GitHub App 安装与短期 token 路径，并安全导入 durable coding Session，同时为普通 Agent 提供只读 connector。当前仍没有真实外部 GitHub App 安装与私有仓库交互 smoke，也没有 Arena 对仓库写回、PR/commit/branch 创建的可见语义证据。Credits 产品状态现已作为独立于 token limit 的 ledger/API/UI 实现并回归，但仍缺登录 Arena 的真实账户状态作为视觉和边界校准证据。
9. Anera 的 Deployment 已验证本地 revision 生命周期，并支持由管理员配置已真实暴露的 `ANERA_PUBLIC_BASE_URL`；工程本身目前不提供第三方托管账户或公网基础设施。D01 reference 显示该 Arena 样本没有 Deploy/Publish surface，临时 E2B Preview 也未通过公开可达验证；它是明确的 unsupported/fail 观察，仍不能回答 Arena 真正部署 URL 的缓存传播、更新原子性和删除/下线行为。
10. 真实 Arena 完成态确认的右侧约 60% docked Website/Artifact pane 已在 Anera 实现：桌面左侧对话约 40%；Website 由用户显式打开，`present_file` Artifact 可自动打开，Preview 打开时仍保留 Task Review，同 URL 手动关闭后不被 snapshot/SSE 重开，Workspace 与 Preview 互斥；toolbar 使用 icon-only `Preview`/`Raw source`、Preview-only Refresh、中央 `Switch file`、Pick an element、Download 和 Close，Raw source 为带行号/轻量高亮的单面板，不再保留旧 200 px Files tree。Workspace 文件树的普通文件现在也复用同一 docked Preview，而不再打开新标签页；Anera 的 `present_file` 现在为 Markdown/text/data、图片、音频、视频、PDF、DOCX、XLSX 和 PPTX 提供对应 docked surface；未知二进制类型才使用明确 open/download fallback。真实会话已验证 `.txt` 不再落入被 Chrome 拦截的 iframe；`reports/ui-execution-log-v21/` 的 61 状态 desktop-only capture（0 console/horizontal/outer vertical overflow，8/8 conversation-follow 及 persistence/ownership/HITL checks 为 true）与真实 DeepSeek Office `present_file` 浏览器 smoke 又分别冻结确定性和生产三类 OOXML viewer。仍缺同一完成任务、同 viewport 的 Arena DOM/PNG reference 来校准分割线、toolbar 精确尺寸、语法高亮和不同 Artifact viewer，因此当前证明的是公开结构契约吻合，不是像素 parity。

## 部分 Arena 数据到达后的关闭流程

1. 保留已导入的 9 条历史版本 reference 作为 schema/行为证据；按价值优先把 34 个 raw-only 任务补成 `metadata/events/QC/canonical`，处理现有 4 个和后续新增的 `UNMAPPED-*`，并为当前 v2.0 另采 exact-version Arena reference。
2. 只有 `taskVersion`、prompt、附件、人工动作和 viewport/zoom 一致时，才对 Anera run 使用 `trace:diff` 计算事件状态机、Artifact/Final、任务时延、模型/工具调用、token 与成本差异。
3. 先修结构差异，再修策略差异；不要为单条轨迹硬编码提示词或工具调用。
4. 所有冻结能力 ID 至少获得一个可定位证据，`UNMAPPED-*` 为 0 后，才允许评价 observable-contract fidelity。
5. 只有外观、任务质量、效率/成本和生命周期四条基线分别达标，才能把完整目标标记完成。
