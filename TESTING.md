# 测试策略

按模块职责和资源边界组织测试，不为某个新闻、会话或模板任务新增专用测试流水线。

证据表示改动先用固定材料做只读投影比较，核对字节上限、来源覆盖、逐字引文、缺失计数与源哈希，不为此启动付费模型。共享excerptRef的解码、损坏引用拒绝、脱敏后身份和收据失效由模块用例覆盖；模型实际看到紧凑输入。投影改善不是语义质量通过，仍需在集中验收时检验真实任务结果。

## 日常只运行需要的层

| 入口 | 用途 | 执行范围 |
| --- | --- | --- |
| `npm test` | 每个改动批次的快速反馈 | 模块契约、纯逻辑和有界文件夹具 |
| `npm run test:watch` | 连续编辑 | Vitest 依赖图驱动的模块/集成增量验证 |
| `npm run test:plan` | 零执行预览 | 列出受影响测试和延后浏览器文件 |
| `npm run test:changed` | 合并一批修改后 | 受影响模块 + 集成；不自动启动完整图形层 |
| `npm run test:changed -- --base origin/main --include-browser` | 分支完整影响验收 | 包含传递依赖影响到的浏览器用例 |
| `npm run verify` | 跨模块交接 | 类型检查 + 全模块/集成，不重复构建 |
| `npm run test:browser` | 图形、字体、DOM 或 Browser 模块验收 | 真 Chromium 图形层 |
| `npm run test:full` | 发布、主分支或明确完整验收 | 所有 Vitest 用例，零付费模型 |
| `npm run test:ui` | 聊天交互模块验收 | 隔离样例的真实滚轮、键盘、触屏和 Viewer |

指定文件可用 `npm run test:full -- src/server/xxx.test.ts`；不需要先跑默认检查再重复同一专项。构建只在构建配置、产物或部署验收涉及它时运行；Showcase 和 Production 是不同产物，不作为每次逻辑修改的双重前置步骤。

`test:changed` 用 Vitest 自带的传递 import 图，不维护“源文件 → 测试”手写映射。包含暂存、未暂存、未跟踪文件及可选 base 的已提交差异。删除文件、配置、脚本、CSS、文档和磁盘夹具变化保守选择整层；源变化找不到消费者也回退整层。它不执行测试来发现测试，没有成功缓存；没有变化时输出“未执行”，不是新的全量通过。无法读取 Git 或解析依赖直接失败，不猜测成功。

测试项目在 [test-tiers.ts](src/eval/test-tiers.ts) 中互斥划分：新普通测试默认模块层，`.integration.test.*` 和 API / 进程 / 编排等现有重型套件归集成，`.browser.test.*` 和既有真实图形套件归浏览器。同一个文件在完整验收中只执行一次。分层不会删除测试；仅模块层通过、延后图形层和完整通过必须分别报告。

## 费用安全边界

Vitest 在加载产品配置前使用 synthetic 凭据和不可达 Provider 地址，并拒绝对真实 Provider 的网络派发。测试需要通过显式假传输或自己创建的 loopback 服务提供响应，不使用开发者 `.env` 的真实账户。离线测试不能证明真实模型质量或实时网络可用性。

旧 `test:*` / canary / benchmark 中会真实调用模型且未预算化的入口已封存：即使传 `--live` 也拒绝。历史脚本与证据保留，不能通过指向正在运行的 Anera 服务来绕过预算。纯对账、离线夹具和独立账户工具不冒充模型验收。

- `npm run test:paid:preflight`：只读现有授权与账本，不发网络请求，不创建会话，也不创建新额度。
- `npm run test:paid`：显式请求预算化验收；每个物理请求（含续写、重试、摘要和 Vision）先按最坏成本预留，未定价路由拒绝。
- 用户于 2026-09-09 明确解除异常停机限制及预算金额上限，当前 `capCny:null` 表示**无固定金额上限**，不是零额度。授权 `user-2026-09-09-remove-test-budget-limit-v1` 通过 `limit_removed` 追加到同一账本；原 ¥20 → ¥40 → ¥70 历史、全部费用与 6 笔未知预留保留，不伪造结算。普通启动/预检不能自动追加该授权，不能通过 CLI/env 换账本。
- 解除上限后仍先记录每次物理请求，再按有效 Provider usage 和已核实费率计量；超出请求 token 上界的有效 usage 作为显式 `usageBound` 诊断随结算保留，不再因金额/该 token 边界暂停。坏账本、畸形用量、未定价路由和过期报价仍不能冒充可靠计量。旧有上限模式与历史 halt 只有明确匹配的授权才可解除，其他未知请求不因此退款。
- 保留最新 Flash、thinking 和原输出限制。先集中修复并完成免费验证，再在真实模型验收确有必要且有可用授权时运行；不逐小改动重跑整段任务。
- 模型目录缺项不等于端点拒绝：仅已核实文档中的固定 Flash 路由可在目录缺项时继续尝试，未知路由仍拒绝，不替换别名。显式 `ANERA_ARENA_CANARY_MODE=capability` 通过同一计量传输最多发一条文本和一条小型合成图像请求；成功只证明请求被接受，不证明模型身份、视觉理解或任务质量。
- 公共读取验收传输与模型预算隔离：未定价的非 GET/HEAD 工具请求在派发前以普通工具能力错误拒绝，不设置整个模型预算的停止状态。既有执行器可回退直接读取；直接读取也失败时必须保留失败，不伪造成功。只豁免当前自有 Anera 服务根路径的 GET，其余请求仍经过原公共网络安全检查。GET/HEAD 策略不代表所有 GET API 都免费；新增收费服务仍需单独确认费率和计量。
- 一轮真实验收只使用最近终态的隔离副本；开始后冻结代码，继续观察同一进程，不能把轮询当成重启理由。失败先按日志定位模块边界、合并修改并只跑受影响的免费用例；没有改变生产方案或关键输入，不启动同输入付费重跑。通过的无关层不在每次恢复前重复执行，延期层也不能算通过。
- 长运行观察订阅新增持久化事件，不每秒重读完整会话/工作区/历史；开始和结束仍读取真实快照，最终验收保留完整日志。报告分开记录产品终态与观察器的 `terminal / deadline / budget / observer_error` 原因，测试窗口取消不冒充模型自主失败或验收通过。订阅须在启动前建立，结束清理；默认 900 秒窗口、取消后的结算等待及所有验收条件不变。
- 分析上下文成本时，先核对实际 Provider 投影；磁盘上的完整参考 profile 不等于重复发送给模型的数据。自动摘要的字节压力与 token 估算共用投影后的上下文口径，计入 system、工具定义和当前补充控制。`context.compacted.contextByteBasis = provider_context_v1` 的 `beforeBytes/afterBytes` 是该上下文字节数；`beforeDurableMessageBytes/afterDurableMessageBytes` 单列持久消息大小。没有此标记的旧事件保留原口径。字节减少不是供应商账单或最终质量通过，也不能释放费用预留。

## 上下文管理的零模型验证

`npx vitest run --project integration src/server/context-records.integration.test.ts src/server/context-pressure.integration.test.ts src/server/visual-compaction.test.ts --project module src/server/checkpoint-context.test.ts src/server/thinking-protocol.test.ts src/server/config.test.ts` 集中检查完整原文重建、会话隔离、哈希校验、调用配对/思考文本保留、真实 Agent 检索调度、持久计量与禁用 token 停止。

`npx tsx scripts/context-record-replay.ts ses_<id>` 只读测量保存快照的 provider 消息体缩减，并分页重建原文、复核源文件不变；临时归档用完清理，不改源会话。输出是 snapshot-only 消息字节数，不是完整任务、包含 system/tools 的请求 token、模型质量或实际费用节省证明。该命令不启动 Agent、不调用供应商。

## 复用与质量边界

工具能力/证据边界回归（零供应商调用）：`npx vitest run --project module src/server/capability-evidence.test.ts src/server/deepseek.test.ts src/server/length-tool-continuation.test.ts src/server/reference-style.test.ts --project integration src/server/context-records.integration.test.ts src/server/visual-phase-context.integration.test.ts`。覆盖所有 thinking 模式的实际请求 JSON、截断续写、参数约束、无工具 Final、阶段控制中的只读定位符，以及绘制属性/字符串塞值/未使用规则的正反例。真实账单节省与全任务质量仍需单独验收，不从离线用例推断。

付费入口各模式的会话、产物、报告以及失败预检记录均保存到 checkout 内 `.anera/canary-runs/run-*`，不使用 OS 临时目录作为唯一证据来源。每次新建私有目录、保留失败结果、共用原预算账本；默认 local-only preflight 不创建运行目录或访问网络。路径仅是存储位置，不构成验收证明。若旧源快照已丢失，不得凭旧报告文字伪造恢复条件或立即重新付费生成；先记录证据缺口。目录被 gitignore 排除，不应上传用户会话或凭据。

`npm run test:ui` 和手工 `long-thought-ui-server.ts` 同样使用该持久根，但禁用 text/Vision/tools，属于零 Provider 调用的真实浏览器验证。当前 10 项交互包含自动定位不带动画、展开时暂停/恢复尾随、折叠后阅读位置恢复与隐藏增量、完成后的显式选择、预览及移动端/减少动效。报告根据全部检查实际执行结束判定，不再维护易失配的固定检查数量；失败报告必须返回非零退出码。按变化边界集中运行，不用它代替模型生成质量验收。

真实历史 UI 不必重新生成：设置 `ANERA_UI_REPLAY_SESSION_DIR=/absolute/data/sessions/ses_...` 后运行 `npx tsx scripts/long-thought-ui-server.ts`（保持 stdin 打开）。仅接受无待恢复人工交互的终态会话，先复制完整 journal/CAS/workspace 到新的持久目录，拒绝符号链接、身份错配与目标覆盖，再启动真实 App。HTTP 仅允许 GET/HEAD；真实模式 stdin 仅允许 `status`/`shutdown`。退出时对源的全部文件哈希复核，text/Vision/tools 尝试必须为零；连接器状态探测独立计数并始终在网络前拒绝。历史 usage 保留原值，不算本次费用。保留 `ui-replay-report.json`，不要把该回放或标准 viewport 渲染结果当成所有展示尺寸的通过证据。

默认交付的模型 scope/limitations 仅以声明来源、内容指纹和数量索引提供；完整文字仍在同快照内容视图。需要实质解释或必要限制时仍可扩展，不能按索引数量推断“有缺陷”或“已解决”。独立成品审阅保留全部观察文字与来源摘录，隔离旧模型解释；单测应同时检查隔离和证据保留，而非仅断言输入更短。

成品内容审阅在研究与确定性源码检查之后、渲染/交互/展示之前运行；已有问题走正常 read/edit 修复，不重复购买相同问题诊断。显式空 verdict 的私有收据绑定审阅版本、原任务和整个证据输入（含文件字节哈希），可跨 Continue 复用，Final 不再重复同版本成品审阅。CSS、未投影文字、来源、正文、任务或审阅器版本变化都失效；收据不代表事实认证，也不代替 Browser/Vision/Final 门禁。测试同时断言调用顺序、变更/取消时禁止写入收据、失败请求计量、重载复用和新任务/撤回的状态清理。

计划、审阅、时间上下文、修复与handoff共用 `activeTaskRequestEvents` 的任务投影。只有已有任务后的纯Continue/Resume控制消息可不改变需求身份；完整journal与模型对话不删除该消息。新附件、结构化反馈、实质追加需求和第一条控制消息保留。回归须走实际 `submit("Continue")` 并断言既有内容收据复用，不能仅测 `resume()` 或单独的plan hash。已完成源需集中重验时，显式 `ANERA_ARENA_CANARY_MODE=continue` 只在新隔离副本提交Continue；`resume`仍拒绝completed，不伪造failed或删除Final。

当前生产审阅同时返回 `artifactIssues` 与 `taskFulfillment`，在同一次模型请求中分开评估已有内容的事实问题和原始需求满足性。只有事实问题为空且需求明确 `satisfied` 才生成包含两种判定的收据；旧事实收据不能自动升级。需求缺失项必须引用原始用户要求，不能从成品/计划/模型建议创造新任务。原有内容全部准确但缺少必要内容时，允许 `artifactIssues:[]` 与 `needs_work` 并存；仅加免责声明仍须复审。来源缺失可受控重入已有研究依赖，不开放任意写入或跳过后续验证。测试按真实状态/边界分组，不能通过换题材重复同一夹具来宣称跨类别验收；通用合约模块和当前接入适配器的范围要分别报告。

语义审阅实验也必须有退出标准：2026-09-10尝试同请求的来源/内容双向覆盖表，免费协议测试通过并不代表模型效果改善。单向版本仍漏检，双向版本连续三段只思考后无有效判定，因此撤回生产接入并保留隔离实验与失败报告，不作为新默认门禁；当前仍为artifact-content-v1。没有改质量、thinking或输出限额来包装实验成功，也不重跑未变输入。后续不能仅凭“协议更严格”恢复该方案，须提出新的可验证设计依据。

研究计划的适用性与来源字节分开验证：控制器在成功 `record_research_brief` 的终态事件中记录任务/计划指纹，投影到 `briefTaskBinding`；不信任模型参数/工具正文里自称的绑定，也不为旧事件补造绑定。原始任务/时间范围或计划版本不匹配时保留来源、重开研究评审；当前明确缺少计划时，不能从历史消息回退恢复它。纯 Continue/Resume 不重置计划，实质反馈或新附件保留为任务范围变化。测试夹具若明确从“已评审计划”开始，应显式生成测试绑定；旧/错误绑定的迁移和恢复必须单独覆盖，禁止在生产中为通过旧夹具自动升级。

工具计量只在拥有传输边界的代码明确证明未派发时记 `not_dispatched / calls:0`。普通错误、HTTP 失败、超时、取消、仅伪造同名异常或字段都保守计数；不得因此退回模型预算、历史未知请求或较早重定向的计量。测试用真实 ToolExecutor 的提取、搜索、图片搜索和失败回退路径验证这一契约，不改供应商历史账本。

JSON-only 内部阶段采用请求级供应商输出契约，同时保留完整 schema/证据验证；不得据 JSON 合规宣称事实正确。测试覆盖普通调用不受影响、transport/空响应重试、reasoning-only 续写、非独立 JSON 后缀拼接及逐次计量。摘要事件的 `checkpointInputBasis:messages_json_v1` 指纹绑定选中的逻辑消息输入，不标识完整 HTTP 请求或续写；优先核对真实选中输入，不能从持久快照大小推断付费成本。

图形夹具可在**同一次测试文件运行**内按完整来源身份复用不可变参考采集：输入、输出深拷贝，失败不缓存。源浏览器在每次 cache miss 内创建并完成清理后才发布 profile，不跨用例空闲驻留；每个候选仍使用新 BrowserManager/context/server，结束后清理。生命周期、取消、网络安全、无效来源和字体/runtime 传输测试不经过成功缓存。跨运行不保存“参考已通过”来替代实际验收。

文字碰撞证据 v2 使用 Range 粗范围与字体实际字形垂直边界分层测量，不能把字号包含的空白直接认定为可见碰撞。v1 范围框证据只保留为历史，恢复时必须重新采集参考与候选；当前渲染验收版本为 `render-layout-surfaces-v5`。真实碰撞、裁剪、中文混排、缩放和测量无法完成的情况均需测试。`rendered-text-layout-replay.ts` 的显式 `ANERA_TEXT_FIT_PHASE=cover` 或 `closing` 可以对终态失败/取消副本做零模型、只读单阶段诊断；不把局部通过当作全部页面或模型交付通过。其证据同样保存在项目内持久目录。

v4在设计尺寸验证外增加 `compact-stage-v1`：宽高各除以3的确定性尺寸，记录根数量/活动所有权、活动根与视口相交、页面身份、viewport与原状态恢复；也覆盖非代表内容页。证据在原文件/来源/字体/page epoch身份下持久化，旧v3收据不复用为v4。恢复后的设计阶段指纹必须一致，无法恢复不能静默改控制器来通过。该探针仅证明代表性最小可见性，不证明全部文字可读、无导航遮挡或所有尺寸响应式；真实交付iframe仍应独立只读核验。测试覆盖设计尺寸通过但紧凑消失、非代表页失败、resize改变所有权/恢复失败和观测缺口，保留原截图尺寸及全部既有负例。

v5进一步在同一surface中采样活动根内可见文字Range，按实际命中层检测外部语义控件拦截；旧v4成功收据必须重验。检测不依赖任务、模板名称或控件语言，覆盖开放shadow DOM，并排除自身内容控件、隐藏、pointer-through及位于内容下层的控件。上限为512个可见文本节点、1024个区域、16个碰撞记录；超界是观测缺口，已确认缺陷仍保留。它不证明逐字字形、非交互遮挡、closed shadow DOM或全部尺寸均通过。真实inline和Viewer的开关/导航需单独零模型回放，不用设计尺寸通过代替。

预览生命周期按资源归属验证：App直接读取的持久workspace文件不依赖预览进程存活；其他进程URL仍受运行状态保护。测试覆盖五种进程状态、缺失URL、外部/近似路径和无关Viewer，真实终态回放需验证点击Open后不被休眠状态立即关闭、原生导航和关闭后的Workspace恢复。这个UI可用性判断不替代文件存在、权限、字体证据或服务端安全校验；禁止为了让回放通过把源状态改为running。

参考观测与恢复的失败按归属验证：二维transform文字通过局部字体边界及仿射多边形测量，奇异反解/3D/不支持属性仍为未知，不可跳过。来源观测不可用由工具实现输出私有能力失败，控制器持久化全部并行结果后停止自动参数纠错；不从正文关键词或伪造异常名认定。`npx tsx scripts/reference-observation-replay.ts /absolute/session-dir` 可零模型读取原始来源；`--controller` 在隔离副本重放一个工具提案，验证真实依赖提交和下一阶段。两者均非生成验收。进展恢复断言应核对重试窗口归零、新证据身份及后续能力，不要求删除可审计历史；验收器自身断言错误应对保存结果补验，保留最初失败报告，不重复整个浏览器/模型流程。

验证结果的 pass/fail/inconclusive 分类只决定恢复路径，不授予交付。测试必须同时覆盖“观测不完整但已证明具体缺陷”和“只有观测缺口”：保留已知缺陷，未知不推断为通过，也不能单凭缺口开放编辑。计数取全部检查结果，不从被截断的文字诊断反推；旧或畸形元数据保守保留缺陷路由。当前三态协议已接入渲染适配器，不代表所有验证器已迁移。

新增回归应测试模块不变量及正反例，优先扩展现有参数化矩阵；任务里的具体人物、日期、文件名只能作为最小复现夹具，不能进入生产分支或通用路由规则。模型响应夹具验证协议与状态机，真实浏览器验证交互与渲染，真实模型验收验证生成质量；三者不能相互冒名替代。

结构化响应 envelope 的提示与校验共用代码契约；回归覆盖缺字段、类型错误、额外字段，以及一次纠正后继续正常领域验证。诊断仅包含代码拥有的字段名/类型与额外字段数量，不记录模型未知键值或私有思考；调用方证据缺失不是模型格式错误，不为此购买格式纠正。缺少需求评估不能默认通过，额外字段不静默剥除，语义/引文/发布门禁不变。没有原始响应的历史失败只能定位到已记录边界，不能把新回放响应当作历史原文。

审阅出站消息对已知v1证据直接使用对象，解码/缺失语义由服务器system控制提供；旧或未知格式仍为不可信字符串。回归应在实际模型消息上比较正文、来源关联、声明隔离和遗漏字段，不只验证中间context。持久收据与修复构造器仍接受原context字符串；测试模拟出站对象时应显式序列化后再调用这些持久边界，不修改生产校验来适配过期夹具。

CI 会取消同分支过时运行。PR 验证受影响边界（包括浏览器），主分支和手动完整验收保留全量；不自动执行付费测试。没有通过的层继续标为未验证，不因降低频率而放宽断言、门槛或超时。

## 集中跨任务验收

```sh
ANERA_ARENA_CANARY_MODE=general npm run test:paid
```

与原入口共用官方目录检查、生产 Flash/thinking/续写配置及同一个物理请求账本。当前只包含代码修改和两页 PDF 两个独立任务，各执行一次；不自动扩张为全量 benchmark，不启动 App/Browser。代码在真实 OS 沙箱内执行原测试及独立边界测试，检查原程序语句被保留（允许新增测试和格式变化，字节是否不变另行记录）；PDF 核对实际解析、字节收据、完整覆盖和展示事件。渲染外观必须另做只读检查，`visualAcceptance` 不由文字解析自动设为成功。没有配置报价的外部工具 Provider 被阻止，PDF npm 库安装保留正常受控安装流程。

生成产物、每项结果和失败报告保留在隔离根目录。验收器自身判据有误时，修正其独立测试并对已保存结果补充核验，不覆盖旧报告或立即为同一产物再付费生成。

## 只检查当前失败的审阅边界（只读）

已有真实 canary 的终态产物需要定位成品审阅问题时，可使用同一入口的只读模式。artifact 诊断可读取 completed/failed/cancelled/timed_out 的匹配快照，不要求先付费通过渲染和展示；真实文件仍必须 hash_verified。运行中、状态/路径/型号不一致的快照拒绝：

```sh
ANERA_ARENA_CANARY_MODE=review ANERA_ARENA_CANARY_SOURCE_ROOT=/absolute/path/to/canary-root npm run test:paid
```

它仍先核对官方模型目录，使用相同 Flash 配置和固定费用账本。只把原任务与 hash-bound 成品/来源证据交给生产的独立成品审阅器，不传 Final 草稿；最多 2 个逻辑审阅（正常请求 + 至多一次格式纠正），每次保留生产客户端的重试与长度续写配置，记录每笔实际 HTTP 请求，不以禁用正常续写降低测试质量。不启动 App/Browser、不修改/恢复源会话、不发布 Final。输出独立报告并比对源 state/events/HTML 的前后哈希。`acceptanceVerified:false` 恒定：协议通过不是事实真值，更不是端到端验收。成品问题仍需模型正常修复并完成必要复验；不要为定位审阅格式问题反复运行整条流程。

只修改最终交付审阅边界时，同一入口可加 `ANERA_REVIEW_TARGET=handoff`：必须是 completed、已展示且无剩余验证阶段的快照，并有已保存 Final。以原需求和当前 hash-bound 证据检查生产交付器，已保存 Final 仅在程序侧用于差异记录，不作为模型输入。它不重新生成成品、不发布结果、不冒充完整成品审阅或端到端通过。默认仍为 `artifact`；未知 target 拒绝。缺失/畸形的可选 corrections 元数据只记录安全诊断，实际文本变化由程序记录；不能用协议合规或解释元数据代替内容正确性。

交付器默认接收完成凭据；若明确的详细回答需要正文，可申请一次同快照完整证据，不授予工具权限或重新采集。该模式最多三个逻辑请求（初次、至多一次证据扩展、至多一次格式修复），不按证据扩展重置修复次数；artifact 模式仍最多两个。每个逻辑请求保留生产续写/transport 配置，所有物理请求共用原账本计费。未知/旧凭据格式保留完整上下文，已知成品矛盾仍走完整内容审阅；不能因减少输入而隐藏限制或直接判内容通过。

内部凭据与交付consumer view分开：默认位置投影不附字节/修订/源元素数量/模型声明索引/链接页码清点；完成记录使用scope/outcome，不把performed升级为pass，也不传执行阶段的动作指令。需要详细审计、校验和、内容或必要限制时，原始操作控制与完整现有有界证据仍在同快照展开中。回归必须检查原数据和数组隔离、用户需求原样保留、未知schema回退、展开后精确身份可取，以及实际控制器在文件变化/取消/超时后的发布拦截。不要再以“默认模型输入包含文件hash”代替真正的发布前身份校验；投影不是新的完成权限。只读文本变短不等于语义/整个任务通过，不能为满足字数目标重复付费。

内容审阅、Final 与精确输出整理继承客户端正常生成额度，不因为预期 JSON 短就将思考与输出共同压到 4,096/2,048 tokens；返回正文的独立字节与 schema 限制保留。DeepSeek 官方当前协议说明无工具请求会忽略历史 reasoning_content，因此无正文的 length 后续请求是重新回答，不冒充已有公开前缀的续写、不把私有思考塞进公开正文，也不添加虚假工具维持思考状态。非思考 checkpoint 的单独额度保持原状。
